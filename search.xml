<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>面试 | CSS</title>
    <url>/2021/06/02/CSS/</url>
    <content><![CDATA[<h1 id="1-css-sprite是什么，有什么优缺点（精灵图）"><a href="#1-css-sprite是什么，有什么优缺点（精灵图）" class="headerlink" title="1. css sprite是什么，有什么优缺点（精灵图）"></a>1. css sprite是什么，有什么优缺点（精灵图）</h1><ul>
<li><p>概念：将多个小图片拼接到一个图片中，通过<code>background-position</code>和元素尺寸调节需要显示的背景图案</p>
</li>
<li><p>优点：</p>
<ul>
<li>减少<code>HTTP</code>请求数，极大地提高页面加载速度</li>
<li>增加图片信息重复度，提高压缩比，减少图片大小</li>
<li>更换风格方便，只需再一张或几张图片上修改颜色或样式即可实现</li>
</ul>
</li>
<li><p>缺点</p>
<ul>
<li>图片合并麻烦</li>
<li>维护麻烦，修改一个图片可能需要重新布局整个图片和样式</li>
</ul>
</li>
</ul>
<h1 id="2-display-none和visibility-hidden的区别"><a href="#2-display-none和visibility-hidden的区别" class="headerlink" title="2. display: none和visibility: hidden的区别"></a>2. display: none和visibility: hidden的区别</h1><ul>
<li>相同点：它们都能让元素不可见</li>
<li>区别：<ul>
<li><code>display: none</code>会让元素完全从渲染树中消失，渲染的时候<strong>不占据任何空间</strong>；|| <code>visibility: hidden</code>不会让元素从渲染树中消失，渲染树元素继续占据空间，只是内容不可见；</li>
<li><code>display: none</code>是非继承属性，子孙节点消失是由于元素从渲染树消失造成的，通过修改子孙节点属性无法显示；<code>visibility: hidden</code>是继承属性，子孙节点消失是由于继承了<code>hidden</code>，通过设置<code>visibility: visible</code>可以让子孙节点显示；</li>
<li>修改常规流中元素的<code>display</code>通常会造成文档重排；<code>修改visibility</code>属性只会造成本元素的重绘；</li>
<li>读屏器不会读取<code>display: node</code>元素内容，会读取<code>visibility: hidden</code>元素内容；</li>
<li><code>visibility: none</code>不会影响计数器计数（ol标签）；</li>
</ul>
</li>
</ul>
<h1 id="3-link和-import的区别"><a href="#3-link和-import的区别" class="headerlink" title="3. link和@import的区别"></a>3. link和@import的区别</h1><ul>
<li><code>link</code>是<code>HTML方</code>式，除了加载CSS外，还能用于定义RSS、定义rel连接属性等作用；<code>@import</code>是CSS方式，只能用于加载CSS；</li>
<li><code>link</code>最大限度支持并行下载，<code>@import</code>过度嵌套导致串行下载，出现<code>FOUC</code>（文档样式短暂失效）；页面被加载的时候，<code>link</code>会同时被加载，<code>@import</code>引用的CSS会等到页面被加载完再加载；</li>
<li><code>link</code>可以通过<code>rel=&quot;alternate stylesheet&quot;</code>指定候选样式；</li>
<li>浏览器对<code>link</code>支持早于<code>@import</code>，可以使用<code>@import</code>对老浏览器隐藏样式；<code>@impor</code>是CSS2.1提出的，只有再IE5以上才能被识别，<code>link</code>是XHTML标签，无兼容性问题；</li>
<li><code>@import</code>必须在样式规则之前，可以在css文件中引用其他文件；</li>
<li><code>link</code>优于<code>@import</code></li>
</ul>
<h1 id="4-什么是FOUC，如何避免？"><a href="#4-什么是FOUC，如何避免？" class="headerlink" title="4. 什么是FOUC，如何避免？"></a>4. 什么是FOUC，如何避免？</h1><ul>
<li>FOUC：<code>Flash of Unstyled Content</code>，用户定义样式表加载之前浏览器使用默认样式显示文档，用户样式加载渲染之后再重新显示文档，造成页面闪烁；</li>
<li><strong>解决办法</strong>：把样式表放到文档的<code>&lt;head&gt;</code>；</li>
</ul>
<h1 id="5-如何创建块级格式化上下文（block-formatting-context），BFC有什么用"><a href="#5-如何创建块级格式化上下文（block-formatting-context），BFC有什么用" class="headerlink" title="5. 如何创建块级格式化上下文（block formatting context），BFC有什么用"></a>5. 如何创建块级格式化上下文（block formatting context），BFC有什么用</h1><ul>
<li><p><strong>BFC（Block Formatting Context）</strong>：块级格式化上下文，是一个独立的渲染区域，让处于BFC内部的元素与外部的元素相互隔离，使内外元素的定位不会相互影响；</p>
</li>
<li><p><strong>触发条件，任意一条</strong></p>
<ul>
<li><p><code>float</code>的值不为<code>none</code> || 浮动元素</p>
</li>
<li><p><code>overflow</code>的值不为<code>visible</code></p>
</li>
<li><p><code>display</code>的值为<code>table-cell</code>、<code>table-caption</code>、<code>inline-block</code>、<code>flex</code> 、i<code>nline-flex</code>之一</p>
</li>
<li><p><code>position</code>的值不为<code>static</code>或者<code>relative</code>中的任何一个 || <code>position: absolute或fixed</code></p>
</li>
<li><p>根元素</p>
<blockquote>
<p>在<code>IE</code>下，<code>Layout</code>可以通过<code>zoom: 1</code>触发</p>
</blockquote>
</li>
</ul>
</li>
<li><p><strong>.BFC布局与普通文档流布局区别</strong></p>
<ul>
<li><p>普通文档流布局：</p>
<ul>
<li>浮动的元素是不会被父级计算高度</li>
<li>非浮动元素会覆盖浮动元素的位置</li>
<li><code>margin</code>会传递给父级元素</li>
<li>两个相邻元素上下的<code>margin</code>会重叠</li>
</ul>
</li>
<li><p>BFC布局规则</p>
<ul>
<li>浮动的元素会被父级计算高度（父级元素触发了<code>BFC</code>）</li>
<li>非浮动元素不会覆盖浮动元素的位置（非浮动元素触发了<code>BFC</code>）</li>
<li><code>margin</code>不会传递给父级元素（父级元素触发<code>BFC</code>）</li>
<li>属于同一个<code>BFC</code>的两个相邻元素上下<code>margin</code>会重叠</li>
</ul>
</li>
</ul>
</li>
</ul>
<ul>
<li><strong>开发中的应用</strong><ul>
<li>阻止<code>margin</code>重叠</li>
<li>可以包含浮动元素——清除内部浮动（清除浮动元素的原理是两个<code>div</code>都位于同一个<code>BFC</code>区域之中）</li>
<li>自适应两栏布局</li>
<li>可以阻止元素被浮动元素覆盖</li>
</ul>
</li>
</ul>
<h1 id="6-display、float、position的关系-待完善…"><a href="#6-display、float、position的关系-待完善…" class="headerlink" title="6. display、float、position的关系  待完善…"></a>6. display、float、position的关系  <strong>待完善…</strong></h1><ul>
<li>如果display的取值为none，那么position和float都不起作用，这种情况下元素不产生框</li>
<li>如果position取值为absolute或者fixed，框就是绝对定位的，float的计算值为none，display根据下面的表格进行调整</li>
<li>如果float不是none，框是浮动的</li>
</ul>
<h1 id="7-清除浮动的几种方式，各自的优缺点"><a href="#7-清除浮动的几种方式，各自的优缺点" class="headerlink" title="7. 清除浮动的几种方式，各自的优缺点"></a>7. 清除浮动的几种方式，各自的优缺点</h1><blockquote>
<p>当元素设置<code>float</code>浮动后，该元素会脱离文档并向左或向右浮动，直到碰到父元素或者另一个浮动元素，浮动元素会造成父元素高度塌陷，所以设置完浮动之后需要清除浮动</p>
</blockquote>
<ul>
<li><p>父级div定义height</p>
</li>
<li><p>父级div也浮动，需要定义宽度</p>
</li>
<li><p><strong>BFC</strong></p>
<ul>
<li>给父容器加上<code>overflow: hidden</code>，加上之后形成<code>BFC</code>，需要计算超出的大小来隐藏，所以父容器会撑开放入子元素，同时计算浮动的子元素</li>
<li>缺点：一旦子元素大小超过父容器大小就会显示异常</li>
</ul>
</li>
<li><p><strong>使用带有<code>clear</code>属性的空元素</strong></p>
<ul>
<li>在浮动元素后面添加一个不浮动的空元素，父容器必须考虑浮动子元素的位置，子元素出现在浮动元素后面，所以显示出来就正常了，同时要给空元素加上<code>style = &quot;clear: both&quot;</code></li>
<li>缺点：需要添加额外的html标签，这违背了语义网的原则</li>
</ul>
</li>
<li><p><strong>使用伪元素<code>::after</code></strong></p>
<ul>
<li><p>它会在父容器尾部自动创建一个子元素，原理和空元素一样，可以把它设置为<code>height: 0</code>不显示，<code>clear: both, display: block</code>，保证空白字符不浮动区块</p>
</li>
<li><p>after不支持IE6，只需要添加上<code>zoom: 1</code>，这个是激活父元素的<code>haslayout</code>属性，让父元素拥有自己的布局</p>
</li>
</ul>
</li>
</ul>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.clearfix</span><span class="selector-pseudo">::after</span> &#123;</span><br><span class="line">  <span class="attribute">content</span>: <span class="string">''</span>;</span><br><span class="line">  <span class="attribute">height</span>: <span class="number">0</span>;</span><br><span class="line">  <span class="attribute">clear</span>: both;</span><br><span class="line">  <span class="attribute">display</span>: block;</span><br><span class="line">&#125;</span><br><span class="line"><span class="selector-class">.clear</span> &#123;</span><br><span class="line">  <span class="attribute">zoom</span>: <span class="number">1</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h1 id="8-为什么要初始化CSS样式？"><a href="#8-为什么要初始化CSS样式？" class="headerlink" title="8. 为什么要初始化CSS样式？"></a>8. 为什么要初始化CSS样式？</h1><ul>
<li>因为浏览器的兼容问题，不同浏览器对有些标签的默认值是不同的，如果没对<code>css</code>初始化往往会出现浏览器之间的页面显示差异</li>
<li>初始化样式会对<code>SEO</code>有一定的影响，力求影响最小的情况下初始化样式</li>
</ul>
<h1 id="9-CSS3有哪些新特性"><a href="#9-CSS3有哪些新特性" class="headerlink" title="9. CSS3有哪些新特性"></a>9. CSS3有哪些新特性</h1><ul>
<li><p>新增选择器：<code>p:nth-child (n) {color: rgba(255, 0, 0, 0.75)}</code></p>
</li>
<li><p>弹性盒模型：<code>display: flex</code>;</p>
</li>
<li><p>多列布局：<code>columm-count: 5</code>;</p>
</li>
<li><p>媒体查询：<code>@media (max-width: 480px) {.box {column-count: 1;}}</code></p>
</li>
<li><p>个性化字体：<code>@font-face {font-family: BorderWeb; src: url (BORDERWO.eot)}</code></p>
  <figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="keyword">@font-face</span> &#123;</span><br><span class="line">  <span class="attribute">font-family</span>: myFirstFont;</span><br><span class="line">  <span class="attribute">src</span>: <span class="built_in">url</span>(sansation_light.woff)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



</li>
</ul>
<ul>
<li><p>颜色透明度：<code>color: rgba(255, 0, 0, 0.75);</code></p>
</li>
<li><p>圆角：<code>border-radius: 5px;</code></p>
</li>
<li><p>渐变：<code>background: linear-gradient(red, green, blue);</code></p>
</li>
<li><p>阴影：<code>box-shadow: 3px 3px 3px rgba(0, 64, 128, 0.3);</code></p>
</li>
<li><p>倒影：<code>box-reflect: blow 2px;</code></p>
</li>
<li><p>文字装饰：<code>text-stroke-color: red;</code></p>
</li>
<li><p>文字溢出：<code>text-overflow: ellipsis;</code></p>
</li>
<li><p>背景效果：<code>background-size: 100px 100px;</code></p>
</li>
<li><p>边框效果：<code>border-image: url(bt_blue.png) 0 10;</code></p>
</li>
<li><p>转换：</p>
<ul>
<li>旋转：<code>transform: rotate(20deg)</code></li>
<li>倾斜：<code>transform: skew(150deg, -10deg);</code></li>
<li>位移：<code>transform:translate(20px, 20px);</code></li>
<li>缩放：<code>transform: scale(.5);</code></li>
</ul>
</li>
<li><p>平滑过渡：<code>tansition: all .3s ease-in .1s</code></p>
</li>
<li><p>动画：<code>@keyframes anim-1 {50% {border-radius: 50%;}} animation: anim-1 1s;</code></p>
  <figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">transition</span>：</span><br><span class="line"><span class="selector-tag">div</span>&#123;</span><br><span class="line">    <span class="attribute">transition-property</span>:width;</span><br><span class="line">    <span class="attribute">transition-duration</span>:<span class="number">1s</span>;</span><br><span class="line">    <span class="attribute">transition-timing-function</span>:linear;</span><br><span class="line">    <span class="attribute">transition-delay</span>:<span class="number">25</span>;</span><br><span class="line">    <span class="comment">/*简写形式*/</span></span><br><span class="line">    <span class="attribute">transition</span>:width <span class="number">1s</span> linear <span class="number">25</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="selector-tag">transform</span>:</span><br><span class="line"><span class="selector-tag">div</span>&#123;</span><br><span class="line">    <span class="attribute">transform</span>:<span class="built_in">rotate</span>(<span class="number">7deg</span>);</span><br><span class="line">    <span class="attribute">-ms-transform</span>:<span class="built_in">rotate</span>(<span class="number">7deg</span>);<span class="comment">/*IE9*/</span></span><br><span class="line">    <span class="attribute">-moz-transform</span>:<span class="built_in">rotate</span>(<span class="number">7deg</span>);<span class="comment">/*FireFox*/</span></span><br><span class="line">    <span class="attribute">-webkit-transform</span>:<span class="built_in">rotate</span>(<span class="number">7deg</span>);<span class="comment">/*Safari和Chrome*/</span></span><br><span class="line">    <span class="attribute">-o-transform</span>:<span class="built_in">rotate</span>(<span class="number">7deg</span>);<span class="comment">/*Opwea*/</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="selector-tag">animation</span>:</span><br><span class="line"><span class="keyword">@keyframes</span> myAnimation&#123;</span><br><span class="line">    0%&#123;<span class="attribute">background</span>:red;<span class="attribute">width</span>:<span class="number">100px</span>;&#125;</span><br><span class="line">    25%&#123;<span class="attribute">background</span>:orange;<span class="attribute">width</span>:<span class="number">200px</span>;&#125;</span><br><span class="line">    50%&#123;<span class="attribute">background</span>:yellow;<span class="attribute">width</span>:<span class="number">100px</span>;&#125;</span><br><span class="line">    75%&#123;<span class="attribute">background</span>:green;<span class="attribute">width</span>:<span class="number">200px</span>;&#125;</span><br><span class="line">    100%&#123;<span class="attribute">background</span>:blue:width:<span class="number">100px</span>;&#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="selector-id">#box</span>&#123;</span><br><span class="line">    <span class="attribute">animation</span>:myAnimation <span class="number">5s</span>;</span><br><span class="line">    <span class="attribute">width</span>:<span class="number">100px</span>;</span><br><span class="line">    <span class="attribute">height</span>:<span class="number">100px</span>;</span><br><span class="line">    <span class="attribute">background</span>:red;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>CSS3新增伪类有哪些？  需要查找相应资料确认</p>
<blockquote>
<p>单冒号用于CSS3伪类，双冒号用于CSS3伪元素。伪元素由双冒号和伪元素名称组成。</p>
</blockquote>
<ul>
<li><code>p:first-of-type</code>：选择属于其父元素的首个<code>&lt;p&gt;</code>元素的每个<code>&lt;p&gt;</code>元素</li>
<li><code>p:last-of-type</code>：选择属于其父元素的最后一个<code>&lt;p&gt;</code>元素的每个<code>&lt;p&gt;</code>元素</li>
<li><code>p:only-of-type</code>：选择属于其父元素唯一的<code>&lt;p&gt;</code>元素的每个<code>&lt;p&gt;</code>元素</li>
<li><code>p:only-child</code>：选择属于其父元素的唯一子元素的每个<code>&lt;p&gt;</code>元素</li>
<li><code>p:nth-child(n)</code>：选择属于其父元素的第n个子元素的每个<code>&lt;p&gt;</code>元素</li>
<li><code>p:nth-last-child(n)</code>：选择属于其父元素的倒数第n个子元素的每个<code>&lt;p&gt;</code>元素</li>
<li><code>p:nth-of-type(n)</code>：选择属于其父元素第n个<code>&lt;p&gt;</code>元素的每个<code>&lt;p&gt;</code>元素</li>
<li><code>p:nth-last-of-type(n)</code>：选择属于其父元素倒数第n个<code>&lt;p&gt;</code>元素的每个<code>&lt;p&gt;</code>元素</li>
<li><code>p:last-child</code>：选择属于其父元素最后一个子元素的每个<code>&lt;p&gt;</code>元素</li>
<li><code>p:empty</code>：选择没有任何子元素的<code>&lt;p&gt;</code>元素（包括文本节点）</li>
<li><code>p:target</code> ：选择当前活动的<code>&lt;p&gt;</code>元素</li>
<li><code>:not(p)</code>：选择所有不是<code>&lt;p&gt;</code>元素的元素  <code>:not()</code>用于匹配不符合一组选择器的元素，防止特定的元素被选中</li>
<li><code>:enabled</code>：控制表单控件的启用状态</li>
<li><code>:disabled</code>：控制表单控件的禁用状态</li>
<li><code>:checked</code>：单选框或复选框被选中</li>
<li><code>p:root</code>：选择p元素在文档的根元素。在HTML中，根元素永远是HTML</li>
</ul>
</li>
</ul>
<h1 id="10-display有哪些值？说明它们的作用"><a href="#10-display有哪些值？说明它们的作用" class="headerlink" title="10. display有哪些值？说明它们的作用"></a>10. display有哪些值？说明它们的作用</h1><ul>
<li><p>CSS2</p>
<ul>
<li><p><code>block</code>：指定对象为块元素</p>
</li>
<li><p><code>inline</code>：指定对象为内联元素</p>
</li>
<li><p><code>none</code>：隐藏对象，不为被隐藏的对象保留其物理空间</p>
</li>
<li><p><code>inline-block</code>：指定对象为内联块元素</p>
</li>
<li><p><code>list-item</code>：指定对象为列表项目</p>
</li>
<li><p><code>table</code>：指定对象作为块元素级的表格，类似于<code>&lt;table&gt;</code></p>
</li>
<li><p><code>inherit</code>：从父元素继承<code>display</code>属性的值</p>
</li>
<li><p><code>inline-table</code>： </p>
<p>  指定对象作为内联元素级的表格。类同于html标签<table>（CSS2） </p>
</li>
<li><p><code>table-caption</code>： 指定对象作为表格标题。类同于html标签<caption>（CSS2） </p>
</li>
<li><p><code>table-cell</code>： 指定对象作为表格单元格。类同于html标签<td>（CSS2） </p>
</li>
<li><p><code>table-row</code>： 指定对象作为表格行。类同于html标签<tr>（CSS2） </p>
</li>
<li><p><code>table-row-group</code>： 指定对象作为表格行组。类同于html标签<tbody>（CSS2） </p>
</li>
<li><p><code>table-column</code>： </p>
<p>  指定对象作为表格列。类同于html标签<col>（CSS2） </p>
</li>
<li><p><code>table-column-group</code>： </p>
<p>  指定对象作为表格列组显示。类同于html标签<colgroup>（CSS2） </p>
</li>
<li><p><code>table-header-group</code>： </p>
<p>  指定对象作为表格标题组。类同于html标签<thead>（CSS2） </p>
</li>
<li><p><code>table-footer-group</code>： </p>
<p>  指定对象作为表格脚注组。类同于html标签<tfoot>（CSS2） </p>
</li>
</ul>
</li>
<li><p>CSS3</p>
<ul>
<li><code>run-in</code>： 根据上下文决定对象是内联对象还是块级对象。（CSS3） </li>
<li><code>box</code>： 将对象作为弹性伸缩盒显示。<em>（伸缩盒最老版本）</em>（CSS3） </li>
<li><code>inline-box</code>： 将对象作为内联块级弹性伸缩盒显示。<em>（伸缩盒最老版本）</em>（CSS3） </li>
<li><code>flexbox</code>： 将对象作为弹性伸缩盒显示。<em>（伸缩盒过渡版本）</em>（CSS3） </li>
<li><code>inline-flexbox</code>： 将对象作为内联块级弹性伸缩盒显示。<em>（伸缩盒过渡版本）</em>（CSS3） </li>
<li><code>flex</code>： 将对象作为弹性伸缩盒显示。<em>（伸缩盒最新版本）</em>（CSS3） </li>
<li><code>inline-flex</code>： 将对象作为内联块级弹性伸缩盒显示。<em>（伸缩盒最新版本）</em>（CSS3）</li>
</ul>
</li>
</ul>
<h1 id="11-介绍一下标准的CSS的盒子模型？低版本IE的盒子模型有什么不同的？-看书确定"><a href="#11-介绍一下标准的CSS的盒子模型？低版本IE的盒子模型有什么不同的？-看书确定" class="headerlink" title="11. 介绍一下标准的CSS的盒子模型？低版本IE的盒子模型有什么不同的？  看书确定"></a>11. 介绍一下标准的CSS的盒子模型？低版本IE的盒子模型有什么不同的？  看书确定</h1><blockquote>
<p>盒子模型有两种：<code>IE</code>盒子模型、<code>W3C</code>盒子模型；</p>
<ul>
<li>盒模型：内容（content）、填充（padding）、边界（margin）、边框（border）；</li>
<li>区别：<code>IE</code>的<code>content</code>部分把<code>border</code>和<code>padding</code>计算了进去。</li>
</ul>
</blockquote>
<ul>
<li><p>盒子模型构成：内容（content）、内填充（padding）、边框（border）、外边距（margin）</p>
</li>
<li><p><code>IE8</code>及其以下版本浏览器，未声明<code>DOCTYPE</code>，内容宽高会包含内填充和边框，称为怪异盒模型（<code>IE</code>盒模型）</p>
</li>
<li><p>标准（<code>W3C</code>）盒模型：元素宽度 = <code>width + padding + border + margin</code></p>
</li>
<li><p>怪异（IE）盒模型：元素宽度 = <code>width + margin</code>  || <code>width = content + padding + border</code></p>
</li>
<li><p>标准浏览器通过设置CSS3的<code>box-sizing: border-box</code>属性，触发“怪异模型”解析计算宽高</p>
</li>
<li><p><strong>box-sizing常用的属性有哪些？分别有什么用</strong></p>
<ul>
<li><p><code>box-sizing: content-box</code>：默认的标准（<code>W3C</code>）盒模型元素效果</p>
<ul>
<li>width和height只包括内容的宽和高，不包括边框、内边距、外边距（内边距、变宽和外边距都在这个盒子的外部）</li>
</ul>
</li>
<li><p>尺寸计算： width = 内容的宽度 || height = 内容的高度</p>
<ul>
<li><p>宽度和高度的计算值都不包含内容的边框（border）和内边距（padding）</p>
<p><img src="C:%5CUsers%5CAdministrator%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210426204824597.png" alt="image-20210426204824597"></p>
</li>
</ul>
</li>
<li><p><code>box-sizing: border-box</code>：触发怪异（<code>IE</code>）盒模型的效果</p>
<ul>
<li><p>width和height属性包括内容、内边距和边框，填充和边框将在盒子内</p>
</li>
<li><p>内容框不能未负，并且被分配到0，使得不可能使用border-box使得元素消失</p>
</li>
<li><p>尺寸计算：width = border + padding + 内容的宽度 || height = border + padding + 内容的宽度</p>
<p><img src="C:%5CUsers%5CAdministrator%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210426204854932.png" alt="image-20210426204854932"></p>
</li>
</ul>
</li>
<li><p><code>box-sizing: inherit</code>：继承父元素<code>box-sizing</code>属性的值</p>
</li>
<li><p><em><code>padding-box</code>：宽高包括了内边距？</em></p>
<figure class="highlight"><table><tr><td class="code"><pre><span class="line">//标准盒模型中，如果你给盒设置width和height，实际设置的是content-box。padding和border再加上设置的宽高一起决定整个盒子的大小</span><br><span class="line"><span class="selector-class">.box</span> &#123;</span><br><span class="line">  <span class="attribute">width</span>: <span class="number">350px</span>;</span><br><span class="line">  <span class="attribute">height</span>: <span class="number">150px</span>;</span><br><span class="line">  <span class="attribute">margin</span>: <span class="number">25px</span>;</span><br><span class="line">  <span class="attribute">padding</span>: <span class="number">25px</span>;</span><br><span class="line">  <span class="attribute">border</span>: <span class="number">5px</span> solid black;</span><br><span class="line">&#125;</span><br><span class="line">//如果使用标准盒模型，宽度 = (350 + 25 + 25 + 25 + 25 + 5 + 5) = 460px</span><br></pre></td></tr></table></figure>

</li>
</ul>
</li>
</ul>
<h1 id="12-CSS优先级算法如何计算？-看书确定"><a href="#12-CSS优先级算法如何计算？-看书确定" class="headerlink" title="12. CSS优先级算法如何计算？  看书确定"></a>12. CSS优先级算法如何计算？  看书确定</h1><ul>
<li>优先级就近原则，同权重情况下样式定义最近者为准</li>
<li>载入样式以最后载入的定位为准</li>
<li>优先级：<code>!important &gt; id &gt; class &gt; tag</code>；<strong><code>!important</code>比内联样式优先级高</strong></li>
</ul>
<h1 id="13-对BFC规范的理解？"><a href="#13-对BFC规范的理解？" class="headerlink" title="13. 对BFC规范的理解？"></a>13. 对BFC规范的理解？</h1><ul>
<li>一个页面是由很多个<code>box</code>组成的，元素的类型和<code>display</code>属性决定了这个box的类型</li>
<li>不同类型的<code>box</code>会参与不同的<code>Formatting Context</code>（决定如何渲染文档的容器），因此Box内的元素会以不同的方式渲染，也就是说BFC内部的元素和外部的元素不会相互影响</li>
</ul>
<h1 id="14-谈谈浮动和清除浮动"><a href="#14-谈谈浮动和清除浮动" class="headerlink" title="14. 谈谈浮动和清除浮动"></a>14. 谈谈浮动和清除浮动</h1><ul>
<li><p>浮动的框可以向左或者向右移动，知道他的外边缘膨胀包含框或另一个浮动框的边框位置；</p>
</li>
<li><p>由于浮动框不在文档的普通流中，文档的普通流的块框表现得就像浮动框不存在一样</p>
</li>
<li><p>浮动的块框会漂浮在文档普通流的边框上</p>
</li>
</ul>
<h1 id="15-position的值，relative和absolute的定位原点"><a href="#15-position的值，relative和absolute的定位原点" class="headerlink" title="15. position的值，relative和absolute的定位原点"></a>15. position的值，relative和absolute的定位原点</h1><ul>
<li><code>absolute</code>：生成<strong>绝对定位</strong>的元素，相对于static定位以外的第一个父元素进行定位</li>
<li><code>fixed</code>：生成<strong>绝对定位</strong>的元素，相对于浏览器窗口进行定位</li>
<li><code>relative</code>：生成<em>相对定位</em>的元素，相对于其正常位置进行定位</li>
<li><code>static</code>：默认值。没有定位，元素出现在正常的流中</li>
<li><code>inherit</code>：规定从父元素继承position的值</li>
</ul>
<h1 id="16-display-inline-block什么时候不会显示间隙"><a href="#16-display-inline-block什么时候不会显示间隙" class="headerlink" title="16. display: inline-block什么时候不会显示间隙"></a>16. display: inline-block什么时候不会显示间隙</h1><ul>
<li>移除空格</li>
<li>使用<code>margin</code>负值</li>
<li>使用<code>font-size: 0</code></li>
<li><code>letter-spacing</code></li>
<li><code>word-spacing</code></li>
</ul>
<h1 id="17-PNG-GIF-JPG的区别"><a href="#17-PNG-GIF-JPG的区别" class="headerlink" title="17. PNG/GIF/JPG的区别"></a>17. PNG/GIF/JPG的区别</h1><ul>
<li><p>GIF</p>
<ul>
<li>8位像素，256色</li>
<li><strong>无损压缩</strong></li>
<li>支持简单动画</li>
<li>支持<code>boolean</code>透明</li>
<li>适合简单动画</li>
</ul>
</li>
<li><p>JPEG</p>
<ul>
<li>颜色限于256</li>
<li><strong>有损压缩</strong></li>
<li>可控制压缩质量</li>
<li>不支持透明</li>
<li>适合照片</li>
</ul>
</li>
<li><p>PNG</p>
<ul>
<li>有<code>PNG8</code>和<code>truecolor PNG</code></li>
<li>PNG8类似GIF颜色上限为256，文件小，支持<code>alpha</code>透明度，无动画</li>
<li>适合图标、背景、按钮</li>
</ul>
</li>
<li><p>因为浏览器的兼容问题，不同浏览器对有些标签的默认值是不同的，如果没对<code>css</code>初始化往往会出现浏览器之间的页面显示差异</p>
</li>
<li><p>初始化样式会对<code>SEO</code>有一定的影响，力求影响最小的情况下初始化样式</p>
</li>
</ul>
<h1 id="18-行内元素float-left后是否变为块级元素"><a href="#18-行内元素float-left后是否变为块级元素" class="headerlink" title="18. 行内元素float: left后是否变为块级元素"></a>18. 行内元素float: left后是否变为块级元素</h1><blockquote>
</blockquote>
<h1 id="19-在网页中的应该使用奇数还是偶数的字体？为什么呢"><a href="#19-在网页中的应该使用奇数还是偶数的字体？为什么呢" class="headerlink" title="19. 在网页中的应该使用奇数还是偶数的字体？为什么呢"></a>19. 在网页中的应该使用奇数还是偶数的字体？为什么呢</h1><p>​    偶数字号相对更容易和web设计的其他部分构成比例关系</p>
<h1 id="20-before和-after中双冒号和单冒号，有什么区别？解释以下这两个伪元素的作用"><a href="#20-before和-after中双冒号和单冒号，有什么区别？解释以下这两个伪元素的作用" class="headerlink" title="20. ::before和: after中双冒号和单冒号，有什么区别？解释以下这两个伪元素的作用"></a>20. ::before和: after中双冒号和单冒号，有什么区别？解释以下这两个伪元素的作用</h1><ul>
<li>单冒号（:）用于CSS3伪类，双冒号（::）用于CSS3 伪元素</li>
<li>用于区分伪类和伪元素</li>
</ul>
<h1 id="21-如果需要手动写动画，你认为最小时间间隔是多久，为什么？（阿里）"><a href="#21-如果需要手动写动画，你认为最小时间间隔是多久，为什么？（阿里）" class="headerlink" title="21. 如果需要手动写动画，你认为最小时间间隔是多久，为什么？（阿里）"></a>21. 如果需要手动写动画，你认为最小时间间隔是多久，为什么？（阿里）</h1><p>多数默认显示器默认频率是<code>60HZ</code>，即<code>1</code>秒刷新<code>60</code>次，理论上最小间隔是<code>1 / 60 * 1000 = 16.7 ms</code></p>
<h1 id="22-CSS合并方法"><a href="#22-CSS合并方法" class="headerlink" title="22. CSS合并方法"></a>22. CSS合并方法</h1><p>避免使用<code>@import</code>引入多个<code>CSS</code>文件，可以使用<code>CSS</code>工具将<code>CSS</code>合并为一个<code>CSS</code>文件，例如使用<code>Sass\Compass</code>等</p>
<h1 id="23-CSS层叠的规则（权重）"><a href="#23-CSS层叠的规则（权重）" class="headerlink" title="23. CSS层叠的规则（权重）"></a>23. CSS层叠的规则（权重）</h1><ul>
<li><p><code>!important</code>规则最重要，大于其他规则</p>
</li>
<li><p>行内样式：<code>1000</code></p>
</li>
<li><p>ID：<code>100</code></p>
</li>
<li><p>类、属性选择器、伪类选择器：<code>10</code></p>
</li>
<li><p>标签选择器：<code>1</code></p>
</li>
<li><p>权值一样，则按照样式规则的先后顺序来应用，顺序靠后的覆盖靠前的规则</p>
<blockquote>
<p>标签权重为1，class的权重为10，id的权重为100</p>
</blockquote>
  <figure class="highlight"><table><tr><td class="code"><pre><span class="line">div &#123;&#125; //权重为1</span><br><span class="line">.class &#123;&#125;  //权重为10</span><br><span class="line">#id1 &#123;&#125;  //权重为100</span><br><span class="line">#id1 div &#123;&#125;  //权重为101</span><br><span class="line">.class1 div &#123;&#125;  //权重为11</span><br><span class="line">.class1 .class2 div &#123;&#125;  //权重为21</span><br></pre></td></tr></table></figure>

</li>
</ul>
<h1 id="24-可以改变页面布局的属性"><a href="#24-可以改变页面布局的属性" class="headerlink" title="24. 可以改变页面布局的属性"></a>24. 可以改变页面布局的属性</h1><p><code>position</code>、<code>display</code>、<code>float</code>、<code>width</code>、<code>height</code>、<code>margin</code>、<code>padding</code>、<code>top</code>、<code>left</code>、<code>right</code></p>
<h1 id="25-CSS在性能优化方面的实践"><a href="#25-CSS在性能优化方面的实践" class="headerlink" title="25. CSS在性能优化方面的实践"></a>25. CSS在性能优化方面的实践</h1><ul>
<li><code>CSS</code>压缩与合并、<code>Gzip</code>压缩</li>
<li><code>CSS</code>文件放在<code>head</code>里，不要使用<code>@import</code></li>
<li>尽量用缩写、避免使用滤镜、合理使用选择器</li>
</ul>
<h1 id="26-CSS3动画"><a href="#26-CSS3动画" class="headerlink" title="26. CSS3动画"></a>26. CSS3动画</h1><ul>
<li>依靠<code>CSS3</code>中提出的三个属性：<code>transition</code>、<code>transform</code>、<code>animation</code></li>
<li><code>transition</code>：定义了元素在变化过程中是怎样的。包含<code>transition-property</code>、<code>transition-duration</code>、<code>transition-timing-function</code>、<code>transition-delay</code></li>
<li><code>transform</code>：定义元素的变化结果，包含<code>rotate</code>、<code>scale</code>、<code>skew</code>、<code>translate</code></li>
<li><code>animation</code>：动画定义了动作的每一帧（<code>@keyframe</code>）有什么效果，包括<code>animation-name</code>、<code>animation-duration</code>、<code>animation-timing-function</code>、<code>animation-delay</code>、<code>animation-iteration-count</code>、<code>animation-direction</code>、<code>animation-play-state</code>、<code>animation-fill-mode</code></li>
</ul>
<h1 id="27-bsase64的原理及优缺点"><a href="#27-bsase64的原理及优缺点" class="headerlink" title="27. bsase64的原理及优缺点"></a>27. bsase64的原理及优缺点</h1><ul>
<li>优点：可加密，减少了<code>HTTP</code>请求</li>
<li>缺点：需要消耗<code>CPU</code>来进行编解码</li>
</ul>
<h1 id="28-几种常见的CSS布局"><a href="#28-几种常见的CSS布局" class="headerlink" title="28. 几种常见的CSS布局"></a>28. 几种常见的CSS布局</h1><ul>
<li><p>流体布局</p>
  <figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.left</span> &#123;</span><br><span class="line">  <span class="attribute">float</span>: left;</span><br><span class="line">  <span class="attribute">width</span>: <span class="number">100px</span>;</span><br><span class="line">  <span class="attribute">height</span>: <span class="number">200px</span>;</span><br><span class="line">  <span class="attribute">background</span>: <span class="number">#f7903d</span>;    </span><br><span class="line">&#125;</span><br><span class="line"><span class="selector-class">.right</span> &#123;</span><br><span class="line">  <span class="attribute">float</span>: right;</span><br><span class="line">  <span class="attribute">width</span>: <span class="number">200px</span>;</span><br><span class="line">  <span class="attribute">height</span>: <span class="number">200px</span>;</span><br><span class="line">  <span class="attribute">background</span>: <span class="number">#4d85bd</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="selector-class">.main</span> &#123;</span><br><span class="line">  <span class="attribute">margin-left</span>: <span class="number">120px</span>;</span><br><span class="line">  <span class="attribute">margin-right</span>: <span class="number">220px</span>;</span><br><span class="line">  <span class="attribute">height</span>: <span class="number">200px</span>;</span><br><span class="line">  <span class="attribute">background</span>: <span class="number">#59a95a</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

  <figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"left"</span>&gt;</span></span><br><span class="line">  123</span><br><span class="line"><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"right"</span>&gt;</span></span><br><span class="line">  789</span><br><span class="line"><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"main"</span>&gt;</span></span><br><span class="line">  456</span><br><span class="line"><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>  <img src="C:%5CUsers%5CAdministrator%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210428113526920.png" alt="image-20210428113526920"></p>
</li>
<li><p>圣杯布局</p>
<ul>
<li><p>三列布局，中间主体内容前置，且宽度自适应，两边内容定宽</p>
</li>
<li><p>好处：重要的内容放在文档流前面可以优先渲染</p>
</li>
<li><p>原理：利用相对定位、浮动、负边距布局，而不添加额外标签</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.container</span> &#123;</span><br><span class="line">  <span class="attribute">padding-left</span>: <span class="number">150px</span>;</span><br><span class="line">  <span class="attribute">padding-right</span>: <span class="number">190px</span>;</span><br><span class="line">  <span class="attribute">height</span>: <span class="number">100px</span>;</span><br><span class="line">  <span class="attribute">background</span>: <span class="number">#666699</span>;</span><br><span class="line">  <span class="attribute">text-align</span>: center;</span><br><span class="line">&#125;</span><br><span class="line"><span class="selector-class">.main</span> &#123;</span><br><span class="line">  <span class="attribute">float</span>: left;</span><br><span class="line">  <span class="attribute">width</span>: <span class="number">100%</span>;</span><br><span class="line">  <span class="attribute">background</span>: <span class="number">#59a95a</span>;</span><br><span class="line">  <span class="attribute">text-align</span>: center;</span><br><span class="line">&#125;</span><br><span class="line"><span class="selector-class">.left</span> &#123;</span><br><span class="line">  <span class="attribute">float</span>: left;</span><br><span class="line">  <span class="attribute">width</span>: <span class="number">190px</span>;</span><br><span class="line">  <span class="attribute">margin-left</span>: -<span class="number">100%</span>;</span><br><span class="line">  <span class="attribute">position</span>: relative;</span><br><span class="line">  <span class="attribute">left</span>: -<span class="number">150px</span>;</span><br><span class="line">  <span class="attribute">background</span>: <span class="number">#4d85bd</span>;</span><br><span class="line">  <span class="attribute">text-align</span>: center;</span><br><span class="line">&#125;</span><br><span class="line"><span class="selector-class">.right</span> &#123;</span><br><span class="line">  <span class="attribute">float</span>: left;</span><br><span class="line">  <span class="attribute">width</span>: <span class="number">190px</span>;</span><br><span class="line">  <span class="attribute">margin-left</span>: -<span class="number">190px</span>;</span><br><span class="line">  <span class="attribute">position</span>: relative;</span><br><span class="line">  <span class="attribute">right</span>: -<span class="number">190px</span>;</span><br><span class="line">  <span class="attribute">background</span>: <span class="number">#f7903d</span>;</span><br><span class="line">  <span class="attribute">text-align</span>: center;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"container"</span>&gt;</span>  container  <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"main"</span>&gt;</span>main<span class="tag">&lt;/<span class="name">div</span>&gt;</span>  <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"left"</span>&gt;</span>left<span class="tag">&lt;/<span class="name">div</span>&gt;</span>  <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"right"</span>&gt;</span>right<span class="tag">&lt;/<span class="name">div</span>&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>无<code>margin-left</code>、<code>left</code>和<code>right</code>时：</p>
<p><img src="C:%5CUsers%5CAdministrator%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210428170227118.png" alt="image-20210428170227118"></p>
<p>添加<code>margin-left: -100%</code>和<code>margin-left: -190px</code>后：</p>
<p><img src="C:%5CUsers%5CAdministrator%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210428170246499.png" alt="image-20210428170246499"></p>
<p>添加<code>left: -150px</code>和<code>right: -190px</code>后：</p>
<p><img src="C:%5CUsers%5CAdministrator%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210428170339639.png" alt="image-20210428170339639"></p>
</li>
</ul>
</li>
</ul>
<ul>
<li><p>双飞翼布局</p>
<ul>
<li><p>对圣杯布局（使用相对定位，对以后布局有局限性）的改进，消除相对定位布局</p>
</li>
<li><p>原理：主体元素上设置左右边距，预留两翼位置。左右两栏使用浮动和负边距归位，消除相对定位</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.main-wrap</span> &#123;  <span class="attribute">float</span>: left;  <span class="attribute">width</span>: <span class="number">100%</span>;  <span class="attribute">text-align</span>: center;&#125;<span class="selector-class">.main</span> &#123;  <span class="attribute">margin-left</span>: <span class="number">150px</span>;  <span class="attribute">margin-right</span>: <span class="number">190px</span>;  <span class="attribute">background</span>: <span class="number">#59a95a</span>;  <span class="attribute">text-align</span>: center;&#125;<span class="selector-class">.left</span> &#123;  <span class="attribute">float</span>: left;  <span class="attribute">width</span>: <span class="number">150px</span>;  <span class="attribute">margin-left</span>: -<span class="number">100%</span>;  <span class="attribute">background</span>: <span class="number">#4d85bd</span>;  <span class="attribute">text-align</span>: center;&#125;<span class="selector-class">.right</span> &#123;  <span class="attribute">float</span>: left;  <span class="attribute">width</span>: <span class="number">190px</span>;  <span class="attribute">margin-left</span>: -<span class="number">190px</span>;  <span class="attribute">background</span>: <span class="number">#f7903d</span>;  <span class="attribute">text-align</span>: center;&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"main-wrap"</span>&gt;</span>  <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"main"</span>&gt;</span>main<span class="tag">&lt;/<span class="name">div</span>&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span><span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"left"</span>&gt;</span>left<span class="tag">&lt;/<span class="name">div</span>&gt;</span><span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"right"</span>&gt;</span>right<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p><img src="C:%5CUsers%5CAdministrator%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210429085624743.png" alt="image-20210429085624743"></p>
</li>
</ul>
</li>
</ul>
<h1 id="29-stylus-sass-less区别-待考证"><a href="#29-stylus-sass-less区别-待考证" class="headerlink" title="29. stylus/sass/less区别    待考证"></a>29. stylus/sass/less区别    待考证</h1><ul>
<li>均具有“变量”、“混合”、“嵌套”、“继承”、“颜色混合”五大基本特性</li>
<li>Scss和Less语法较为严谨，Less要求一定要使用大括号<code>{}</code>，Scss和Stylus可以通过缩进表示层次与嵌套关系</li>
<li>Scss无全局变量的概念，Less和Stylus有类似于其他语言的作用域概念</li>
<li>Sass是基于Ruby语言的，而Less和Stylus可以基于NodeJS和NPM下载相应库后进行编译</li>
</ul>
<h1 id="30-postcss的作用"><a href="#30-postcss的作用" class="headerlink" title="30. postcss的作用"></a>30. postcss的作用</h1><ul>
<li>一个平台</li>
<li><code>PostCSS</code>提供了一个解析器，能够将<code>CSS</code>解析成抽象语法树</li>
<li>通过在<code>PostCSS</code>这个平台上，能够开发一些插件来处理<code>CSS</code>，例如<code>autoprefixer</code></li>
<li><code>PostCSS</code>可以对<code>Sass</code>处理过后的<code>CSS</code>再处理</li>
</ul>
<h1 id="31-自定义字体的使用场景"><a href="#31-自定义字体的使用场景" class="headerlink" title="31. 自定义字体的使用场景"></a>31. 自定义字体的使用场景</h1><ul>
<li>宣传/品牌/<code>banner</code>等固定文案</li>
<li>字体图标</li>
</ul>
<h1 id="32-如何美化CheckBox"><a href="#32-如何美化CheckBox" class="headerlink" title="32. 如何美化CheckBox   ????"></a>32. 如何美化CheckBox   ????</h1><ul>
<li><code>&lt;label&gt;</code>属性<code>for</code>和<code>id</code></li>
<li>隐藏原生的<code>&lt;input&gt;</code></li>
<li><code>:checked + &lt;label&gt;</code></li>
</ul>
<h1 id="33-伪类和伪元素的区别"><a href="#33-伪类和伪元素的区别" class="headerlink" title="33. 伪类和伪元素的区别"></a>33. 伪类和伪元素的区别</h1><ul>
<li>伪类表状态</li>
<li>伪元素是真的有元素</li>
<li>伪类单冒号，伪元素双冒号</li>
</ul>
<h1 id="34-base64的使用"><a href="#34-base64的使用" class="headerlink" title="34. base64的使用"></a>34. base64的使用</h1><ul>
<li>用于减少<code>HTTP</code>请求</li>
<li>适用于小图片</li>
<li><code>base64</code>的体积约为原图的<code>4/3</code>文件体积变大</li>
</ul>
<h1 id="35-自适应布局-完善…"><a href="#35-自适应布局-完善…" class="headerlink" title="35. 自适应布局  完善…"></a>35. 自适应布局  完善…</h1><h1 id="36-幻灯片效果页面-完善…"><a href="#36-幻灯片效果页面-完善…" class="headerlink" title="36. 幻灯片效果页面  完善…"></a>36. 幻灯片效果页面  完善…</h1><h1 id="37-什么是外边距重叠？重叠的结果是什么？"><a href="#37-什么是外边距重叠？重叠的结果是什么？" class="headerlink" title="37. 什么是外边距重叠？重叠的结果是什么？"></a>37. 什么是外边距重叠？重叠的结果是什么？</h1><blockquote>
<p>外边距重叠即<code>margin-collapse</code></p>
</blockquote>
<ul>
<li>在CSS中，相邻的两个盒子（可能是兄弟关系也可能是祖先关系）的外边距可以结合成一个单独的外边距。这种合并外边距的方式被称为折叠，折叠后形成的外边距称为折叠外边距</li>
<li>两个或多个毗邻的普通流中的块元素垂直方向上的<code>margin</code>会折叠</li>
<li>浮动元素或<code>inline-block</code>元素或绝对定位元素的<code>margin</code>不会和垂直方向上的其他元素的<code>margin</code>折叠</li>
<li>创建了块级格式化上下文的元素，不会和它的子元素发生<code>margin</code>折叠</li>
<li>元素自身的<code>margin-bottom</code>和<code>margin-top</code>相邻时也会折叠</li>
<li>折叠结果遵循下列计算规则：<ul>
<li>两个相邻的外边距都是正数时，折叠结果是它们之间较大的值</li>
<li>两个相邻的外边距都是负数是，折叠结果是两者绝对值的较大值</li>
<li>两个外边距一正一负时，折叠结果是两者相加的和</li>
</ul>
</li>
</ul>
<h1 id="38-rgba-和opacity的透明效果有什么不同？"><a href="#38-rgba-和opacity的透明效果有什么不同？" class="headerlink" title="38. rgba()和opacity的透明效果有什么不同？"></a>38. rgba()和opacity的透明效果有什么不同？</h1><ul>
<li><code>rgba()</code>和<code>opacity</code>都能实现透明效果，但<code>opacity</code>作用于元素，以及元素内的所有内容的透明度</li>
<li><code>rgba()</code>只作用于元素的颜色及其背景色，<strong>设置<code>rgba()</code>透明的元素的子元素不会继承透明效果</strong></li>
<li>当opacity属性的值应用于某个元素上时，把这个元素和它内容当作一个整体来看待，即使这个值没有被子元素继承。因此一个元素和它包含的元素都会有与元素背景相同的透明度，哪怕父子元素由不同的opacity的值</li>
</ul>
<h1 id="39-CSS中可以让文字在垂直和水平方向上重叠的两个属性是什么"><a href="#39-CSS中可以让文字在垂直和水平方向上重叠的两个属性是什么" class="headerlink" title="39. CSS中可以让文字在垂直和水平方向上重叠的两个属性是什么"></a>39. CSS中可以让文字在垂直和水平方向上重叠的两个属性是什么</h1><ul>
<li>垂直方向：<code>line-height</code></li>
<li>水平方向：<code>letter-spacing</code></li>
</ul>
<h1 id="40-如何垂直水平居中一个元素"><a href="#40-如何垂直水平居中一个元素" class="headerlink" title="40. 如何垂直水平居中一个元素"></a>40. 如何垂直水平居中一个元素</h1><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"wrap"</span>&gt;</span>  <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"content"</span>&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br></pre></td></tr></table></figure>

<figure class="highlight"><table><tr><td class="code"><pre><span class="line">/*已知元素的宽高*/.wrap &#123;         width: 800px;        height: 450px;        position: relative;     //父元素相对定位  background: #666699;&#125;    .content &#123;        width: 200px;       height: 200px;         position: absolute;      //子元素绝对定位  top: 50%;        left: 50%;       margin-top: -100px;     //一半高  margin-left: -100px;    //一半宽  background: #f7903d;&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight"><table><tr><td class="code"><pre><span class="line">/*不使用元素宽高*/.wrap &#123;         width: 800px;        height: 450px;        position: relative;     //父元素相对定位  background: #666699;&#125;.content &#123;  width: 200px;  height: 200px;  position: absolute;   margin: auto;  left: 0;  top: 0;  right: 0;  bottom: 0;  background: #f7903d;&#125;</span><br></pre></td></tr></table></figure>

<p><img src="C:%5CUsers%5CAdministrator%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210429093751682.png" alt="image-20210429093751682"></p>
<h1 id="41-如何垂直居中一个img"><a href="#41-如何垂直居中一个img" class="headerlink" title="41. 如何垂直居中一个img"></a>41. 如何垂直居中一个img</h1><figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.wrap</span> &#123;  <span class="attribute">width</span>: <span class="number">600px</span>;  <span class="attribute">height</span>: <span class="number">600px</span>;  <span class="attribute">display</span>: table-cell;  <span class="attribute">text-align</span>: center;  <span class="attribute">vertical-align</span>: middle;  <span class="attribute">background</span>: <span class="number">#666699</span>;&#125;<span class="selector-tag">img</span>&#123;  <span class="attribute">width</span>: <span class="number">500px</span>;&#125;</span><br></pre></td></tr></table></figure>

<p><img src="C:%5CUsers%5CAdministrator%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210429095202486.png" alt="image-20210429095202486"></p>
<h1 id="42-Rem-em和px的区别"><a href="#42-Rem-em和px的区别" class="headerlink" title="42. Rem, em和px的区别"></a>42. Rem, em和px的区别</h1><ul>
<li><p>px是绝对长度单位，px的值是固定的，指定是多少就多少，计算比较容易</p>
</li>
<li><p>em是相对长度单位，继承父级元素的字体大小，所有字体都是相对于父元素大小的（父元素为16px，则1em = 16px），html页面第一个父元素是body</p>
</li>
<li><p>rem（root em）是相对长度单位，但它是相对于根元素（HTML），不会像em造成混乱，通过它既可以做到只修改根元素就成比例地调整所有字体大小，又可以避免字体逐层复合的连锁反应。除了IE8及更早的版本，所有浏览器均已支持rem，不吃的浏览器需要多写一个绝对单位的声明：<code>p {font-size: 14px; font-size: .875rem}</code></p>
</li>
<li><p>任意浏览器的默认字体高都是<code>16px</code>。所有未经调整的浏览器都符合:<code>1em = 16px</code>。那么<code>12px = 0.75em</code>,<code>10px = 0.625em</code>。为了简化font-size的换算，需要在css中的body选择器中声明<strong>Font-size=62.5%</strong>，这就使em值变为 <code>16px * 62.5% = 10px</code>,这样<code>12px = 1.2em</code>, <code>10px = 1em</code>, 也就是说只需要将你的原来的px数值除以10，然后换上em作为单位就行了。</p>
<p>  在写CSS的时候，需要注意：</p>
<ul>
<li><p>body选择器中声明<strong>Font-size=62.5%</strong>；</p>
</li>
<li><p>将你的原来的px数值除以10，然后换上em作为单位；</p>
</li>
<li><p>重新计算那些被放大的字体的em数值。避免字体大小的重复声明。也就是避免<code>1.2 * 1.2= 1.44</code>的现象。比如说你在<code>#content</code>中声明了字体大小为1.2em，那么在声明p的字体大小时就只能是1em，而不是1.2em, 因为此em非彼em，它因继承#content的字体高而变为了1em=12px</p>
<blockquote>
<p>对于只需要适配少部分手机设备，并且分辨率对页面影响不大时用px即可；</p>
<p>对于需要适配各种移动设备，使用rem，例如只需要适配iPhone和iPad等分辨率差别比较大的设备</p>
</blockquote>
</li>
</ul>
</li>
</ul>
<h1 id="43-Sass和Less是什么"><a href="#43-Sass和Less是什么" class="headerlink" title="43. Sass和Less是什么"></a>43. Sass和Less是什么</h1><ul>
<li><code>CSS</code>预处理器，是<code>CSS</code>上的一种抽象层，是一种特殊的语法/语言编译成<code>CSS</code></li>
<li>Less是一种动态样式语言，将CSS赋予了动态语言的特性，如变量、继承、运算、函数。Less既可以在客户端上运行（支持IE6+，Webkit，Firefox），也可以在服务端上运行（借助Node.js）</li>
<li>优势：<ul>
<li>结构清晰，便于扩展</li>
<li>可以方便地屏蔽浏览器私有语法差异，封装对浏览器语法差异的重复处理可以减少无意义的机械劳动？？？</li>
<li>可以轻松实现多重继承</li>
<li>完全兼容CSS代码，可以方便地应用到老项目中。Less只是在CSS语法上做了扩展，所以老的CSS代码也可以于Less代码一起编译</li>
</ul>
</li>
</ul>
<h1 id="44-CSS中的content属性的作用和应用"><a href="#44-CSS中的content属性的作用和应用" class="headerlink" title="44. CSS中的content属性的作用和应用"></a>44. CSS中的content属性的作用和应用</h1><ul>
<li><p>CSS的content属性专门应用在before/after伪元素上，用于插入生成内容</p>
</li>
<li><p>常用来清除浮动</p>
  <figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.clearfix</span> &#123;  <span class="attribute">display</span>: blcok;  <span class="attribute">content</span>: <span class="string">""</span>;  <span class="attribute">height</span>: <span class="number">0</span>;  <span class="attribute">visibility</span>: hidden;  <span class="attribute">clear</span>: both;&#125;</span><br></pre></td></tr></table></figure>

</li>
</ul>
<h1 id="45-水平居中的方法"><a href="#45-水平居中的方法" class="headerlink" title="45. 水平居中的方法"></a>45. 水平居中的方法</h1><ul>
<li>元素为行内元素，设置父元素<code>text-align: center</code></li>
<li>如果元素宽度固定，可以设置<code>margin: 0 auto</code></li>
<li>绝对定位和移动：absolute + transform</li>
<li>使用<code>flex-box</code>布局，指定<code>justify-content: center</code></li>
<li>display设置为teble-cell</li>
</ul>
<h1 id="46-垂直居中的方法"><a href="#46-垂直居中的方法" class="headerlink" title="46. 垂直居中的方法"></a>46. 垂直居中的方法</h1><ul>
<li><code>display: table-cell; vertical-align: middle;</code></li>
<li>flex布局，设置为<code>align-item: center</code></li>
<li>绝对定位中设置<code>bottom: 0; top: 0; margin: auto</code></li>
<li>绝对定位中固定高度时设置<code>top: 50%; margin-top: -height / 2;</code></li>
<li>文本垂直居中设置<code>lineheight: height</code></li>
<li>单行文本，line-height设置为height值</li>
</ul>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.vertical</span> &#123;  <span class="attribute">height</span>: <span class="number">100px</span>;  <span class="attribute">line-height</span>: <span class="number">100px</span>;&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>已知高度的块级子元素，采用绝对定位和负边距</li>
</ul>
<figure class="highlight"><table><tr><td class="code"><pre><span class="line">.container &#123;  position: relative;&#125;.vertical &#123;  height: 300px;        //子元素高度  position: absolute;     top: 50%;             //父元素高度50%  margin-top: -150px;   //自身高度的一半&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>未知高度的块级父子元素居中，模拟表格布局（缺点：IE6/7不兼容，父级overflow: hidden失效）</li>
</ul>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.container</span> &#123;  <span class="attribute">display</span>: table;&#125;<span class="selector-class">.content</span> &#123;  <span class="attribute">display</span>: table-cell;  <span class="attribute">vertical-align</span>: middle;&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>新增<code>inline-block</code>兄弟元素，设置vertical-align（缺点：需要增加额外标签，IE6/7不兼容）</li>
</ul>
<figure class="highlight"><table><tr><td class="code"><pre><span class="line">.container &#123;  height: 100px;  //定义父级元素高度，作为参考&#125;.extra .vertical &#123;  display: inline-block;    //行内块显示  vertical-align: middle;   //垂直居中&#125;.extra &#123;  height: 100%;             //设置新增元素高度为100%&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>绝对定位配合CSS3位移</li>
</ul>
<figure class="highlight"><table><tr><td class="code"><pre><span class="line">.vertical &#123;  position: absolute;  top: 50%;            //父元素高度50%  transform: translateY(-50%);&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>CSS弹性盒模型</li>
</ul>
<figure class="highlight"><table><tr><td class="code"><pre><span class="line">.container &#123;  display: flex;  justify-content: center;  //子元素水平居中  align-items: center;      //子元素垂直居中&#125;</span><br></pre></td></tr></table></figure>

<h1 id="47-如何使用CSS实现硬件加速"><a href="#47-如何使用CSS实现硬件加速" class="headerlink" title="47. 如何使用CSS实现硬件加速"></a>47. 如何使用CSS实现硬件加速</h1><ul>
<li>硬件加速是指通过创建独立的复合图层，让CPU来渲染这个图层，从而提高性能</li>
<li>一半触发硬件加速的CSS属性有<code>transform</code>、<code>opacity</code>、<code>filter</code>，为了避免2D动画在开始和结束的适合的<code>repaint</code>操作，一般使用<code>transform: translateZ(0)</code></li>
</ul>
<h1 id="48-重绘和回流（重排）是什么，如何避免？"><a href="#48-重绘和回流（重排）是什么，如何避免？" class="headerlink" title="48. 重绘和回流（重排）是什么，如何避免？"></a>48. 重绘和回流（重排）是什么，如何避免？</h1><ul>
<li>重绘：当渲染树中的元素外观（如颜色）发生改变，<strong>不影响布局</strong>时，产生重绘</li>
<li>回流：当渲染树中的元素的布局（如尺寸、位置、隐藏/显示状态）发生改变时，产生重绘回流</li>
<li>JS获取Layout属性值（如<code>offsetLeft</code>、<code>scrollTop</code>、<code>getComputedStyle</code>等）也会引起回流，因为浏览器需要通过回流计算最新值</li>
<li><strong>回流必将引起重绘，重绘不一定会引起回流</strong></li>
<li>最小化重绘（repaint）和回流（reflow）：<ul>
<li>需要对元素进行复杂的操作时，可以先隐藏（<code>display: none</code>），操作完成后再显示</li>
<li>需要创建多个<code>DOM</code>节点时，使用<code>DocumentFragment</code>创建完后一次性的加入<code>document</code></li>
<li>缓存<code>Layout</code>属性值，如：<code>var left = elem.offserLeft</code>，这样多次使用left只产生一次回流</li>
<li>尽量避免使用<code>table</code>布局（<code>table</code>元素一旦触发回流就会导致table里所有的其他元素回流）</li>
<li>避免使用<code>css</code>表达式（<code>expression</code>），因为每次调用都会重新计算值（包括加载页面）</li>
<li>尽量使用<code>css</code>属性简写，如：用<code>border</code>代替<code>border-width</code>，<code>border-style</code>，<code>border-color</code></li>
<li>批量修改元素样式：<code>elem.className</code>和<code>elem.style.cssText</code>代替<code>elem.style.xxx</code></li>
</ul>
</li>
</ul>
<h1 id="49-CSS3的animation"><a href="#49-CSS3的animation" class="headerlink" title="49. CSS3的animation"></a>49. CSS3的animation</h1><ul>
<li><p>CSS3的animation是css3新增的动画属性，这个css3动画的每一帧是通过@keyframes来声明的，keyframes声明了动画的名称，通过from、to或者是百分比来定义</p>
</li>
<li><p>每一帧动画元素的状态，通过animation-name来引用这个动画，同时css3动画也可以定义动画运行的时长、动画开始时间、动画播放方向、动画循环次数、动画播放的方式</p>
</li>
<li><p>动画子属性有：</p>
<ul>
<li><code>animation-name</code>：动画名</li>
<li><code>animation-duration</code>：动画播放时长</li>
<li><code>animation-delay</code>：动画延迟播放的时间</li>
<li><code>animation-direction</code>：动画的播放方向</li>
<li><code>animation-iteration-count</code>：动画播放次数</li>
<li><code>animation-fill-mode</code>：动画播放之后的状态</li>
<li><code>animation-play-state</code>：动画播放状态，如暂停运行等</li>
<li><code>animation-timing-function</code>：动画播放的方式，如恒速播放、艰涩播放</li>
</ul>
</li>
</ul>
<h1 id="50-左边边宽度固定，右边自适应"><a href="#50-左边边宽度固定，右边自适应" class="headerlink" title="50. 左边边宽度固定，右边自适应"></a>50. 左边边宽度固定，右边自适应</h1><blockquote>
<p>左侧固定宽度，右侧自适应宽度的两列布局实现</p>
</blockquote>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"outer"</span>&gt;</span>  <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"left"</span>&gt;</span>固定宽度<span class="tag">&lt;/<span class="name">div</span>&gt;</span>  <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"right"</span>&gt;</span>自适应宽度<span class="tag">&lt;/<span class="name">div</span>&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br></pre></td></tr></table></figure>

<ul>
<li>方法1：左侧div设置成浮动<code>float: left</code>，右侧div宽度会自适应拉升</li>
</ul>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.outer</span> &#123;  <span class="attribute">width</span>: <span class="number">100%</span>;  <span class="attribute">height</span>: <span class="number">500px</span>;  <span class="attribute">background</span>: <span class="number">#666699</span>;&#125;<span class="selector-class">.left</span> &#123;  <span class="attribute">float</span>: left;  <span class="attribute">width</span>: <span class="number">200px</span>;  <span class="attribute">height</span>: <span class="number">200px</span>;  <span class="attribute">background</span>: <span class="number">#4d85bd</span>;&#125;<span class="selector-class">.right</span> &#123;  <span class="attribute">height</span>: <span class="number">200px</span>;  <span class="attribute">background</span>: <span class="number">#59a95a</span>;&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>方法2：对右侧<code>div</code>进行绝对定位，然后再设置<code>right = 0</code>，既可以实现宽度自适应</li>
</ul>
<blockquote>
<p>绝对定位元素具有自动伸缩的功能，将<code>width</code>设置为<code>auto</code>时，绝对定位元素会根据其<code>left</code>和<code>right</code>自动伸缩其大小</p>
</blockquote>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.outer</span> &#123;  <span class="attribute">width</span>: <span class="number">100%</span>;  <span class="attribute">height</span>: <span class="number">500px</span>;  <span class="attribute">background</span>: <span class="number">#666699</span>;  <span class="attribute">position</span>: relative;&#125;<span class="selector-class">.left</span> &#123;  <span class="attribute">width</span>: <span class="number">200px</span>;  <span class="attribute">height</span>: <span class="number">200px</span>;  <span class="attribute">background</span>: <span class="number">#4d85bd</span>;&#125;<span class="selector-class">.right</span> &#123;  <span class="attribute">height</span>: <span class="number">200px</span>;  <span class="attribute">background</span>: <span class="number">#59a95a</span>;  <span class="attribute">position</span>: absolute;  <span class="attribute">left</span>: <span class="number">200px</span>;  <span class="attribute">top</span>: <span class="number">0</span>;  <span class="attribute">right</span>: <span class="number">0</span>;&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>方法3：将左侧<code>div</code>进行绝对定位，右侧<code>div</code>设置<code>margin-left: 200px</code></li>
</ul>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.outer</span> &#123;  <span class="attribute">width</span>: <span class="number">100%</span>;  <span class="attribute">height</span>: <span class="number">500px</span>;  <span class="attribute">background</span>: <span class="number">#666699</span>;  <span class="attribute">position</span>: relative;&#125;<span class="selector-class">.left</span> &#123;  <span class="attribute">width</span>: <span class="number">200px</span>;  <span class="attribute">height</span>: <span class="number">200px</span>;  <span class="attribute">background</span>: <span class="number">#4d85bd</span>;  <span class="attribute">position</span>: absolute;&#125;<span class="selector-class">.right</span> &#123;  <span class="attribute">height</span>: <span class="number">200px</span>;  <span class="attribute">background</span>: <span class="number">#59a95a</span>;  <span class="attribute">margin-left</span>: <span class="number">200px</span>;&#125;</span><br></pre></td></tr></table></figure>

<p>方法4：使用flex布局</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.outer</span> &#123;  <span class="attribute">width</span>: <span class="number">100%</span>;  <span class="attribute">height</span>: <span class="number">500px</span>;  <span class="attribute">background</span>: <span class="number">#666699</span>;  <span class="attribute">display</span>: flex;  <span class="attribute">flex-direction</span>: row;&#125;<span class="selector-class">.left</span> &#123;  <span class="attribute">width</span>: <span class="number">200px</span>;  <span class="attribute">height</span>: <span class="number">200px</span>;  <span class="attribute">background</span>: <span class="number">#4d85bd</span>;&#125;<span class="selector-class">.right</span> &#123;  <span class="attribute">height</span>: <span class="number">200px</span>;  <span class="attribute">background</span>: <span class="number">#59a95a</span>;  <span class="attribute">flex</span>: <span class="number">1</span>;&#125;</span><br></pre></td></tr></table></figure>

<h1 id="51-两种以上方式实现已知或者未知宽度的垂直水平居中"><a href="#51-两种以上方式实现已知或者未知宽度的垂直水平居中" class="headerlink" title="51. 两种以上方式实现已知或者未知宽度的垂直水平居中"></a>51. 两种以上方式实现已知或者未知宽度的垂直水平居中</h1><h1 id="52-如何实现小于12px的字体效果"><a href="#52-如何实现小于12px的字体效果" class="headerlink" title="52. 如何实现小于12px的字体效果"></a>52. 如何实现小于12px的字体效果</h1><blockquote>
<p>transform: scale()这个属性只可以缩放可以定位宽高的元素，而行内元素时没有宽高的，我们可以加上一个<code>inline-block</code></p>
</blockquote>
<h1 id="53-CSS-hack原理及常用hack"><a href="#53-CSS-hack原理及常用hack" class="headerlink" title="53. CSS hack原理及常用hack"></a>53. CSS hack原理及常用hack</h1><ul>
<li>原理：利用不同浏览器对CSS的支持和解析结果不一样编写针对特定浏览器样式</li>
<li>常见的hack：<ul>
<li>属性hack</li>
<li>选择器hack</li>
<li>IE条件注释</li>
</ul>
</li>
</ul>
<h1 id="54-CSS有哪些继承属性-待完善"><a href="#54-CSS有哪些继承属性-待完善" class="headerlink" title="54. CSS有哪些继承属性  待完善"></a>54. CSS有哪些继承属性  待完善</h1><ul>
<li>字体相关：<ul>
<li><code>font-family</code>字体系列</li>
<li><code>font-weight</code>字体粗细</li>
<li><code>font-size</code>字体大小</li>
<li><code>font-style</code>字体风格</li>
</ul>
</li>
<li>文本相关：<ul>
<li><code>text-indent</code>文本缩进</li>
<li><code>text-align</code>文本对齐方式</li>
<li><code>line-height</code>行高</li>
<li><code>color</code>文本颜色</li>
</ul>
</li>
<li>元素可见性：<code>visibility</code>控制元素的显示和隐藏</li>
<li>列表布局属性：<code>list-style</code>列表风格，包括<code>list-style-type</code>和<code>list-style-image</code>等</li>
<li>光标属性：<code>cursor</code>光标显示的各种形态</li>
</ul>
<h1 id="55-CSS选择符有哪些？哪些属性可以继承-待完善"><a href="#55-CSS选择符有哪些？哪些属性可以继承-待完善" class="headerlink" title="55.CSS选择符有哪些？哪些属性可以继承  待完善"></a>55.CSS选择符有哪些？哪些属性可以继承  待完善</h1><h1 id="56-CSS3新增伪类有哪些-待完善"><a href="#56-CSS3新增伪类有哪些-待完善" class="headerlink" title="56. CSS3新增伪类有哪些  待完善"></a>56. CSS3新增伪类有哪些  待完善</h1><h1 id="57-行内元素有哪些？块级元素有哪些？空元素有哪些？"><a href="#57-行内元素有哪些？块级元素有哪些？空元素有哪些？" class="headerlink" title="57. 行内元素有哪些？块级元素有哪些？空元素有哪些？"></a>57. 行内元素有哪些？块级元素有哪些？空元素有哪些？</h1><ul>
<li>行内元素：a、b、span、img、input、select、strong</li>
<li>块级元素：div、ul、ol、li、dl、dt、dd、h1、h2、p</li>
<li>空元素：<ul>
<li>常见：br、hr、img、input、link、meta</li>
<li>不常见：area、base、col、command、embed、param、source、track、wbr</li>
</ul>
</li>
</ul>
<h1 id="58-行内元素与块级元素的区别和互换"><a href="#58-行内元素与块级元素的区别和互换" class="headerlink" title="58. 行内元素与块级元素的区别和互换"></a>58. 行内元素与块级元素的区别和互换</h1><ul>
<li>区别：<ul>
<li>行内元素会在一行水平方向排列，块级元素独占一行，自动填满父级元素</li>
<li>块级元素可以包含行内元素和块级元素，行内元素只能包含文本和其他行内元素</li>
<li>盒模型属性上，行内元素width、height无效，padding和margin垂直方向上均无效</li>
</ul>
</li>
<li>互换：display: inline/blcok</li>
<li>inline-block元素：既可以设置高宽，又有inline元素不换行的特性</li>
</ul>
<h1 id="59-inline-blcok、inline和block的区别"><a href="#59-inline-blcok、inline和block的区别" class="headerlink" title="59. inline-blcok、inline和block的区别"></a>59. inline-blcok、inline和block的区别</h1><ul>
<li>block是块级元素，能设置宽高，margin/padding都有效，前后都有换行符</li>
<li>inline设置宽高无效，margin在竖直方向无效，padding有效，前后无换行符</li>
<li>inline-block可以设置宽高，margin/padding有效，前后无换行符</li>
</ul>
<h1 id="60-为什么img是inline还可以设置宽高？"><a href="#60-为什么img是inline还可以设置宽高？" class="headerlink" title="60. 为什么img是inline还可以设置宽高？"></a>60. 为什么img是inline还可以设置宽高？</h1><blockquote>
<p>img是<strong>可替换元素</strong>，这类元素的展现效果不是由CSS来控制的，它们是一种外部对象，外观的渲染独立于CSS。内容不受当前文档的样式影响，CSS可以影响可替换元素的位置，但是不会影响到可替换元素的内容。（如iframe可能有自己的样式表，不会继承父文档的样式），可替换元素有内置宽高，性质同设置了inline-block一样</p>
</blockquote>
<h1 id="61-CSS中inline元素可以设置padding和margin吗"><a href="#61-CSS中inline元素可以设置padding和margin吗" class="headerlink" title="61. CSS中inline元素可以设置padding和margin吗"></a>61. CSS中inline元素可以设置padding和margin吗</h1><ul>
<li>width和height可以设置，但是没有用</li>
<li>padding和margin在垂直（top/bottom）方向没有，水平方向（left/right）上有用</li>
</ul>
<h1 id="62-伪类和伪元素"><a href="#62-伪类和伪元素" class="headerlink" title="62. 伪类和伪元素"></a>62. 伪类和伪元素</h1><ul>
<li>伪类：<ul>
<li>伪类可以理解为一种状态，而伪元素则代表一些实实在在存在的元素，但它们都是抽象刻画的，游离于标准文档之外</li>
<li>伪类存在的意义是为了通过选择器，格式化DOM树以外的信息（:visted/:link），以及不能被常规CSS选择器获取到的信息（比如说获取第一个子元素，常规CSS选择器不行，可以用<code>first-child</code></li>
<li>伪类常用的有first-child、last-child、nth-child、first-of-type（父元素第一个特定的子元素）、last-of-type、nth-of-type、lang、focus、lvha（a标签）</li>
</ul>
</li>
<li>伪元素：<ul>
<li>伪元素可以创建一些文档语言无法创建的虚拟元素，比如文档语言没有一种机制可以描述元素内容第一个字母或者第一行，但是伪元素可以<code>::first-letter</code>，<code>::first-line</code>。同时伪元素还可以创建文档中不存在的内容，比如说:after，::before</li>
<li>伪元素主要有：<code>::after</code>，<code>::before</code>，<code>::first-letter</code>，<code>::first-line</code>，<code>::selection</code></li>
</ul>
</li>
</ul>
<h1 id="63-元素隐藏方法和区别"><a href="#63-元素隐藏方法和区别" class="headerlink" title="63. 元素隐藏方法和区别"></a>63. 元素隐藏方法和区别</h1><ul>
<li>display: none元素不可见，不占据空间，资源会加载，DOM可以访问</li>
<li>visibility: hidden元素不可见，不能点击，但占据空间，资源会加载，可以使用</li>
<li>opacity: 0元素不可见、可以点击、占据空间，可以使用（不占据的话需要再加一个<code>position: absolute</code>/不能点击且不占据空间可以添加<code>position: absolute; z-inde: -1</code>/不能点击、占据空间需添加<code>position: relative; z-index: -1</code>）</li>
</ul>
<h1 id="64-img和background-image的区别"><a href="#64-img和background-image的区别" class="headerlink" title="64. img和background-image的区别"></a>64. img和background-image的区别</h1><ul>
<li>解析机制：img属于html标签，background-img属于CSS，img先解析</li>
<li>SEO: img标签有一个alt属性可以指定图像的替代文本，有利于SEO，并且在图片加载失败时有利于阅读</li>
</ul>
<h1 id="65-outline和border的区别"><a href="#65-outline和border的区别" class="headerlink" title="65. outline和border的区别"></a>65. outline和border的区别</h1><ul>
<li>outline轮换是绘制于元素周围的一条线，位于边框外缘的外围，可以起到突出元素的作用</li>
<li>outline的效果将随元素的focus而自动出现，相应的由blur自动消失，这些都是浏览器的默认行为，不需要JS配合CSS来控制</li>
<li>outline不占据空间，不会像border那样影响元素的尺寸或者位置</li>
</ul>
<h1 id="66-CSS动画如何实现"><a href="#66-CSS动画如何实现" class="headerlink" title="66. CSS动画如何实现"></a>66. CSS动画如何实现</h1><ul>
<li>创建动画序列，需要<code>animation</code>属性或其子属性，属性允许配置动画时间、时长和动画细节</li>
<li>动画的实际表现由<code>@keyframes</code>规则实现</li>
<li>transition也可以实现动画，但强调过度，是元素的一个或多个属性变化时产生的过渡效果，同一个元素通过两个不同的途径或许样式，而第二个途径当<strong>某种改变发生</strong>时（如hover）才能获取样式，这样就会产生过渡动画</li>
</ul>
<h1 id="67-transition和animation的区别"><a href="#67-transition和animation的区别" class="headerlink" title="67. transition和animation的区别"></a>67. transition和animation的区别</h1><ul>
<li><code>animation</code>和<code>transition</code>大部分属性相同，都是随时间改变元素的属性值</li>
<li><code>transition</code>需要触发一个事件才能改变属性，<code>animation</code>不需要触发任何事件即可随时间改变属性</li>
<li><code>transition</code>为2帧<code>from...to</code>，<code>animation</code>可以一帧一帧</li>
</ul>
<h1 id="68-硬件加速"><a href="#68-硬件加速" class="headerlink" title="68. 硬件加速"></a>68. 硬件加速</h1><h2 id="68-1-让一个元素左移100px，使用left和transform有什么区别"><a href="#68-1-让一个元素左移100px，使用left和transform有什么区别" class="headerlink" title="68.1 让一个元素左移100px，使用left和transform有什么区别"></a>68.1 让一个元素左移100px，使用left和transform有什么区别</h2><blockquote>
<p>使用<code>left</code>等属性来设置动画会<strong>一直触发浏览器的重绘</strong>，而使用CSS3中的<code>transform</code>会采用<strong>GPU硬件加速</strong>，不会触发重绘，性能更好</p>
</blockquote>
<h2 id="68-2-硬件加速的原理"><a href="#68-2-硬件加速的原理" class="headerlink" title="68.2 硬件加速的原理"></a>68.2 硬件加速的原理</h2><ul>
<li>DOM树和CSSOM合并后形成Render树</li>
<li>渲染树中包含了大量的渲染元素，每一个渲染元素会被分到一个图层中，每个图层又会被加载到GPU形成渲染纹理</li>
<li>GPU中的<code>transform</code>是不会触发重绘的，这一点非常类似3D绘图功能</li>
<li>这些使用<code>transform</code>的图层都会由独立的合成器进程进行处理</li>
</ul>
<blockquote>
<p>CSS3 transform创建了一个新的复合图层，可以被GPU直接用来执行transform，从而不触发浏览器的重绘来达到加速的目的。</p>
<p>以下情况会创建一个独立的复合图层：</p>
<ul>
<li>3D或者CSS transform</li>
<li><code>&lt;video&gt;</code>和<code>&lt;canvas&gt;</code>标签</li>
<li>CSS filters</li>
<li>元素覆盖时，如使用了<code>z-index</code>属性</li>
</ul>
</blockquote>
<h2 id="68-3-如何硬件加速"><a href="#68-3-如何硬件加速" class="headerlink" title="68.3 如何硬件加速"></a>68.3 如何硬件加速</h2><ul>
<li>transform</li>
<li>opacity</li>
<li>filter</li>
<li>will-change：告知浏览器哪一个属性即将变化</li>
<li>通过transform的3D属性强制开始GPU加速<code>transform: translateZ(0);transform: rotateZ(360deg);</code></li>
</ul>
<h2 id="68-4-使用硬件加速的注意事项"><a href="#68-4-使用硬件加速的注意事项" class="headerlink" title="68.4 使用硬件加速的注意事项"></a>68.4 使用硬件加速的注意事项</h2><ul>
<li>不能让每个元素都启用硬件加速，这样会占用很大的内存，使页面有很强的卡顿感</li>
<li>GPU渲染会影响字体的抗锯齿效果，这是因为GPU和CPU具有不同的渲染机制，即使最终硬件加速停止了，文本还是会在动画期间显示得很模糊</li>
</ul>
<h1 id="69-CSS模块化"><a href="#69-CSS模块化" class="headerlink" title="69. CSS模块化"></a>69. CSS模块化</h1><h2 id="69-1-vue中style标签中设置scoped的原理"><a href="#69-1-vue中style标签中设置scoped的原理" class="headerlink" title="69.1 vue中style标签中设置scoped的原理"></a>69.1 vue中style标签中设置scoped的原理</h2><blockquote>
<p>这个局部样式是通过PostCSS给组件中所有的DOM添加了一个独一无二的动态属性，然后通过CSS属性选择器来选择组件中的DOM</p>
</blockquote>
<h2 id="69-2-CSS预处理"><a href="#69-2-CSS预处理" class="headerlink" title="69.2 CSS预处理"></a>69.2 CSS预处理</h2><ul>
<li><p>优点：</p>
<ul>
<li>用一种专门的变成语言，进行Web页面样式设计，再编译成正常的CSS文件，以便项目使用</li>
<li>CSS更加简洁、适应性更强，更易于代码的维护</li>
</ul>
</li>
<li><p>基本语法</p>
<blockquote>
<p>Less的基本语法和CSS差不多，SASS和Stylus都可以用缩进代替花括号，并且空格有重要的意义</p>
</blockquote>
  <figure class="highlight"><table><tr><td class="code"><pre><span class="line">//less&amp;scssul &#123;  list-style: none;&#125;//sassul	list-style: none//stylusul	list-style: none</span><br></pre></td></tr></table></figure>
</li>
<li><p>变量</p>
  <figure class="highlight"><table><tr><td class="code"><pre><span class="line">//less&amp;scss@purple: #666699header &#123;  background: @purple&#125;//sass$purple: #666699header	background: $purple/stylusbgpurple = #666699header	background: bgpurple</span><br></pre></td></tr></table></figure>
</li>
<li><p>变量作用域</p>
  <figure class="highlight"><table><tr><td class="code"><pre><span class="line">//less: less中变量的计算，都是以这个变量最后一次被定义的值为准，这个行为叫懒加载@size: 40px;.content &#123;  width:＠size;&#125;@size: 60px;.container &#123;  width:＠size;&#125;=&gt; 编译输出.content &#123;  width: 60px;&#125;.container &#123;  width: 60px;&#125;//sass、stylus$size: 40px;.content &#123;  width: $size;&#125;$size: 60px;.container &#123;  width: $size;&#125;=&gt; 编译输出.content &#123;  width: 40px;&#125;.container &#123;  width: 60px;&#125;</span><br></pre></td></tr></table></figure>

<blockquote>
<p>sass和less语法严谨，Stylus相对自由，因为Less更像原生的CSS，所有可能学起来更容易</p>
<p>sass和compass、stylus和nib都是好朋友？？？</p>
<p>sass和stylus都具有类语言的逻辑方式处理：条件、循环等；less需要通过when等关键词模拟这些功能，这方面less比不上sass和stylus</p>
<p>less在丰富性及特色上都不及sass和stylus秒如果不是bootstrap引入了less，可能不会这样被广泛应用</p>
</blockquote>
</li>
</ul>
<h1 id="70-布局"><a href="#70-布局" class="headerlink" title="70. 布局"></a>70. 布局</h1><h2 id="70-1-水平居中的实现方案"><a href="#70-1-水平居中的实现方案" class="headerlink" title="70.1 水平居中的实现方案"></a>70.1 水平居中的实现方案</h2><ul>
<li>利用块级元素撑满父元素的特点，如果宽度已定，左右margin auto就可以平分剩余空间</li>
<li>利用行内块居中：把父级元素设置为<code>text-align: center</code>，之后子元素的display设置为inline-bolck</li>
<li>绝对定位：position: absolute，之后left: 50%</li>
<li>flex：父元素<code>display: flex; justify-content: center</code></li>
</ul>
<h2 id="70-2-垂直居中的实现方案"><a href="#70-2-垂直居中的实现方案" class="headerlink" title="70.2 垂直居中的实现方案"></a>70.2 垂直居中的实现方案</h2><ul>
<li><p>元素无高度：利用内边距，让块级元素包裹在padding中，实现垂直居中</p>
</li>
<li><p>父元素高度确定的单行文本：利用行高特性：line-height = height</p>
</li>
<li><p>父元素高度确定的多行文本：利用<code>vertical-align</code>（只能内联元素），如果是div，可以设置为table和table-cell</p>
  <figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">display</span>: <span class="selector-tag">table-cell</span>;<span class="selector-tag">vertical-align</span>: <span class="selector-tag">center</span>;</span><br></pre></td></tr></table></figure>
</li>
<li><p>元素高度位置未知：</p>
<ul>
<li><p>绝对定位，设置top: 50%</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.parent</span> &#123;  <span class="attribute">position</span>: relative;&#125;<span class="selector-class">.child</span> &#123;  <span class="attribute">position</span>: absolute;  <span class="attribute">top</span>: <span class="number">50%</span>;  <span class="attribute">transform</span>: <span class="built_in">translateY</span>(-<span class="number">50%</span>);&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>子元素有高度：</p>
<figure class="highlight"><table><tr><td class="code"><pre><span class="line">.child &#123;  position: absolute;  top: 50%;  height = 50px;  margin-top: (50/2)px;&#125;</span><br></pre></td></tr></table></figure>
</li>
</ul>
</li>
<li><p>父元素高度已知：</p>
  <figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.parent</span> &#123;  <span class="attribute">height</span>: <span class="number">50px</span>;&#125;<span class="selector-class">.child</span> &#123;  <span class="attribute">position</span>: relative;  <span class="attribute">top</span>: <span class="number">50%</span>;  <span class="attribute">transform</span>: <span class="built_in">translateY</span>(-<span class="number">50%</span>);&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>flex方法</p>
  <figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">display</span>: <span class="selector-tag">flex</span>;<span class="selector-tag">aligin-item</span>: <span class="selector-tag">center</span>;</span><br></pre></td></tr></table></figure>

</li>
</ul>
<h2 id="70-3-垂直水平居中的实现方案"><a href="#70-3-垂直水平居中的实现方案" class="headerlink" title="70.3 垂直水平居中的实现方案"></a>70.3 垂直水平居中的实现方案</h2><ul>
<li><p>居中元素的宽高已知：</p>
<ul>
<li><p>利用绝对定位和margin</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.parent</span> &#123;  <span class="attribute">position</span>: relative;&#125;<span class="selector-class">.child</span> &#123;  <span class="attribute">position</span>: absolute;  <span class="attribute">height</span>: <span class="number">50px</span>;  <span class="attribute">width</span>: <span class="number">50px</span>;  <span class="attribute">top</span>: <span class="number">50%</span>;  <span class="attribute">left</span>: <span class="number">50%</span>;  <span class="attribute">margin-top</span>: (-<span class="number">50</span>)/<span class="number">2px</span>;  <span class="attribute">margin-left</span>: (-<span class="number">50</span>)/<span class="number">2px</span>;&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>absolute+margin: auto</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.child</span> &#123;  <span class="attribute">width</span>: <span class="number">50px</span>;  <span class="attribute">height</span>: <span class="number">50px</span>;  <span class="attribute">position</span>: absolute;  <span class="attribute">top</span>: <span class="number">0</span>;  <span class="attribute">left</span>: <span class="number">0</span>;  <span class="attribute">right</span>: <span class="number">0</span>;  <span class="attribute">bottom</span>: <span class="number">0</span>;  <span class="attribute">margin</span>: auto;&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>用calu计算</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.child</span> &#123;  <span class="attribute">position</span>: absolute;  <span class="attribute">top</span>: <span class="number">50%</span>;  <span class="attribute">left</span>: <span class="number">50px</span>;  <span class="attribute">transform</span>: <span class="built_in">translate</span>(-<span class="number">50%</span>, -<span class="number">50%</span>);&#125;</span><br></pre></td></tr></table></figure>
</li>
</ul>
</li>
<li><p>垂直居中的元素宽高位置</p>
<ul>
<li><p>transform的translate方法</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.child</span> &#123;  <span class="attribute">position</span>: absolute;  <span class="attribute">top</span>: <span class="number">50%</span>;  <span class="attribute">left</span>: <span class="number">50%</span>;  <span class="attribute">transform</span>: <span class="built_in">translate</span>(-<span class="number">5</span>o%, -<span class="number">50%</span>);&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>flex布局</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.child</span> &#123;  <span class="attribute">display</span>: flex;  <span class="attribute">align-items</span>: center;  <span class="attribute">justify-content</span>: center;&#125;</span><br></pre></td></tr></table></figure>

</li>
</ul>
</li>
</ul>
<h2 id="70-4-两栏布局，左边固定，先加载内容区"><a href="#70-4-两栏布局，左边固定，先加载内容区" class="headerlink" title="70.4  两栏布局，左边固定，先加载内容区"></a>70.4  两栏布局，左边固定，先加载内容区</h2><ul>
<li>float =&gt;两个div。 <code>左边：float: left; width: 200px; 右边：margin-left = width</code></li>
<li>绝对定位 =&gt; 两个div。<code>左边：absolute或者fixed; 右边：margin-left = width</code></li>
<li>table布局 =&gt; 三个div。<code>父元素display: table，子元素display: table-cell; width: xxpx; 右边自适应</code></li>
<li>flex布局 =&gt; 三个div。<code>父元素display: flex; 子元素flex: 1;</code></li>
</ul>
<h2 id="70-5-三栏布局"><a href="#70-5-三栏布局" class="headerlink" title="70.5 三栏布局"></a>70.5 三栏布局</h2><ul>
<li>浮动布局：<code>float: left right</code></li>
<li>绝对定位</li>
<li>BFC</li>
<li>Flex写法左中右</li>
<li>table</li>
<li>圣杯布局</li>
<li>双飞翼</li>
</ul>
<h2 id="70-6-Flex布局"><a href="#70-6-Flex布局" class="headerlink" title="70.6 Flex布局"></a>70.6 Flex布局</h2><blockquote>
<p>flex是弹性布局，用来为盒子模型提供最大的灵活性，布局的传统解决方法基于盒子模型，依赖于display、position和float属性。<strong>任何一个容器都可以指定为flex布局</strong></p>
<p>设置为flex布局后，子元素的float、clear和vertical-align属性将失效</p>
<p>属性分为容器属性和元素属性。</p>
</blockquote>
<ul>
<li><p>容器属性：</p>
<ul>
<li><p>flex-direction：决定主轴方向</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.box</span> &#123;  <span class="attribute">flex-direction</span>: row | row-reverse | column | column-reverse;&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>flex-wrap：决定了如何换行</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.box</span> &#123;  <span class="attribute">flex-wrap</span>: no-wrap | wrap | wrap-reverse;&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>flex-flow：前面两个的简写</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.box</span> &#123;  <span class="attribute">flex-flow</span>: &lt;flex-direction&gt; || &lt;flex-wrap&gt;;&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>justify-content：水平轴对齐方式</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.box</span> &#123;  <span class="attribute">flex-flow</span>: flex-start | flex-end | center | space-between | space-around;&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>align-items：垂直轴对齐方式</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.box</span> &#123;  <span class="attribute">align-items</span>: flex-start | flex-end | center | space-between | space-around;&#125;</span><br></pre></td></tr></table></figure>
</li>
</ul>
</li>
<li><p>元素属性align-items</p>
<ul>
<li>order定义项目的排序顺序，越小越靠前</li>
<li>flex-grow：放大比例，默认是0，即使存在空间也不会放大，1是说等分剩余空间</li>
<li>flex-shrink：缩小比例，当空间不够的情况下，会等比缩小，为0不缩小，为1等比缩小</li>
<li>flex-basis：定义分配多余空间时，项目占据的空间</li>
<li>flex：上面三个属性的缩写，默认是0 1 auto，后两个属性可选</li>
<li>align-self：允许单个与其他不一样的对齐方式，可以覆盖align-items属性，默认是auto表示继承</li>
</ul>
</li>
</ul>
<h2 id="70-7-Grid布局"><a href="#70-7-Grid布局" class="headerlink" title="70.7 Grid布局"></a>70.7 Grid布局</h2><p>容器属性：</p>
<ul>
<li>display: grid指定一个容器采用网格布局</li>
<li>grid-template-columns：定义每一列的列宽，可以用百分比</li>
<li>grid-template-rows：定义每一行的行高，可以用百分比</li>
</ul>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.container</span> &#123;  <span class="attribute">display</span>: grid;  <span class="attribute">grid-template-columns</span>: <span class="number">33.33%</span> <span class="number">33.33%</span> <span class="number">33.33%</span>;  <span class="attribute">grid-template-rows</span>: <span class="number">33.33%</span> <span class="number">33.33%</span> <span class="number">33.33%</span>;&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>grid-row-gap：行间距</li>
<li>grid-column-gap：列间距</li>
<li>grid-gap：上面两个的简写</li>
</ul>
<h2 id="70-8-多端适应"><a href="#70-8-多端适应" class="headerlink" title="70.8 多端适应"></a>70.8 多端适应</h2><ul>
<li>静态布局<ul>
<li>特点：不管浏览器尺寸具体是多少，网页布局始终按照最初写代码时的布局来显示</li>
<li>一般都使用min-width定宽，小于这个宽度就会出现滚动条，大于就内容居中加背景</li>
<li>设计方法：<ul>
<li>PC端居中布局所有样式绝对宽高，设计一个layout，在屏幕宽高有调整时，使用横向和竖向的滚动条来查阅被遮掩的部分</li>
<li>移动设备另外建立，单独设计一个布局，使用不同的域名如wap或m</li>
</ul>
</li>
</ul>
</li>
<li>流式布局<ul>
<li>特点：屏幕分辨率变化时，页面里元素的大小会变化但布局不变。（如果屏幕太大或者太小都会导致元素无法正常显示）</li>
<li>设计方法：<ul>
<li>使用%百分比定义宽度，高度大都是用px固定住，可以根据可视区域（viewport）和父元素的实时尺寸进行调整，尽可能地适应各种分别率</li>
<li>往往配合max-width/min-width等属性控制尺寸流动范围，以免过大或者过小影响阅读</li>
</ul>
</li>
<li>该布局方式在Web前端开发的早期历史上，用来应对不同尺寸的PC屏幕（那时屏幕尺寸的差异不会太大），在当今的移动端开发也是常用布局方式，但缺点明显：<ul>
<li>如果屏幕尺寸跨度太大，那么在相对其原始设计而言，过小或者过大的屏幕上不能正常显示</li>
<li>因为宽度使用%百分比定义，但是高度和文字大小等都是用px固定，所以在大屏幕的手机下显示效果会变成有些页面元素宽度被拉得很长，但是高度、文字带下还是和原来一样，显示非常不协调</li>
</ul>
</li>
</ul>
</li>
<li>自适应布局<ul>
<li>分别为不同的屏幕分辨率定义布局，即创建多个静态布局，每个静态布局对应一个屏幕分辨率。改变屏幕分辨率可以切换不同的静态局部页面（页面元素位置发生改变），但在每个静态布局中，页面元素不随窗口大小的调整发生变化，可以把自适应布局看作是静态布局的一个系列</li>
<li>特点：屏幕分辨率变化时，页面里面元素的位置会变化，但大小不会变化</li>
<li>设计方法：使用<code>@media</code>媒体查询给不同尺寸和介质的设备切换不同的样式，在优秀的响应范围设计下可以给适配范围内的设备最好的体验，在同一个设备下实际还是固定的布局</li>
</ul>
</li>
<li>响应式布局<ul>
<li>响应式设计的目标是确保一个页面在所有终端上（各种尺寸的PC、手机、手表、冰箱的Web浏览器等等）都能显示出令人满意的效果，对CSS编写者而言，在实现上不拘泥于具体手法，但通常是糅合了流式布局+弹性布局，再搭配媒体查询技术使用</li>
<li>分别为不同的屏幕分辨率定义布局，同时在每个布局中，应用流式布局的理念，即页面元素宽度随着窗口调整而自动适配，即：创建多个流体式布局，分别对应一个屏幕分辨率范围，可以把响应式布局看作是<strong>流式布局和自适应布局设计理念的融合</strong></li>
<li>特点：每个屏幕分辨率下面会有一个布局样式，即元素位置和大小都会变</li>
<li>媒体查询+流式布局：通常使用<code>@media</code>媒体查询和网格系统（Grid System）配合相对布局单位进行布局，实际上就是综合响应式、流动等上述技术通过CSS给单一网页不同设备返回不同样式的技术统称</li>
<li>优点：适应PC和移动端，效果好</li>
<li>缺点：<ul>
<li>媒体查询是有限的，就是可以枚举出来的，只能适应主流的宽高</li>
<li>要匹配足够多的屏幕大小，工作量不小，设计也需要多个版本</li>
</ul>
</li>
</ul>
</li>
<li>弹性布局<ul>
<li>特点：包裹文字的<strong>各元素的尺寸</strong>采用em/rem做单位，而页面的主要划分区域的尺寸仍使用百分数或px做单位（同<code>流式布局</code>或<code>静态/固定布局</code>。早期浏览器不支持整个页面按比例缩放，仅支持网页内文字尺寸的放大，这种情况下，使用em/rem做单位，可以使包裹文字的元素随着文字的缩放而缩放</li>
</ul>
</li>
</ul>
<p>i</p>
]]></content>
      <categories>
        <category>面试</category>
      </categories>
      <tags>
        <tag>面试</tag>
        <tag>CSS</tag>
      </tags>
  </entry>
  <entry>
    <title>面试 | HTML</title>
    <url>/2021/06/02/HTML/</url>
    <content><![CDATA[<h1 id="1-Doctype有什么作用"><a href="#1-Doctype有什么作用" class="headerlink" title="1. Doctype有什么作用"></a>1. Doctype有什么作用</h1><blockquote>
<p>声明文件类型定义（DTD），位于文档中最前面，作用是<strong>为了告知浏览器应该用哪种文档类型规范来解析文档</strong>。</p>
</blockquote>
<h1 id="2-如何区分严格模式和混杂模式？区分的意义是什么？"><a href="#2-如何区分严格模式和混杂模式？区分的意义是什么？" class="headerlink" title="2. 如何区分严格模式和混杂模式？区分的意义是什么？"></a>2. 如何区分严格模式和混杂模式？区分的意义是什么？</h1><ul>
<li>定义：<ul>
<li>严格模式（标准模式）：浏览器按照W3C标准来解析</li>
<li>混杂模式：向后兼容的解析方法，浏览器用自己的方式解析代码</li>
</ul>
</li>
<li>如何区分？用DTD来判断：<ul>
<li>严格格式DTD——严格模式</li>
<li>有URL的过渡DTD——严格模式，没有URL的过渡DTD——混杂模式</li>
<li>DTD不存在/格式不对——混杂模式</li>
<li>HTML5没有严格和混杂模式之分</li>
</ul>
</li>
<li>区分的意义：严格模式的排版和js运行模式以浏览器支持的最高标准运行，如果只存在严格模式，那么很多旧网站站点无法工作</li>
</ul>
<h1 id="3-为什么HTML5只需要写-lt-DOCTYPE-HTML-gt"><a href="#3-为什么HTML5只需要写-lt-DOCTYPE-HTML-gt" class="headerlink" title="3. 为什么HTML5只需要写&lt;!DOCTYPE HTML&gt;"></a>3. 为什么HTML5只需要写<code>&lt;!DOCTYPE HTML&gt;</code></h1><blockquote>
<p>HTML5不基于SGML（Standard Generalized Markup Language，标准通用标记语言），因此不需要对DTD进行引用，但是需要doctype来规范浏览器的行为</p>
</blockquote>
<h1 id="4-meta标签用来做什么？"><a href="#4-meta标签用来做什么？" class="headerlink" title="4. meta标签用来做什么？"></a>4. meta标签用来做什么？</h1><blockquote>
<p>提供给机器解读的一些元信息（meta-information），如页面搜索引擎优化、定义页面实用语言等；</p>
<p>标签位于文档的头部，不包含任何内容，其属性定义了与文档相关联的名称/值对;</p>
<p>所有浏览器都支持meta标签；</p>
<p>HTML中meta标签没有结束标签；</p>
<p>XHTML中meta标签必须被正确地关闭；</p>
<p><strong>meta标签永远位于head元素内部，元数据总是以名称/值的形式被成对传递</strong></p>
</blockquote>
<ul>
<li>http-equi+content<ul>
<li>charset：编码格式</li>
<li>expires：过期时间</li>
<li>refresh：特定时间内自动刷新跳转</li>
<li>pragma：禁止浏览器从本地计算机缓存中访问页面内容no-cache</li>
<li>windows-target：设定页面在窗口中以独立页面展示，防止被当成frame页调用</li>
<li>set-cookie：自定义cookie</li>
<li>content-type：字符集</li>
</ul>
</li>
<li>name+content<ul>
<li>keywords：关键字</li>
<li>description：主要内容</li>
<li>robots：none不被检索</li>
<li>author、generator：使用的制作软件</li>
<li>copyright</li>
<li>viewport：缩放比例</li>
</ul>
</li>
</ul>
<h1 id="5-src和href的区别"><a href="#5-src和href的区别" class="headerlink" title="5. src和href的区别"></a>5. src和href的区别</h1><ul>
<li>href指向网络资源位置，建立当前文档和资源的链接，一般用于<strong>超链接</strong>。</li>
<li>src将资源嵌入到当前文档中，在请求src资源时会将其指向的资源下载并应用到文档内，例如JS脚本、img图片和frame等元素。<strong>当浏览器解析到该元素时，会暂停其他资源的下载和处理，直到该资源加载、编译、执行完毕，图片和框架等元素也是如此，类似于将所指向资源嵌入当前标签内。</strong>这也是为什么将JS脚本放在底部而不是头部。</li>
</ul>
<h1 id="6-Ajax和Flash的优劣对比"><a href="#6-Ajax和Flash的优劣对比" class="headerlink" title="6. Ajax和Flash的优劣对比"></a>6. Ajax和Flash的优劣对比</h1><ul>
<li>Ajax：<ul>
<li>优势：<ul>
<li>可搜索性：<strong>普通的文本网页会更有利于SEO。</strong>文本内容是搜索引擎容易检索的，而繁琐的swf字节码却是搜索引擎不愿触及的。虽然Google等一些大型的搜索引擎可以检索SWF内部的内容，但仍然有很多麻烦存在。</li>
<li>开放性：Flash常年以来被Macromedia看得很死。包括Flex、FMS等辅佐技术一直都需要昂贵的安装、维护费用，而JS则没有这样的麻烦，没有人愿意承担法律和版权的风险。</li>
<li>费用：Flash开发是很昂贵的，因为FlashIDE等环境都是要收费的，而Ajax则不同，虽然有一些便宜的生成swf的工具，但它们的功能无法满足复杂要求。</li>
<li>易用性：Ajax程序有更好的易用性。由于中间有一层Flashplayer代理层，因此许多辅助功能无法被Flash灵活利用。而且Flash在一些方面有着不好的口碑，如广告弹窗、恶意代码等。</li>
</ul>
</li>
<li>劣势：<ul>
<li>可能破坏浏览器的后退功能。</li>
<li>使用动态页面更新使得用户难于将某个特定的状态保存到收藏夹中（这些都有相关方法解决）  哪些方法？</li>
</ul>
</li>
</ul>
</li>
<li>Flash：<ul>
<li>优势：<ul>
<li>多媒体处理：Flash在音频、视频等多媒体领域相比HTML有绝对的优势，现在几乎所有的网站都包含有Flash内容。</li>
<li>兼容性好：由于通过了唯一的Flashplayer代理，不必像调试JS一样在不同的浏览器中调试程序。</li>
<li>矢量图形：Flash最大的优势，在该领域SVG、Canvas和Direct都不能与Flash相比。</li>
<li>客户端资源调度：能够更容易地调用浏览器以外的外部资源。如摄像头、麦克风等们这些是普通的HTML无法完成的。</li>
</ul>
</li>
<li>劣势：<ul>
<li>二进制格式</li>
<li>格式私有</li>
<li>Flash文件经常会很大，用户第一次使用的时候需要忍耐较长的等待时间</li>
<li>性能问题</li>
</ul>
</li>
</ul>
</li>
</ul>
<h1 id="7-HTML5新特点"><a href="#7-HTML5新特点" class="headerlink" title="7. HTML5新特点"></a>7. HTML5新特点</h1><ul>
<li>Canvas：JS绘图</li>
<li>draggable属性：可拖动</li>
<li>geolocationAPI：获取用户地理位置</li>
<li>audio/video：音频/视频元素</li>
<li>input类型增多：color、date、datetime、datatime-local、email、month、search、tel、time、url、week</li>
<li>表单元素增强：datalist与input配合使用规定输入域的选项列表；keygen密钥；output定义不同类的输出</li>
<li>web存储：sessionStorage针对一个session进行数据存储，关闭浏览器窗口后清除，localStorage没有时间限制，不过可能会因为本地时间修改失效，大量复杂数据结构一般使用indexDB</li>
<li>Web worker页面中执行脚本时，页面状态不可响应，直到脚本完成。在后台运行，独立于其他脚本，不会影响页面的性能（相当于多进程并发）</li>
<li>SSE（server-sent-event）：网页自动获取来自服务器的更行，用于接收服务器发送时间通知</li>
<li>WebSocket：在单个TCP连接上进行全双工通讯的协议。只需要握手一次，形成快速通道，传输数据。客户端和服务器可以直接通过TCP交换数据。获取连接之后，可以用send发送数据，用onmessage接收服务器返回的数据</li>
<li>新API：History、Command、Application cache……</li>
</ul>
<h1 id="8-HTML新的语义-结构元素"><a href="#8-HTML新的语义-结构元素" class="headerlink" title="8. HTML新的语义/结构元素"></a>8. HTML新的语义/结构元素</h1><ul>
<li><p>新的元素：</p>
<table>
<thead>
<tr>
<th align="left">标签</th>
<th align="left">描述</th>
</tr>
</thead>
<tbody><tr>
<td align="left"><article></td>
<td align="left">定义文档内的文章。</td>
</tr>
<tr>
<td align="left"><aside></td>
<td align="left">定义页面内容之外的内容。</td>
</tr>
<tr>
<td align="left"><bdi></td>
<td align="left">定义与其他文本不同的文本方向。</td>
</tr>
<tr>
<td align="left"><details></td>
<td align="left">定义用户可查看或隐藏的额外细节。</td>
</tr>
<tr>
<td align="left"><dialog></td>
<td align="left">定义对话框或窗口。</td>
</tr>
<tr>
<td align="left"><figcaption></td>
<td align="left">定义 <figure> 元素的标题。</td>
</tr>
<tr>
<td align="left"><figure></td>
<td align="left">定义自包含内容，比如图示、图表、照片、代码清单等等。</td>
</tr>
<tr>
<td align="left"><footer></td>
<td align="left">定义文档或节的页脚。</td>
</tr>
<tr>
<td align="left"><header></td>
<td align="left">定义文档或节的页眉。</td>
</tr>
<tr>
<td align="left"><main></td>
<td align="left">定义文档的主内容。</td>
</tr>
<tr>
<td align="left"><mark></td>
<td align="left">定义重要或强调的内容。</td>
</tr>
<tr>
<td align="left"><menuitem></td>
<td align="left">定义用户能够从弹出菜单调用的命令/菜单项目。</td>
</tr>
<tr>
<td align="left"><meter></td>
<td align="left">定义已知范围（尺度）内的标量测量。</td>
</tr>
<tr>
<td align="left"><nav></td>
<td align="left">定义文档内的导航链接。</td>
</tr>
<tr>
<td align="left"><progress></td>
<td align="left">定义任务进度。</td>
</tr>
<tr>
<td align="left"><rp></td>
<td align="left">定义在不支持 ruby 注释的浏览器中显示什么。</td>
</tr>
<tr>
<td align="left"><rt></td>
<td align="left">定义关于字符的解释/发音（用于东亚字体）。</td>
</tr>
<tr>
<td align="left"><ruby></td>
<td align="left">定义 ruby 注释（用于东亚字体）。</td>
</tr>
<tr>
<td align="left"><section></td>
<td align="left">定义文档中的节。</td>
</tr>
<tr>
<td align="left"><summary></td>
<td align="left">定义 <details> 元素的可见标题。</td>
</tr>
<tr>
<td align="left"><time></td>
<td align="left">定义日期/时间。</td>
</tr>
<tr>
<td align="left"><wbr></td>
<td align="left">定义可能的折行（line-break）。</td>
</tr>
</tbody></table>
</li>
<li><p>新的表单元素：</p>
<table>
<thead>
<tr>
<th>标签</th>
<th>描述</th>
</tr>
</thead>
<tbody><tr>
<td><dttalist></td>
<td>定义输入空间的预定义选项</td>
</tr>
<tr>
<td><keygen></td>
<td>定义键对生成器字段（用于表单）</td>
</tr>
<tr>
<td><output></td>
<td>定义计算结果</td>
</tr>
</tbody></table>
</li>
<li><p>新的输入类型：</p>
<ul>
<li>新的输入类型：<ul>
<li>color</li>
<li>date</li>
<li>datetime</li>
<li>datetime-local</li>
<li>email</li>
<li>month</li>
<li>number</li>
<li>range</li>
<li>search</li>
<li>tel</li>
<li>time</li>
<li>url</li>
<li>week</li>
</ul>
</li>
<li>新的输入属性：<ul>
<li>autocomplete</li>
<li>autofocus</li>
<li>form</li>
<li>formaction</li>
<li>formenctype</li>
<li>formmethod</li>
<li>formnovalidate</li>
<li>formtarget</li>
<li>height 和 width</li>
<li>list</li>
<li>min 和 max</li>
<li>multiple</li>
<li>pattern (regexp)</li>
<li>placeholder</li>
<li>required</li>
<li>step</li>
</ul>
</li>
</ul>
</li>
<li><p>新的属性语法：</p>
<blockquote>
<p>HTML5允许四种不同的属性语法</p>
</blockquote>
<table>
<thead>
<tr>
<th align="left">标签</th>
<th align="left">描述</th>
</tr>
</thead>
<tbody><tr>
<td align="left">Empty</td>
<td align="left"><input type="text" value="John Doe" disabled></td>
</tr>
<tr>
<td align="left">Unquoted</td>
<td align="left"><input type="text" value=John></td>
</tr>
<tr>
<td align="left">Double-quoted</td>
<td align="left"><input type="text" value="John Doe"></td>
</tr>
<tr>
<td align="left">Single-quoted</td>
<td align="left"><input type="text" value='John Doe'></td>
</tr>
</tbody></table>
</li>
<li><p>HTML5图像：</p>
<table>
<thead>
<tr>
<th align="left">标签</th>
<th align="left">描述</th>
</tr>
</thead>
<tbody><tr>
<td align="left"><canvas></td>
<td align="left">定义使用 JavaScript 的图像绘制。</td>
</tr>
<tr>
<td align="left"><svg></td>
<td align="left">定义使用 SVG 的图像绘制。</td>
</tr>
</tbody></table>
</li>
<li><p>新的媒介元素：</p>
<table>
<thead>
<tr>
<th align="left">标签</th>
<th align="left">描述</th>
</tr>
</thead>
<tbody><tr>
<td align="left"><audio></td>
<td align="left">定义声音或音乐内容。</td>
</tr>
<tr>
<td align="left"><embed></td>
<td align="left">定义外部应用程序的容器（比如插件）。</td>
</tr>
<tr>
<td align="left"><source></td>
<td align="left">定义 <video> 和 <audio> 的来源。</td>
</tr>
<tr>
<td align="left"><track></td>
<td align="left">定义 <video> 和 <audio> 的轨道。</td>
</tr>
<tr>
<td align="left"><video></td>
<td align="left">定义视频或影片内容。</td>
</tr>
</tbody></table>
</li>
</ul>
<h1 id="9-什么是shadow-DOM"><a href="#9-什么是shadow-DOM" class="headerlink" title="9. 什么是shadow DOM"></a>9. 什么是shadow DOM</h1><blockquote>
<p>shadow DOM是浏览器的一种功能，能自动添加子元素，比如radio元素的controls，这些相关元素由浏览器生成</p>
</blockquote>
<h1 id="10-Canvas有什么用"><a href="#10-Canvas有什么用" class="headerlink" title="10. Canvas有什么用"></a>10. Canvas有什么用</h1><ul>
<li><p>相当于在页面上新建了一个画布，可以用JS画图</p>
</li>
<li><p>获取DOM，创建画布<code>getContext(&#39;2d&#39;)</code></p>
<ul>
<li><p>矩形：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">fillRect(x, y, width, height) <span class="comment">//填充</span></span><br><span class="line">strokeRect(x, y, width, height) <span class="comment">//边框</span></span><br><span class="line">clearRect(x, y, width, height) <span class="comment">//清除指定区域</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>路径：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">beginPath() <span class="comment">//新建路径</span></span><br><span class="line">closePath() <span class="comment">//闭合路径</span></span><br><span class="line">stroke() <span class="comment">//描边</span></span><br><span class="line">fill() <span class="comment">//填充</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>移动笔触：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">moveTo(x, y) <span class="comment">//移动到某点</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>绘制直线：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">lineTo(x, y) <span class="comment">//从当前位置绘制到(x, y)的一条直线</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>绘制圆：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">arc(x, y, radius, startAngle, endAngle, anticlockwise) <span class="comment">//以(x, y)为圆心，radius为半径，stratAngle和endAngle以x轴为准开始结束的弧度，anticlockwise为false为顺时针，true则逆时针</span></span><br></pre></td></tr></table></figure>

</li>
</ul>
</li>
</ul>
<h1 id="11-不改变图片原始大小画到canvas上面"><a href="#11-不改变图片原始大小画到canvas上面" class="headerlink" title="11.  不改变图片原始大小画到canvas上面"></a>11.  不改变图片原始大小画到canvas上面</h1><ul>
<li><p>第一种，直接在坐标上画图，如果图片大小超出了画布也不缩放<code>drawImage(image,x,y);</code></p>
</li>
<li><p>第二种，绘制开始位置，缩放位置，图片会变形<code>drawImage(image,x1,y1,x2,y2);</code></p>
</li>
<li><p>第三种，从图片的某个坐标开始截图，截取m*n的大小。然后截的图片从canvas的x1,y1画到x2,y2。<code>drwaImage(image,xi,yi,m,n,x1,y1,x2,y2)</code></p>
</li>
</ul>
<h1 id="12-前端需要注意哪些SEO？"><a href="#12-前端需要注意哪些SEO？" class="headerlink" title="12. 前端需要注意哪些SEO？"></a>12. 前端需要注意哪些SEO？</h1><ul>
<li>合理的<code>title</code>、<code>description</code>、<code>keywords</code>：搜索对着三项的权重逐个较小，<code>title</code>值强调重点即可，重要关键词出现不要超过2次，而且要靠前，不同页面<code>title</code>要有所不同；<code>description</code>把页面内容高度概括，长度合适，不可过分堆砌关键词，不同页面<code>description</code>有所不同；<code>keywords</code>列举出重要关键词即可</li>
<li>语义化的<code>HTML</code>代码，符合<code>W3C</code>规范：语义化代码让搜索引擎容易理解网页</li>
<li>重要内容<code>HTML</code>代码放在最前：搜索引擎抓取HTML顺序是从上到下，有的搜索引擎对抓取长度有限制，保证重要内容一定会被抓取</li>
<li>重要内容不要用<code>js</code>输出：爬虫不会执行<code>js</code>获取内容</li>
<li>少用<code>iframe</code>：搜索引擎不会抓取<code>iframe</code>中的内容</li>
<li>非装饰性图片必须要加<code>alt</code></li>
<li>提高网站速度：网站速度是搜索引擎排序的一个重要指标</li>
</ul>
<h1 id="13-img的title和alt有什么区别"><a href="#13-img的title和alt有什么区别" class="headerlink" title="13. img的title和alt有什么区别"></a>13. img的title和alt有什么区别</h1><ul>
<li>当鼠标滑动到元素上的时候显示<code>title</code>的内容</li>
<li>alt是<code>&lt;img&gt;</code>的特有属性，是图片内容的等价描述，用于图片无法加载时显示、读屏器阅读图片；提高图片的可访问性，除了纯装饰性图片都必须设置有意义的值，搜索引擎会重点分析</li>
</ul>
<h1 id="14-HTTP的几种请求方法和用途？"><a href="#14-HTTP的几种请求方法和用途？" class="headerlink" title="14. HTTP的几种请求方法和用途？"></a>14. HTTP的几种请求方法和用途？</h1><ul>
<li><code>GET</code>方法：发送一个请求来<strong>获得</strong>服务器上的某一资源。</li>
<li><code>POST</code>方法：向URL指定的资源<strong>提交</strong>数据或附加新的数据。</li>
<li><code>PUT</code>方法：跟<code>POST</code>方法很像，也是向服务器提交数据。<code>PUT</code>指定了资源在服务器上的位置，而<code>POST</code>没有。</li>
<li><code>HEAD</code>方法：只请求服务器上的某资源。</li>
<li><code>OPTION</code>方法：用于获取当前URL所支持的方法。如果请求成功，会有一个<code>ALLOW</code>的头包含类似<code>GET/POST</code>的信息。</li>
<li><code>TRACE</code>方法：被用于激发一个远程的、应用层的请求消息回路。</li>
<li><code>CONNECT</code>方法：把请求连接转换到透明的<code>TCP/IP</code>通道。</li>
</ul>
<h1 id="15-从浏览器地址栏输入url到显示页面的步骤-待完善"><a href="#15-从浏览器地址栏输入url到显示页面的步骤-待完善" class="headerlink" title="15. 从浏览器地址栏输入url到显示页面的步骤  待完善"></a>15. 从浏览器地址栏输入url到显示页面的步骤  待完善</h1><h1 id="16-如何进行网站性能优化？-继续完善"><a href="#16-如何进行网站性能优化？-继续完善" class="headerlink" title="16. 如何进行网站性能优化？  继续完善"></a>16. 如何进行网站性能优化？  继续完善</h1><ul>
<li>content方面：<ul>
<li>减少HTTP请求：合并文件、CSS精灵、<code>inline Image</code></li>
<li>减少DNS查询：DNS缓存、将资源分布到恰当数量的主机名</li>
<li>减少DOM元素数量</li>
</ul>
</li>
<li>Server方面：<ul>
<li>使用CDN</li>
<li>配置ETag</li>
<li>对组件使用Gzip压缩</li>
</ul>
</li>
<li>Cookie方面：减小cookie大小</li>
<li>CSS方面：<ul>
<li>将样式表放到页面顶部</li>
<li>不使用CSS表达式</li>
<li>使用<code>&lt;link&gt;</code>不使用<code>@import</code></li>
</ul>
</li>
<li>JavaScript方面：<ul>
<li>将脚本放到页面底部</li>
<li>将JavaScript和CSS从外部引入</li>
<li>压缩JavaScript和CSS</li>
<li>删除不需要的脚本</li>
<li>减少DOM访问</li>
</ul>
</li>
<li>图片方面：<ul>
<li>优化图片：根据实际颜色需要选择色深、压缩</li>
<li>优化CSS精灵</li>
<li>不要再HTML中拉伸图片</li>
</ul>
</li>
</ul>
<h1 id="17-HTTP状态码及其含义"><a href="#17-HTTP状态码及其含义" class="headerlink" title="17.HTTP状态码及其含义"></a>17.HTTP状态码及其含义</h1><ul>
<li><code>1XX</code>：信息状态码<ul>
<li><code>100 Continue</code>：继续。一般在发送POST请求时，已发送了<code>http header</code>之后服务端将返回此信息，表示确认，之后发送具体参数信息。</li>
</ul>
</li>
<li><code>2XX</code>：成功状态码<ul>
<li><code>200 OK</code>：正常返回信息。</li>
<li><code>201 Created</code>：请求成功并且服务器创建了新的资源。</li>
<li><code>202 Accepted</code>：服务器已接收请求，但<strong>尚未处理。</strong></li>
</ul>
</li>
<li><code>3XX</code>：重定向<ul>
<li><code>301 Moved Permanently</code>：请求的网页已永久移动到新位置。</li>
<li><code>302 Found</code>：临时性重定向。</li>
<li><code>303 See Other</code>：临时性重定向，且总是使用GET请求新的URI（用于标识互联网资源名称的字符串）。</li>
<li><code>304 Not Modified</code>：自从上次请求后，请求的网页未修改过。</li>
</ul>
</li>
<li><code>4XX</code>：客户端错误<ul>
<li><code>400 Bad Request</code>：服务器无法理解请求的格式，客户端不应当尝试再次使用相同的内容发起请求。</li>
<li><code>401 Unauthorized</code>：请求未授权。</li>
<li><code>403 Forbidden</code>：禁止访问。</li>
<li><code>404 Not Found</code>：找不到任何与URI相匹配的资源。</li>
</ul>
</li>
<li><code>5XX</code>：服务器错误<ul>
<li><code>500 Internal Server Error</code>：最常见的客户端错误。</li>
<li><code>503 Service Unavailable</code>：服务端暂时无法处理请求（过载或维护）。</li>
</ul>
</li>
</ul>
<h1 id="18-语义化的理解"><a href="#18-语义化的理解" class="headerlink" title="18. 语义化的理解"></a>18. 语义化的理解</h1><h1 id="19-浏览器内核的理解"><a href="#19-浏览器内核的理解" class="headerlink" title="19. 浏览器内核的理解"></a>19. 浏览器内核的理解</h1><blockquote>
<p>主要分成两部分：渲染引擎（layout engineer或Rendering Engine）和JS引擎。</p>
</blockquote>
<ul>
<li>渲染引擎：<ul>
<li>负责取得网页的内容（HTML、XML、图像等等）、整理讯息（例如加入CSS等），以及计算网页的显示方式，输出至显示器或打印机。</li>
<li>浏览器的内核的不同对于网页的语法解释会有不同，所以渲染的效果也不相同。</li>
<li>所有网页浏览器、电子邮件客户端以及其他需要编辑、显示网络内容的应用程序都需要内核。</li>
</ul>
</li>
<li>JS引擎：解析和执行JavaScript来实现网页的动态效果。</li>
<li>最开始渲染引擎和JS引擎并没有区分的很明确，后来JS引擎越来越独立，内核就倾向于只指渲染引擎。</li>
<li>常见的浏览器内核：<ul>
<li>Trident内核：IE、MaxThon、TT、The World，360，搜狗浏览器等（又称MSHTML）。</li>
<li>Gecko内核：Netscape6及以上版本，FF/MozillaSuite/SeaMonkey等。</li>
<li>Presto内核：Opera7及以上（Opera内核原为Presto，现为Blink）。</li>
<li>Webkit内核：Safari、Chrome等（Chrome的Blink是webkit的分支）。</li>
</ul>
</li>
</ul>
<h1 id="20-HTML新特性？移除元素？"><a href="#20-HTML新特性？移除元素？" class="headerlink" title="20. HTML新特性？移除元素？"></a>20. HTML新特性？移除元素？</h1><ul>
<li>新特性：<ul>
<li>新增选择器：<code>document.querytSlector</code>、<code>document.querySelectorAll</code></li>
<li>拖拽释放：drag and drop API</li>
<li>媒体播放：video和audio</li>
<li>本地存储：localStorage和sessionStorage</li>
<li>离线应用：manifest</li>
<li>桌面通知：Notifications</li>
<li>语义化标签：article、footer、header、nav、section</li>
<li>增强表单空间：calendar、date、time、email、url、search</li>
<li>地理位置：Geolocation</li>
<li>多任务：webworker</li>
<li>全双工通信协议：websocket</li>
<li>历史管理：history</li>
<li>跨域资源共享（CORS）：Access-Control-Allow-Origin</li>
<li>页面可见性改变事件：visibilitychange</li>
<li>跨窗口通信：PostMessage</li>
<li>Form Data对象</li>
<li>绘画canvas</li>
</ul>
</li>
<li>移除元素：<ul>
<li>纯表现的元素：basefont、big、center、font、s、strike、tt、u</li>
<li>对可用性产生负面影响的元素：frame、frameset、noframes</li>
</ul>
</li>
<li>支持HTML5新标签：<ul>
<li><code>IE8/IE7/IE6</code>支持通过<code>document.createElement</code>方法产生的标签。</li>
<li>可以利用这一特性让这些浏览器支持<code>HTML5</code>新标签。</li>
<li>浏览器支持新标签后，还需要添加标签默认的样式。</li>
<li>html5shim：让 IE 识别并支持 HTML5 元素。</li>
</ul>
</li>
<li>如何区分HTML和HTML5：<code>DOCTYPE声明</code>、新增的结构元素、功能元素。</li>
</ul>
]]></content>
      <categories>
        <category>面试</category>
      </categories>
      <tags>
        <tag>面试</tag>
        <tag>HTML</tag>
      </tags>
  </entry>
  <entry>
    <title>JavaScript学习总结</title>
    <url>/2020/04/28/JavaScript%E5%AD%A6%E4%B9%A0%E6%80%BB%E7%BB%93/</url>
    <content><![CDATA[<p>先给自己挖个坑埋了。</p>
]]></content>
      <categories>
        <category>JavaScript高级程序设计</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
        <tag>笔记</tag>
      </tags>
  </entry>
  <entry>
    <title>面试 | JavaScript</title>
    <url>/2021/06/02/JavaScript/</url>
    <content><![CDATA[<h1 id="1-基本数据类型与类型判断"><a href="#1-基本数据类型与类型判断" class="headerlink" title="1. 基本数据类型与类型判断"></a>1. 基本数据类型与类型判断</h1><h2 id="1-1-基本数据类型"><a href="#1-1-基本数据类型" class="headerlink" title="1.1 基本数据类型"></a>1.1 基本数据类型</h2><blockquote>
<p>6种：boolean、number、string、undefined、null、symbol</p>
</blockquote>
<h2 id="1-2-null是Object吗"><a href="#1-2-null是Object吗" class="headerlink" title="1.2 null是Object吗"></a>1.2 null是Object吗</h2><blockquote>
<p>typeof null会输出object，这个是JS存在的悠久bug.</p>
<p>在JS的最初版本中使用的是32位系统，为了性能考虑使用低位存储变量的类型信息，000开头代表是对象，而null表示为全0，因此将它错误地判断为object。</p>
</blockquote>
<h2 id="1-3-typeof-amp-instanceof"><a href="#1-3-typeof-amp-instanceof" class="headerlink" title="1.3 typeof   &amp;  instanceof"></a>1.3 typeof   &amp;  instanceof</h2><ul>
<li><code>typeof</code>对于原始类型来说，除了null会显示成object，其余都可以显示正确的类型；<code>typeof</code>对于对象来说，除了函数都会显示object，<code>typeof</code>并不能准确判断变量到底是什么类型。</li>
<li>如果像判断一个对象的正确类型，可以考虑使用<code>instanceof</code>，因为内部机制是通过原型链来判断的。</li>
</ul>
<h1 id="1-4-如何判断一个对象类型是数组"><a href="#1-4-如何判断一个对象类型是数组" class="headerlink" title="1.4 如何判断一个对象类型是数组"></a>1.4 如何判断一个对象类型是数组</h1><ul>
<li>根据构造函数来判断<code>xxx instanceof Array</code></li>
<li>根据class属性判断<code>Object.prototype.toString.call(obj) === &#39;[object Array]&#39;</code></li>
<li>直接用<code>isArray</code>判断</li>
</ul>
<h1 id="2-类型转换"><a href="#2-类型转换" class="headerlink" title="2. 类型转换"></a>2. 类型转换</h1><blockquote>
<p>在JS中类型转换只有三种情况，分别是：</p>
<ul>
<li>转换为布尔值</li>
<li>转换为数字</li>
<li>转换为字符串</li>
</ul>
</blockquote>
<h1 id="2-1-转Boolean"><a href="#2-1-转Boolean" class="headerlink" title="2.1 转Boolean"></a>2.1 转Boolean</h1><blockquote>
<p>在条件判断时，除了undefined、null、false、NaN、””、0、-0，其他所有值都转为true，包括所有对象。</p>
</blockquote>
<h1 id="2-2-对象转原始类型"><a href="#2-2-对象转原始类型" class="headerlink" title="2.2 对象转原始类型   ???"></a>2.2 对象转原始类型   ???</h1><p>对象在转换类型的时候，会调用内置的[[ToPrimitive]]函数，对于该函数来说，算法逻辑一般来说如下：</p>
<ul>
<li>如果已经是原始类型，那就不需要转换了。</li>
<li>如果需要转字符串类型就调用<code>x.toString()</code>，转换为基础类型的话就返回转换的值。不是字符串类型的话就先调用<code>valueOf</code>，结果不是基础类型的话再调用<code>toString</code>。</li>
<li>调用<code>x.valueOf()</code>，如果转换为基础类型就返回转换的值。</li>
<li>如果都没有返回原始类型，就会报错。</li>
</ul>
<p>可重写<code>Symbol.toPrimitive</code>，该方法在转原始类型时调用优先级最高：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> a = &#123;</span><br><span class="line">  valueOf() &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">  &#125;,</span><br><span class="line">  toString() &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="string">'1'</span>;</span><br><span class="line">  &#125;,</span><br><span class="line">  [<span class="built_in">Symbol</span>.toPrimitive]() &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">2</span>;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="number">1</span> + a <span class="comment">// =&gt; 3</span></span><br></pre></td></tr></table></figure>

<h2 id="2-3-四则运算符"><a href="#2-3-四则运算符" class="headerlink" title="2.3 四则运算符"></a>2.3 四则运算符</h2><blockquote>
<p>加法运算符不同于其他几个运算法，特点如下：</p>
<ul>
<li>运算中其中一方为字符串，就会把另一方也转换为字符串。</li>
<li>如果一方不是字符串或者数字，那么会将它转换为数字或者字符串。</li>
</ul>
</blockquote>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="number">1</span> + <span class="string">'1'</span>  <span class="comment">//'11' 数字1转换为字符串</span></span><br><span class="line"><span class="literal">true</span> + <span class="literal">true</span> <span class="comment">//2  true转换为数字1</span></span><br><span class="line"><span class="number">4</span> + [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>]  <span class="comment">//'41, 2, 3' 数组转换为字符串'1, 2, 3'</span></span><br><span class="line"><span class="string">'a'</span> + + <span class="string">'b'</span>  <span class="comment">//aNaN   + 'b'等于NaN</span></span><br></pre></td></tr></table></figure>

<p>对于除了加法的运算符来说，只要其中一方是数字，那么另一方就会被转为数字：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="number">4</span> * <span class="string">'3'</span>     -&gt; <span class="number">12</span></span><br><span class="line"><span class="number">4</span> * []      -&gt; <span class="number">0</span></span><br><span class="line"><span class="number">4</span> * [<span class="number">1</span>, <span class="number">2</span>]  -&gt; <span class="literal">NaN</span></span><br></pre></td></tr></table></figure>

<h2 id="2-4-比较运算符"><a href="#2-4-比较运算符" class="headerlink" title="2.4 比较运算符"></a>2.4 比较运算符</h2><ul>
<li>如果是对象，就通过<code>toPrimitive</code>转换对象</li>
<li>如果是字符串，就通过<code>unicode</code>字符索引来比较</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> a = &#123;</span><br><span class="line">  valueOf() &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">  &#125;,</span><br><span class="line">  toString() &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="string">'1'</span>;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line">a &gt; <span class="number">-1</span> <span class="comment">//true</span></span><br></pre></td></tr></table></figure>

<h2 id="2-5-比较"><a href="#2-5-比较" class="headerlink" title="2.5 ==比较"></a>2.5 ==比较</h2><ul>
<li>Boolean，number，string三类比较的时候把值转换成数字，再看转换结果是否相等。</li>
<li>undefined参与比较，换成了NaN，所以其他三个类型给它比较都是false，<strong>跟null类型比较的时候是true</strong>。（NaN == NaN是假）</li>
<li>null参与比较，被当成对象，因为null没有valueOf和toString，除了undefined，谁跟他比较都是false。</li>
<li>值类型与对象比较：先调用对象valueOf，如果仍返回对象，调用toString，如果还是没有就是不等。</li>
</ul>
<h2 id="2-6-与"><a href="#2-6-与" class="headerlink" title="2.6 == 与 ==="></a>2.6 == 与 ===</h2><blockquote>
<p>对于<code>==</code>来说，如果对比方的类型<strong>不一样</strong>的话，就会进行类型转换。</p>
</blockquote>
<p>假如我们需要对比x和y是否相同，就会进行如下判断流程：</p>
<ul>
<li><p>首先判断两者类型是否相同，相同则比大小。</p>
</li>
<li><p>类型不相同的话，会进行类型转换。</p>
</li>
<li><p>先判断是否在对比null和undefined，是的话就会返回true。</p>
</li>
<li><p>判断两者类型是否是string和number，是的话就会将字符串转换为number。</p>
  <figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="number">1</span> == <span class="string">'1'</span>  =&gt; <span class="number">1</span> == <span class="number">1</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>判断其中一方是否为boolean，是的话就会把boolean转为number再进行判断。</p>
  <figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="string">'1'</span> == <span class="function"><span class="params">true</span> =&gt;</span> <span class="string">'1'</span> == <span class="number">1</span> =&gt; <span class="number">1</span> == <span class="number">1</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>判断其中一方是否为object且另一方为string、number或者symbol，是的话就会把object转为原始类型再进行判断。</p>
  <figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="string">'1'</span> == &#123;<span class="attr">name</span>: <span class="string">'xxx'</span>&#125;  =&gt;  <span class="string">'1'</span> == <span class="string">'[object object]'</span></span><br></pre></td></tr></table></figure>

</li>
</ul>
<blockquote>
<p><code>[] == ![]</code></p>
<p>首先执行的是<code>![]</code>，会得到true；接着<code>[] == flase</code>，返回true。</p>
</blockquote>
<blockquote>
<p><code>[] == []</code>，<code>{} == {}</code></p>
<p>类型一致，它们就是引用类型，地址是不一样的，所以是false。</p>
</blockquote>
<p><strong>对于<code>===</code>，判断两者类型和值是否相同。</strong></p>
<h1 id="3-面向对象"><a href="#3-面向对象" class="headerlink" title="3. 面向对象"></a>3. 面向对象</h1><blockquote>
<p>JS有两种属性值：数据属性和访问器属性。</p>
</blockquote>
<h2 id="3-1-数据属性"><a href="#3-1-数据属性" class="headerlink" title="3.1 数据属性"></a>3.1 数据属性</h2><p>包含一个数据值的位置。</p>
<h3 id="3-1-1-四个特性"><a href="#3-1-1-四个特性" class="headerlink" title="3.1.1 四个特性"></a>3.1.1 四个特性</h3><ul>
<li>Configurable：表示能否通过delete删除属性从而重新定义属性，默认为true。</li>
<li>Enumerable：表示能否通过for-in循环返回属性，默认为true。</li>
<li>Writable：表示能否修改属性的值，默认为true。</li>
<li>Value：包含这个属性的数据值，默认为undefined。</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">//使用字面量创建对象，前三个属性默认值为true，value为指定的值</span></span><br><span class="line"><span class="keyword">var</span> person = &#123;</span><br><span class="line">  name: <span class="string">'xxxx'</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="3-1-2-修改属性默认值"><a href="#3-1-2-修改属性默认值" class="headerlink" title="3.1.2 修改属性默认值"></a>3.1.2 修改属性默认值</h3><p><code>Object.defineProperty(obj, attr, {})</code></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> person = &#123;&#125;;<span class="built_in">Object</span>.defineProperty (person, <span class="string">'name'</span>, &#123;  <span class="attr">writable</span>: <span class="literal">false</span>,  <span class="attr">value</span>: <span class="string">'xxxx'</span>,  <span class="attr">configurable</span>: <span class="literal">false</span>;   <span class="comment">//一旦被设置为false，不可以更改&#125;)person.name = 'xx'console.log(person.name);  //xxx，严格模式下会报错delete person.name   //无效，严格模式下会报错</span></span><br></pre></td></tr></table></figure>

<h2 id="3-2-访问器属性"><a href="#3-2-访问器属性" class="headerlink" title="3.2 访问器属性"></a>3.2 访问器属性</h2><ul>
<li>四个特性<ul>
<li>Configurable：表示能否通过delete删除属性从而重新定义属性，默认为true 。</li>
<li>Enumerable：表示能否通过for-in循环返回属性，默认为true。</li>
<li>Writable：表示能否修改属性的值，默认为undefined。</li>
<li>Value：包含这个属性的数据值，默认为undefined。</li>
</ul>
</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> person = &#123;  <span class="attr">_age</span>: <span class="number">20</span>,   <span class="comment">//下划线写法表示只能通过对象方法访问的属性  state: 'young'&#125;Object.defineProperty (person, 'age', &#123;  get: function() &#123;    return this._age;  &#125;,  set: function() &#123;    if (newVal &gt; 50) &#123;      this._age = newVal;      this.state = 'old';    &#125; else &#123;      this._age = newVal;    &#125;  &#125;&#125;)</span></span><br></pre></td></tr></table></figure>

<p><strong>定义多个属性，使用<code>Object.defineProperties()</code></strong>。</p>
<h2 id="3-3-读取属性的特性"><a href="#3-3-读取属性的特性" class="headerlink" title="3.3 读取属性的特性"></a>3.3 读取属性的特性</h2><p><code>Object.getOwnPropertyDescriptor()</code></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> person = &#123;&#125;;<span class="built_in">Object</span>.defineProperties(person, &#123;  <span class="attr">_age</span>: &#123;    <span class="attr">values</span>: <span class="number">20</span>;  &#125;,  <span class="attr">state</span>: &#123;    <span class="attr">value</span>: <span class="string">'young'</span>;  &#125;,  <span class="attr">age</span>: &#123;    <span class="attr">get</span>: <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;      <span class="keyword">return</span> <span class="keyword">this</span>._age;    &#125;,    <span class="attr">set</span>: <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;      <span class="keyword">if</span> (newVal &gt; <span class="number">50</span>) &#123;        <span class="keyword">this</span>._age = newVal;        <span class="keyword">this</span>.state = <span class="string">'old'</span>;      &#125; <span class="keyword">else</span> &#123;        <span class="keyword">this</span>._age = newVal;      &#125;    &#125;  &#125;&#125;)<span class="keyword">var</span> descriptor = <span class="built_in">Object</span>.getOwnPropertyDescriptor(person, <span class="string">'_age'</span>);descriprot.value  <span class="comment">//20descriptor.configurabble //falsevar descriptor = Object.getOwnPropertyDescriptor(person, 'age');descriptor.value //undefineddescriptor.configurable //falsetypeof descriptor //function</span></span><br></pre></td></tr></table></figure>

<h1 id="4-深拷贝和浅拷贝"><a href="#4-深拷贝和浅拷贝" class="headerlink" title="4. 深拷贝和浅拷贝"></a>4. 深拷贝和浅拷贝</h1><p><strong>深拷贝和浅拷贝只针对Object和Array这样的引用数据类型的。</strong></p>
<h2 id="4-1-浅拷贝"><a href="#4-1-浅拷贝" class="headerlink" title="4.1 浅拷贝"></a>4.1 浅拷贝</h2><p>浅拷贝只复制指向某个对象的指针，而不复制对象本身，新旧对象还是共享同一块内。</p>
<ul>
<li>Object.assign</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> a = &#123;  <span class="attr">age</span>: <span class="number">1</span>;&#125;<span class="keyword">let</span> b = <span class="built_in">Object</span>.assign(&#123;&#125;, a);a.age = <span class="number">2</span>;<span class="built_in">console</span>.log(b.age); <span class="comment">//1</span></span><br></pre></td></tr></table></figure>

<ul>
<li>展开运算符…</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> a = &#123;  <span class="attr">age</span>: <span class="number">1</span>;&#125;<span class="keyword">let</span> b = &#123;...a&#125;;a.age = <span class="number">2</span>;<span class="built_in">console</span>.log(b.age) <span class="comment">//1</span></span><br></pre></td></tr></table></figure>

<h2 id="4-2-深拷贝"><a href="#4-2-深拷贝" class="headerlink" title="4.2 深拷贝"></a>4.2 深拷贝</h2><p>深拷贝会另外创造一个一模一样的对象，新对象跟原对象不共享内存，修改新对象不会改到原对象。</p>
<p><code>JSON.parse(JSON.stringify(object))</code></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> a = &#123;  <span class="attr">age</span>: <span class="number">1</span>,  <span class="attr">jobs</span>: &#123;    <span class="attr">first</span>: <span class="string">'FE'</span>;  &#125;;&#125;;<span class="keyword">let</span> b = <span class="built_in">JSON</span>.parse(<span class="built_in">JSON</span>.stringify(a));a.jobs.first = <span class="string">'native'</span>;<span class="built_in">console</span>.log(b.jobs.first) <span class="comment">//FE</span></span><br></pre></td></tr></table></figure>

<p>局限性：会忽略undefined、symbol；不能序列化函数；不能解决循环引用的对象。 </p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">deepClone</span> (<span class="params">obj</span>) </span>&#123;  <span class="function"><span class="keyword">function</span> <span class="title">isObject</span> (<span class="params">o</span>) </span>&#123;    <span class="keyword">return</span> (<span class="keyword">typeof</span> o === <span class="string">'object'</span> || <span class="keyword">typeof</span> o === <span class="string">'function'</span>) &amp;&amp; o !== <span class="literal">null</span>;  &#125;    <span class="keyword">if</span> (!isObject(obj)) &#123;    <span class="keyword">throw</span> <span class="keyword">new</span> <span class="built_in">Error</span>(<span class="string">'非对象'</span>)  &#125;    <span class="keyword">let</span> isArray = <span class="built_in">Array</span>.isArray(obj);  <span class="keyword">let</span> newObj = isArray ? [...obj]: &#123;...obj&#125;;  <span class="built_in">Reflect</span>.ownKeys(newObj).forEach(<span class="function"><span class="params">key</span> =&gt;</span> &#123;    newObj[key] = isObject(obj[key]) ? deepClone(obj[key]) : obj[key]  &#125;)  <span class="keyword">return</span> newObj;&#125;</span><br></pre></td></tr></table></figure>

<h1 id="5-this"><a href="#5-this" class="headerlink" title="5. this"></a>5. this</h1><h2 id="5-1-四种绑定规则"><a href="#5-1-四种绑定规则" class="headerlink" title="5.1 四种绑定规则"></a>5.1 四种绑定规则</h2><h1 id="this的4种绑定规则"><a href="#this的4种绑定规则" class="headerlink" title="this的4种绑定规则"></a><a href="https://www.cnblogs.com/xiaohuochai/p/5735901.html" target="_blank" rel="noopener">this的4种绑定规则</a></h1><ul>
<li><p>默认绑定</p>
<ul>
<li><strong>全局</strong>环境，默认绑定到<code>window</code></li>
<li>函数<strong>独立调用</strong>的时候，this默认绑定到<code>window</code></li>
<li>被嵌套的函数<strong>独立调用</strong>时，this默认绑定到<code>window</code></li>
<li><strong>立即执行</strong>的函数this是window</li>
<li><strong>闭包</strong>的this默认绑定到window</li>
</ul>
</li>
<li><p>隐式绑定：</p>
<blockquote>
<p>被直接对象所包含的函数调用时，也成为方法调用，this隐式绑定到该直接对象。</p>
</blockquote>
</li>
<li><p>显式绑定</p>
</li>
</ul>
<blockquote>
<p>通过call()、apply()、bind()方法把对象绑定到this上，叫做显式绑定。对于被调用的函数来说，叫做间接调用。</p>
</blockquote>
<ul>
<li>new绑定</li>
</ul>
<blockquote>
<p>如果函数或者方法调用之前带有关键字new，它就构成函数调用。对于this绑定来说，称为new绑定。</p>
</blockquote>
<h2 id="5-2-优先级"><a href="#5-2-优先级" class="headerlink" title="5.2 优先级"></a>5.2 优先级</h2><blockquote>
<p>new的方式优先级最高-&gt;bind这类函数-&gt;obj.foo()调用方式-&gt;foo调用方式。箭头函数的this一旦被绑定，就不会被任何方式所改变。</p>
</blockquote>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">foo</span>(<span class="params"></span>) </span>&#123;  <span class="built_in">console</span>.log(<span class="keyword">this</span>.a);&#125;<span class="keyword">var</span> a = <span class="number">1</span>;foo();<span class="keyword">const</span> obj = &#123;  <span class="attr">a</span>: <span class="number">2</span>,  <span class="attr">foo</span>: foo;&#125;obj.foo();<span class="keyword">const</span> c = <span class="keyword">new</span> foo();</span><br></pre></td></tr></table></figure>

<blockquote>
<ol>
<li>对于直接调用foo来说，不管foo函数被放在了什么地方，this一定是window。</li>
<li>对于obj.foo()来说，<strong>谁调用了函数，谁就是this</strong>，在这个场景下foo函数中的this就是obj对象。</li>
<li>对于new的方式来说，this被永远绑定在了c上面，不会被任何方式改变this。</li>
</ol>
</blockquote>
<p>箭头函数的this：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">a</span>(<span class="params"></span>) </span>&#123;  <span class="keyword">return</span> <span class="function"><span class="params">()</span> =&gt;</span> &#123;    <span class="keyword">return</span> <span class="function"><span class="params">()</span> =&gt;</span> &#123;      <span class="built_in">console</span>.log(<span class="keyword">this</span>);    &#125;;  &#125;;&#125;;</span><br></pre></td></tr></table></figure>

<blockquote>
<p>箭头函数其实是没有this的，<strong>箭头函数中的this只取决于包裹箭头函数的第一个普通函数的this。</strong>在这个例子中，因为包裹箭头函数的第一个普通函数是a，所以此时的this是window。另外<strong>对箭头函数使用bind这类函数是无效的。</strong></p>
</blockquote>
<p>对于bind这些改变上下文的API，这些函数的this取决于第一个参数，如果第一个参数为空，那么就是window。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> a = &#123;&#125;;<span class="keyword">let</span> fn = <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;  <span class="built_in">console</span>.log(<span class="keyword">this</span>);&#125;;fn.bind.bind(a)</span><br></pre></td></tr></table></figure>

<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">//fn.bind.bind(a) 等价于：let fn2 = function fn1() &#123;  return function() &#123;    return fn.apply();  &#125;.apply(a);&#125;fn2;</span></span><br></pre></td></tr></table></figure>

<p><strong>不管我们给函数bind几次，fn中的this永远由第一次的bind决定，所以结果永远是window。</strong></p>
<h1 id="6-闭包与作用域链"><a href="#6-闭包与作用域链" class="headerlink" title="6. 闭包与作用域链"></a>6. 闭包与作用域链</h1><h2 id="6-1-执行环境与作用域"><a href="#6-1-执行环境与作用域" class="headerlink" title="6.1 执行环境与作用域"></a>6.1 执行环境与作用域</h2><ul>
<li>执行环境：定义了变量或函数有权访问其他数据，决定了它们各自的行为。</li>
<li>全局执行环境是window对象，即全局变量和函数都是作为window对象的属性和方法创建的，每个函数都有自己的执行环境。</li>
<li>全局对象：每个执行环境都有一个与之相关的变量对象，执行环境中定义的所有变量和函数都会保存在这个对象中。</li>
<li>作用域链：代码执行过程中会常见变量对象的一个作用域链，作为域链的前端是当前执行环境的变量对象，末端是全局执行环境的变量对象。作用域链的作用是：保证执行环境里有权访问的变量和函数是有序的，作用域链的变量<strong>只能向上访问</strong>，变量访问到window对象即被终止，作用域链向下访问不行。</li>
</ul>
<h2 id="6-2-闭包"><a href="#6-2-闭包" class="headerlink" title="6.2 闭包"></a>6.2 闭包</h2><ul>
<li>定义：闭包就是能够读取其他函数内部变量的函数，就是利用了作用域链向上查找的特点。</li>
<li>查找规范：先在自己的变量范围中找，如果找不到就沿着作用域往上找。</li>
</ul>
<h1 id="7-原型链"><a href="#7-原型链" class="headerlink" title="7. 原型链"></a>7. 原型链</h1><h2 id="7-1-什么是原型链"><a href="#7-1-什么是原型链" class="headerlink" title="7.1 什么是原型链"></a>7.1 什么是原型链</h2><ul>
<li>在js中，每个函数都有prototype属性，这个属性值是一个对象，同时它带有constructor属性，指向这个构造函数。</li>
<li>通过new可以创建一个函数的实例，每个实例都有<code>_proto_</code>对象，指向构造函数的prototype对象。</li>
<li>当我们在一个对象或者方法获取某个值的时候，会先查找实例是否存在这个值，如果没有的话，就在原型里找查找。</li>
</ul>
<h2 id="7-2-怎么判断一个属性是对象上的属性还是其原型对象上的属性"><a href="#7-2-怎么判断一个属性是对象上的属性还是其原型对象上的属性" class="headerlink" title="7.2 怎么判断一个属性是对象上的属性还是其原型对象上的属性"></a>7.2 怎么判断一个属性是对象上的属性还是其原型对象上的属性</h2><blockquote>
<p>使用<code>hasOwnProperty()</code>返回true，说明是这个对象上的。</p>
<p>如果返回false，但是属性<code>in</code>这个对象返回了true，说明是原型对象上的属性。</p>
<p>如果都是false，那么不存在这个属性。</p>
</blockquote>
<h1 id="8-继承"><a href="#8-继承" class="headerlink" title="8. 继承"></a>8. 继承</h1><h2 id="8-1-继承的原理"><a href="#8-1-继承的原理" class="headerlink" title="8.1 继承的原理"></a>8.1 继承的原理</h2><blockquote>
<p>跟原型链有关。</p>
<p>每个函数都有个原型对象，这个对象用来存储<code>通过这个函数所创建的所有实例</code>的共有属性和方法。</p>
<p>在读取某个对象属性的时候，从实例开始，如果有实例就返回。通过实例只能访问原型对象里的值，但是不能修改，这就实现了继承。</p>
</blockquote>
<h2 id="8-2-组合继承"><a href="#8-2-组合继承" class="headerlink" title="8.2 组合继承"></a>8.2 组合继承</h2><blockquote>
<p>组合继承是最常用的继承方式。</p>
</blockquote>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Parent</span>(<span class="params">value</span>) </span>&#123;  <span class="keyword">this</span>.val = value;&#125;Parent.prototype.getValue = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;  <span class="built_in">console</span>.log(<span class="keyword">this</span>.val);&#125;<span class="function"><span class="keyword">function</span> <span class="title">Child</span>(<span class="params">value</span>) </span>&#123;  Parent.call(<span class="keyword">this</span>, value);&#125;Child.prototype = <span class="keyword">new</span> Parent();<span class="keyword">const</span> child = <span class="keyword">new</span> Child(<span class="number">1</span>);child.getValue()  <span class="comment">//1child instanceof Parent  //true</span></span><br></pre></td></tr></table></figure>

<blockquote>
<p>以上继承的方式核心是在子类的构造函数中通过<code>Parent.call(this)</code>继承父类的属性，然后改变<code>子类的原型为new Parent()</code>l来继承父类的函数。</p>
<p>这种继承方式优点在于<code>构造函数可以传承</code>，不会与父类引用属性共享，可以复用父类的函数，但是也存在一个缺点就是在继承父类函数的时候调用了父类构造函数，导致<code>子类的原型上多了不需要的父类属性，存在内存上的浪费。</code></p>
</blockquote>
<h2 id="8-3-寄生组合继承"><a href="#8-3-寄生组合继承" class="headerlink" title="8.3 寄生组合继承"></a>8.3 寄生组合继承</h2><blockquote>
<p>这种继承方式对组合继承进行了优化，组合继承缺点在于继承父类函数时调用了构造函数，我们只需要优化掉这点就行了。</p>
</blockquote>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Parent</span>(<span class="params">value</span>) </span>&#123;  <span class="keyword">this</span>.val = value;&#125;Parent.prototype.getValue = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;  <span class="built_in">console</span>.log(<span class="keyword">this</span>.val);&#125;<span class="function"><span class="keyword">function</span> <span class="title">Child</span>(<span class="params">value</span>) </span>&#123;  Parent.call(<span class="keyword">this</span>, value);&#125;Child.prototype = <span class="built_in">Object</span>.create(Parent.prototype, &#123;  <span class="attr">constructor</span>: &#123;    <span class="attr">value</span>: Child,    <span class="attr">enumerable</span>: <span class="literal">false</span>,    <span class="attr">writable</span>: <span class="literal">true</span>,    <span class="attr">configurable</span>: <span class="literal">true</span>  &#125;&#125;)<span class="keyword">const</span> child = <span class="keyword">new</span> Child(<span class="number">1</span>);child.getValue()  <span class="comment">//1child instanceof Parent  //true</span></span><br></pre></td></tr></table></figure>

<p>以上继承实现的核心就是讲父类的原型赋值给了子类，并且将构造函数设置为子类，这样既解决了无用的父类属性问题，华能正确地找到子类的构造函数。</p>
<h2 id="8-3-Class继承"><a href="#8-3-Class继承" class="headerlink" title="8.3 Class继承"></a>8.3 Class继承</h2><blockquote>
<p>以上两种继承方式都是通过原型去解决的，在ES6中，我们可以使用class去实现继承，并且实现起来很简单。</p>
</blockquote>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Parent</span> </span>&#123;  <span class="keyword">constructor</span>(value) &#123;    <span class="keyword">this</span>.val = value  &#125;  getValue() &#123;    <span class="built_in">console</span>.log(<span class="keyword">this</span>.val)  &#125;&#125;<span class="class"><span class="keyword">class</span> <span class="title">Child</span> <span class="keyword">extends</span> <span class="title">Parent</span> </span>&#123;  <span class="keyword">constructor</span>(value) &#123;    <span class="keyword">super</span>(value)  &#125;&#125;<span class="keyword">let</span> child = <span class="keyword">new</span> Child(<span class="number">1</span>)child.getValue() <span class="comment">// 1child instanceof Parent // true</span></span><br></pre></td></tr></table></figure>

<blockquote>
<p>class实现继承的核心在于使用extends表明继承自哪个父类，并且在子类构造函数中必须调用super，因为这段代码可以看场Parent.call(this, value)。</p>
</blockquote>
<h2 id="8-4-Class中的箭头函数和普通函数有何区别"><a href="#8-4-Class中的箭头函数和普通函数有何区别" class="headerlink" title="8.4 Class中的箭头函数和普通函数有何区别"></a>8.4 Class中的箭头函数和普通函数有何区别</h2><ul>
<li>箭头函数相当于绑定上了this，类似构造函数内的一个值。</li>
<li>箭头函数会绑定在实例对象上，普通函数最后会绑定在原型上。</li>
</ul>
<h1 id="9-DOM与事件"><a href="#9-DOM与事件" class="headerlink" title="9. DOM与事件"></a>9. DOM与事件</h1><h2 id="9-1-DOM如何创建元素"><a href="#9-1-DOM如何创建元素" class="headerlink" title="9.1 DOM如何创建元素"></a>9.1 DOM如何创建元素</h2><ul>
<li>创建元素：<code>createHTML()&#39;div</code></li>
<li>添加：<code>appendChild(element); insertBefore(insertdom.chosendom)</code></li>
</ul>
<h2 id="9-2-DOM获取元素的方式"><a href="#9-2-DOM获取元素的方式" class="headerlink" title="9.2 DOM获取元素的方式"></a>9.2 DOM获取元素的方式</h2><ul>
<li><p>通过元素类型获取</p>
  <figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="built_in">document</span>.getElementById() <span class="comment">//实际开发中较少使用，选择器中多用class，ud一般只用在顶级层存在，不能泰国依赖于iddocument.getElementsByTagName()  //标签名document.getElementsByClassName() //类名document.getElementByName()  //name属性值，一般不用document.querySelector()   //css选择符模式，返回与该模式匹配的第一个元素；如果没找到匹配的元素，则返回nulldocument.querySelectorAll()  //css选择符模式，返回与该模式匹配的所有元素，结果为一个类数组</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>根据关系树来选择</p>
<ul>
<li><code>parentNode</code>：获取所选节点的父节点，最顶层的节点为<code>#document</code></li>
<li><code>childNodes</code>：获取所选节点的子节点们</li>
<li><code>firstChild</code>：获取所选节点的第一个子节点</li>
<li><code>lastChild</code>：获取所选节点的最后一个子节点</li>
<li><code>nextSibling</code>：获取所选节点的后一个兄弟节点，列表中最后一个节点的nextSibling属性值为null</li>
<li><code>previousSibling</code>：获取所选节点的前一兄弟节点，列表中第一个节点的previousSibling属性值为null</li>
</ul>
</li>
<li><p>根据元素节点树来选择</p>
<ul>
<li><code>parentElement</code>：返回当前元素的父元素节点（IE9以下不兼容）</li>
<li><code>children</code>：返回当前元素的元素子节点</li>
<li><code>firstElementChild</code>：返回的是第一个元素子节点（IE9以下不兼容）</li>
<li><code>lastElementChild</code>：返回的是最后一个元素子节点（IE9以下不兼容）</li>
<li><code>nextElementSibling</code>：返回的是后一个兄弟元素节点（IE9以下不兼容）</li>
<li><code>previousElementSibling</code>：返回的是前一个兄弟元素节点（IE9以下不兼容）</li>
</ul>
</li>
</ul>
<h2 id="9-3-如何给元素添加事件"><a href="#9-3-如何给元素添加事件" class="headerlink" title="9.3 如何给元素添加事件"></a>9.3 如何给元素添加事件</h2><ul>
<li>在HTML元素中绑定事件<code>onclick = show()</code></li>
<li>获取DOM，<code>dom.onclick</code></li>
<li>addEventListener(click, shown, 1/0)  //第三个参数为布尔值</li>
</ul>
<h2 id="9-4-addEventListener三个参数，取值意思"><a href="#9-4-addEventListener三个参数，取值意思" class="headerlink" title="9.4 addEventListener三个参数，取值意思"></a>9.4 addEventListener三个参数，取值意思</h2><ul>
<li>第一个参数是事件类型。</li>
<li>第二个是事件发生的回调函数。</li>
<li>第三个是布尔值，默认为false，false是冒泡阶段执行，true是捕获阶段。</li>
</ul>
<h2 id="9-5-节点属性中children和childNodes有什么区别"><a href="#9-5-节点属性中children和childNodes有什么区别" class="headerlink" title="9.5 节点属性中children和childNodes有什么区别"></a>9.5 节点属性中children和childNodes有什么区别</h2><ul>
<li>childNodes返回的是节点的子节点结合（NodeLists），包括元素节点、文本节点还有属性节点。</li>
<li>children返回的只是节点的元素节点集合（HTMLCollection）</li>
</ul>
<h2 id="9-6-HTMLCollection和NodeList的比较"><a href="#9-6-HTMLCollection和NodeList的比较" class="headerlink" title="9.6 HTMLCollection和NodeList的比较"></a>9.6 HTMLCollection和NodeList的比较</h2><blockquote>
<p>querySelectorAll返回的NodeList是个浅拷贝的类数组对象，在节点数目上是非实时的，不过对节点属性进行修改，还是实时反映的。</p>
</blockquote>
<ul>
<li>共同点：<ul>
<li>都是类数组对象，都有length属性</li>
<li>都有共同的方法：item，可以通过<code>item(index)</code>获取返回结果的元素</li>
<li>都是实时变动的，document上面的更改会反映到相关的对象上</li>
</ul>
</li>
<li>区别：<ul>
<li>NodeList可以包含任何节点类型，HTMLCollection只包含元素节点。elementNode就是HTML中的标签。</li>
<li>HTMLCollection比NodeList多一个方法：<code>nameitem()</code>，除了可以用id，还可以用name来获取节点信息。</li>
</ul>
</li>
</ul>
<h2 id="9-7-事件冒泡与事件捕获"><a href="#9-7-事件冒泡与事件捕获" class="headerlink" title="9.7 事件冒泡与事件捕获"></a>9.7 事件冒泡与事件捕获</h2><ul>
<li>事件是先捕获，后冒泡</li>
<li>捕获阶段是外部元素先触发，然后触发内部元素</li>
<li>冒泡阶段是内部元素先触发，然后触发外部元素</li>
</ul>
<h2 id="9-8-如何阻止事件冒泡？如何取消默认事件？如何阻止事件的默认行为？"><a href="#9-8-如何阻止事件冒泡？如何取消默认事件？如何阻止事件的默认行为？" class="headerlink" title="9.8 如何阻止事件冒泡？如何取消默认事件？如何阻止事件的默认行为？"></a>9.8 如何阻止事件冒泡？如何取消默认事件？如何阻止事件的默认行为？</h2><ul>
<li>阻止事件冒泡<ul>
<li>W3C：stopPropagation()</li>
<li>IE：e.cancelBubble = true</li>
<li><code>window.event ? window.event.cancelBubble = true: e.stopPropagation()</code></li>
</ul>
</li>
<li>取消默认事件<ul>
<li>W3C：preventDefault()</li>
<li>IE：e.returnValue: false</li>
</ul>
</li>
<li>阻止默认行为：<ul>
<li>return false</li>
<li>原生的js会阻止默认行为，但会继续冒泡</li>
<li>jQuery会阻止默认行为，并停止冒泡</li>
</ul>
</li>
</ul>
<h2 id="9-10-获取DOM节点get系列和query系列哪种性能好？"><a href="#9-10-获取DOM节点get系列和query系列哪种性能好？" class="headerlink" title="9.10 获取DOM节点get系列和query系列哪种性能好？"></a>9.10 获取DOM节点get系列和query系列哪种性能好？</h2><ul>
<li>从性能上说get系列的性能都比query系列好，get系列里面各有差异，这些差异可以结合算法如何遍历搜索去理解，都解释的通。</li>
<li>getElementByTagName比querySelectorAll快的原因在于：getElementByTagName创建的过程不需要做任何操作，<strong>只需要返回一个指针</strong>即可。而<strong>querySelectorAll会循环遍历所有的结果，然后创建一个新的NodeList</strong>。</li>
<li>实际在用的过程中取决于要获取的是什么，再进行选择。</li>
</ul>
<h1 id="10-ES6"><a href="#10-ES6" class="headerlink" title="10. ES6"></a>10. ES6</h1><h2 id="10-1-变量提升与var、let及const"><a href="#10-1-变量提升与var、let及const" class="headerlink" title="10.1 变量提升与var、let及const"></a>10.1 变量提升与var、let及const</h2><p>变量提升：虽然变量还没有被声明，但是我们可以使用这个未被声明的变量，这种情况就叫做声明，并且提升的是声明。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="built_in">console</span>.log(a)  <span class="comment">//undefinedvar a = 1</span></span><br></pre></td></tr></table></figure>

<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a = <span class="number">10</span><span class="keyword">var</span> aconsole.log(a)  <span class="comment">//10//等价于var avar aa = 10console.log(a)</span></span><br></pre></td></tr></table></figure>

<p>不仅变量会提升，函数也会被提升。即使变量声明在函数之后，这也说明了函数会被提升，并且优先于变量提升。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">consol.log(a)  <span class="comment">// f a()&#123;&#125;function a() &#123;&#125;var a = 1</span></span><br></pre></td></tr></table></figure>

<p>使用var声明的变量会被提升到作用域的顶部。</p>
<p>在全局作用域下使用let和const声明变量，变量并不会被挂载到window上，这一点和var声明有了区别。</p>
<p>如果我们在声明a之前使用了a，就会出现报错。</p>
<p><code>Uncaught ReferenceError: Cannot access &#39;a&#39; before initialization</code></p>
<p>报错的原因是因为存在<code>暂时性死区</code>，我们不能在声明前就是用变量，这也是let和const优于var的一点。这里的提升和var的提升是有区别的，虽然变量在编译的环节中被告知在这块作用域中可以访问，但是访问是受限制的。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a = <span class="number">1</span><span class="keyword">let</span> b = <span class="number">1</span><span class="keyword">const</span> c = <span class="number">1</span><span class="built_in">console</span>.log(<span class="built_in">window</span>.b)  <span class="comment">//undefinedconsole.log(window.c)  //undefinedfunction test()&#123;  console.log(a)  let a&#125;test()   //报错</span></span><br></pre></td></tr></table></figure>

<p>提升存在的根本元素是<strong>为了解决函数间互相调用的情况。</strong></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">test1</span>(<span class="params"></span>) </span>&#123;  test2()&#125;<span class="function"><span class="keyword">function</span> <span class="title">test2</span>(<span class="params"></span>) </span>&#123;  test1()&#125;test1()<span class="comment">//如果不存在提升，那么将无法实现上述代码，因为不可能存在test1在test2前面，而test2又在test1前面。</span></span><br></pre></td></tr></table></figure>

<blockquote>
<p>函数提升优先于变量提升，函数提升会把整个函数挪到作用域顶部，变量提升只会把声明挪到作用域顶部。</p>
<p>var存在提升，我们能在声明之前使用。let和const因为暂时性死区的原因，不能在声明前使用。</p>
<p>var在全局作用域下声明变量会导致变量挂载在window上，其他两者不会。</p>
<p>let和const作用基本一致，但是后者声明的变量不能再次赋值。</p>
</blockquote>
<h2 id="10-2-let、const和var的区别"><a href="#10-2-let、const和var的区别" class="headerlink" title="10.2 let、const和var的区别"></a>10.2 let、const和var的区别</h2><ul>
<li><p>let</p>
<ul>
<li><p>作用域是块级作用域（之前只有函数作用域和全局作用域）</p>
</li>
<li><p>不存在变量声明提前（在let之前使用，会报错<code>is not undefined</code></p>
</li>
<li><p>不可以重复定义（var可以，不会报错，但是let会说<code>has already been declared</code></p>
</li>
<li><p>存在暂时性死区（var可以，不会报错，但是let会说<code>has already been declared</code></p>
<blockquote>
<p>在一个块级作用域中，变量唯一存在，一旦声明了一个，就属于这个块级作用域，不受外部的影响。</p>
<p>本质：只要已进去当前作用域，所要用的变量就已经存在了，但是不可以获取，只要等到声明变量那行开始才可以获取使用。</p>
<p>意义：标准化代码，将所有的变量声明放在最前面。</p>
</blockquote>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a = <span class="number">1</span><span class="keyword">if</span> (<span class="number">1</span>) &#123;  <span class="built_in">console</span>.log(a)  <span class="keyword">let</span> a = <span class="number">1</span>&#125;<span class="comment">//cannot access 'a' before initialization</span></span><br></pre></td></tr></table></figure>
</li>
</ul>
</li>
<li><p>const</p>
<ul>
<li>一般用来声明常量，不允许修改</li>
<li>只读属性，声明同时就要赋值</li>
<li>和let一样，都是块级作用域，存在暂时性死区，没有变量声明提前，不允许重复定义</li>
</ul>
</li>
<li><p>var</p>
<ul>
<li>var的作用域是函数作用域，在一个函数内部利用var声明一个变量，则这个变量只在函数内有效</li>
<li>存在变量声明提前，但是赋值并没有提前，提前访问会返回undefined</li>
</ul>
</li>
</ul>
<h2 id="10-3-let存储的位置在哪"><a href="#10-3-let存储的位置在哪" class="headerlink" title="10.3 let存储的位置在哪"></a>10.3 let存储的位置在哪</h2><ul>
<li>var定义的变量存储在作用中全局变量中（global，也就是window）。</li>
<li>let定义的变量存储在global同级的这个script的这个域中。</li>
</ul>
<h2 id="10-4-Promise"><a href="#10-4-Promise" class="headerlink" title="10.4 Promise"></a>10.4 Promise</h2><ul>
<li>Promise是一种用于解决异步问题的思路、方案，简单说就是个容器，里面存储的是某个未来会结束的结果。也是一个对象，可以获取异步操作的消息。</li>
<li>有三种状态，<code>pending resolved rejected</code>，状态变了就不能修改了。</li>
<li>在js里面，经常用异步的是Ajax，比如sucess：一个回调，error一个回调。但是如果一次请求需要多个接口的时候就产生了回调地狱，promise可以用then来处理，它可以在一个then里面再写一个promise对象。</li>
<li>promise.all：多任务并行，输出失败的那个。如果成功，则返回所有的执行结果。</li>
<li>promis.race：多任务执行，返回最先执行结束的任务结果。</li>
</ul>
<h2 id="10-5-Generator-yield"><a href="#10-5-Generator-yield" class="headerlink" title="10.5 Generator+yield"></a>10.5 Generator+yield</h2><ul>
<li>在ES6里面的新数据类型，像一个函数，可以返回多次。特点是函数有个*号。</li>
<li>调用的话就是不断调用next()，返回当前的value值done的状态。</li>
<li>return()直接忽略所有yield，返回最终的结果。</li>
<li>可以随心所欲的交出和恢复函数的执行权。</li>
</ul>
<h2 id="10-6-await-async"><a href="#10-6-await-async" class="headerlink" title="10.6 await+async"></a>10.6 await+async</h2><ul>
<li>async函数返回的是一个promise对象，await用于等待一个async函数的返回值。</li>
<li>优势在于处理then链。如果是多个Promise组成的then链，那么优势就比较明显了，可以用promise来解决多层回调的题，可以进一步优化它。</li>
</ul>
<h2 id="10-7-Proxy"><a href="#10-7-Proxy" class="headerlink" title="10.7 Proxy"></a>10.7 Proxy</h2><blockquote>
<p>Proxy是ES6中新增的功能，它可以用来自定义对象中的操作。</p>
</blockquote>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> p = <span class="keyword">new</span> <span class="built_in">Proxy</span>(target, handler)</span><br></pre></td></tr></table></figure>

<p>target代表需要添加代理的对象，handler用来自定义对象中的操作，比如可以用来自定义set或者get函数。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> onWatch = <span class="function">(<span class="params">obj, setBind, getLogger</span>) =&gt;</span> &#123;  <span class="keyword">let</span> handler = &#123;    <span class="keyword">get</span>(target, property, receiver) &#123;      getLogger(target, property)      <span class="keyword">return</span> <span class="built_in">Reflect</span>.get(target, property, receiver)    &#125;,    <span class="keyword">set</span>(target, property, value, receiver) &#123;      setBind(value, property)      <span class="keyword">return</span> <span class="built_in">Reflect</span>.set(target, property, value)    &#125;  &#125;  <span class="keyword">return</span> <span class="keyword">new</span> <span class="built_in">Proxy</span>(obj, handler)&#125;<span class="keyword">let</span> obj = &#123; <span class="attr">a</span>: <span class="number">1</span> &#125;<span class="keyword">let</span> p = onWatch(  obj,  (v, property) =&gt; &#123;    <span class="built_in">console</span>.log(<span class="string">`监听到属性<span class="subst">$&#123;property&#125;</span>改变为<span class="subst">$&#123;v&#125;</span>`</span>)  &#125;,  (target, property) =&gt; &#123;    <span class="built_in">console</span>.log(<span class="string">`'<span class="subst">$&#123;property&#125;</span>' = <span class="subst">$&#123;target[property]&#125;</span>`</span>)  &#125;)p.a = <span class="number">2</span> <span class="comment">// 监听到属性a改变p.a // 'a' = 2</span></span><br></pre></td></tr></table></figure>

<blockquote>
<p>通过自定义set和get函数的方式，在原本的逻辑中插入了我们的函数逻辑，实现了在对对象任何属性及逆行读写时发出通知。</p>
</blockquote>
<h1 id="11-EventLoop"><a href="#11-EventLoop" class="headerlink" title="11. EventLoop"></a>11. EventLoop</h1><p>浏览器执行是有一个执行栈的，当遇到异步的代码时，会被挂起并在需要执行的时候加到task队列里面。一旦执行栈为空，eventloop会从task队列里拿出需要执行的代码放入执行栈中执行。执行完毕后就弹出。</p>
<p>不同的任务源会分到微任务和宏任务里面。</p>
<p>顺序：</p>
<ul>
<li>首先执行同步代码，属于宏任务。</li>
<li>执行完所有宏任务之后，查询有没有异步代码需要执行，然后执行微任务。</li>
<li>执行微任务之后，如果有必要会重新渲染页面。</li>
<li>然后下一轮执行宏任务中异步代码，比如setTimeout的回调函数。</li>
</ul>
<blockquote>
<p>同步任务：所有任务都在主线程上执行，形成一个执行栈，执行完前一个，才能执行下一个。</p>
<p>异步任务：不进入主线程，主线程任务执行完通知“任务队列”。系统把异步任务放到任务队列之中，然后继续执行后续的任务。</p>
</blockquote>
<h1 id="12-JS的链式调用"><a href="#12-JS的链式调用" class="headerlink" title="12. JS的链式调用"></a>12. JS的链式调用</h1><blockquote>
<p>常见的一般有三种：一种是jQuery直接返回this的方式。一种是underscore的可选式方式，loadash惰性求值的方式。</p>
</blockquote>
<h2 id="12-1-jQuery直接返回this"><a href="#12-1-jQuery直接返回this" class="headerlink" title="12.1 jQuery直接返回this"></a>12.1 jQuery直接返回this</h2><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">A</span>(<span class="params">num</span>)</span>&#123;  <span class="keyword">this</span>.value=num||<span class="number">0</span>;&#125;<span class="comment">//运算并返回thisA.prototype.add=function(a)&#123;this.value+=a;return this;&#125;A.prototype.reduce=function(a)&#123; this.value+=a;return this;&#125;//为了显示正常，修改两个继承的方法A.prototype.valueOf=function()&#123;return this.value;&#125;A.prototype.toString=function()&#123;return this.value+'';&#125;//验证let a = new A(2);console.log(a.add(1).reduce(2));</span></span><br></pre></td></tr></table></figure>

<h2 id="12-2-underscore中的链式调用"><a href="#12-2-underscore中的链式调用" class="headerlink" title="12.2 underscore中的链式调用"></a>12.2 underscore中的链式调用</h2><blockquote>
<p>underscore规定了两种调用方式，<code>_.forEach(arr, fn); _.map(arr, fn)</code>和<code>_.chain(arr).forEach(fn).map(fn)</code></p>
</blockquote>
<p>第一步：先实现forEach，map的功能</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">//对数组进行处理var _ = &#123;&#125;_.foreach = function(array, fn)&#123;  array.forEach(function(v, i, array)&#123;    fn.apply(v, [v, i, array])  &#125;)&#125;_.map = function(array, fn)&#123;    return array.map(function(v, i, array)&#123;      return fn.apply(v, [v, i, array])  &#125;)&#125;</span></span><br></pre></td></tr></table></figure>

<p>用ES5数组原型的方法实现了，接下来去实现链式调用。</p>
<p>在第二种调用方式中，所有的操作都是在<code>_.chain(arr)</code>上调用的，所以<code>_.chian(arr)</code>应该是返回了一个对象，这个对象上有和<code>_</code>上相同的方法，只是传参由2个变成了1个。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> _=<span class="function"><span class="keyword">function</span>(<span class="params">array</span>)</span>&#123;    <span class="keyword">this</span>._value=<span class="built_in">Array</span>.prototype.slice.apply(array);&#125;;_.forEach=<span class="function"><span class="keyword">function</span>(<span class="params">array,fn</span>)</span>&#123;    array.forEach(<span class="function"><span class="keyword">function</span>(<span class="params">v,i,array</span>)</span>&#123;        fn.apply(v,[v,i,array]);    &#125;)&#125;;_.map=<span class="function"><span class="keyword">function</span>(<span class="params">array,fn</span>)</span>&#123;    <span class="keyword">return</span> array.map(<span class="function"><span class="keyword">function</span>(<span class="params">v,i,array</span>)</span>&#123;        <span class="keyword">return</span> fn.apply(v,[v,i,array]);    &#125;)&#125;;_.chain=<span class="function"><span class="keyword">function</span>(<span class="params">array</span>)</span>&#123;    <span class="keyword">return</span> <span class="keyword">new</span> _(array);&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span>(<span class="keyword">var</span> i <span class="keyword">in</span> _)&#123;<span class="comment">//遍历_    if(i !=='chain')&#123;        _.prototype[i]=(function(i)&#123;            //把其他方式经过处理赋值给_.prototype            return function()&#123;//通过闭包把i转化为局部变量                var args=Array.prototype.slice.apply(arguments);//取出fn                args.unshift(this._value);//把_value放入参数数组的第一位                if(i==='map')&#123;//map 需要修改_value值                    this._value=_[i].apply(this,args);                &#125;else&#123;                    //forEach不修改                    _[i].apply(this,args);                &#125;                return this;            &#125;        &#125;)(i)    &#125;&#125;</span></span><br></pre></td></tr></table></figure>

<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">_.prototype.value=<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;    <span class="keyword">return</span> <span class="keyword">this</span>._value;&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> a=[<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>];_.forEach(a,<span class="function"><span class="keyword">function</span>(<span class="params">v</span>)</span>&#123;<span class="built_in">console</span>.log(v)&#125;)<span class="comment">//1 2 3_.map(a,function(v)&#123;return ++v;&#125;)//[2,3,4]_.chain(a).map(function(v)&#123;return ++v;&#125;).foreach(function(v)&#123;console.log(v)&#125;).value();//2 3 4//[2,3,4]</span></span><br></pre></td></tr></table></figure>

<h2 id="12-3-loadash惰性链式调用"><a href="#12-3-loadash惰性链式调用" class="headerlink" title="12.3 loadash惰性链式调用"></a>12.3 loadash惰性链式调用</h2><ul>
<li>惰性调用：例如上面的<code>._chain(arr).forEach(fn).map(fn).value()</code>，当执行到chain(arr)的时候，返回了一个对象。执行到forEach的时候开始轮询，轮询完了之后，又返回到这个对象，执行到map的时候再次轮询，轮询完又返回这个对象，最后执行到value，返回对象中<code>_value</code>的值。</li>
<li>以上的每一步都是独立、一次进行的。</li>
<li>惰性调用就是，执行到forEach的时候不执行轮询操作，把操作<strong>塞进队列</strong>，执行到map的时候，再把map操作塞进队列。当某个特定的操作塞进队列的时候才会开始执行之前队列中所有的操作。比如value被调用时，才开始执行forEach、map和value。</li>
<li>惰性调用的好处：obj.job1().job2().job3()。如果需要连续执行100遍job1-3，那么就需要写100遍，或者用for把整个链条躲开100次。所以传统的链式操作缺点明显，函数链中都是job，不存在controller，如果用惰性调用的话，就是obj.loop(100).job1().job2().job3().end().done()。其中loop是开始循环，end是结束循环，done是执行。</li>
</ul>
<h2 id="12-4-如何调用？"><a href="#12-4-如何调用？" class="headerlink" title="12.4 如何调用？"></a>12.4 如何调用？</h2><ul>
<li>首先要有个构造函数，生成可供链式调用的对象，任何controller或者job的调用都是把它塞入任务队列，这个构造函数自然要有个队列属性。如果要实现loop，那么还有loop的总次数和当前loop的次数，需要记录一下loop开始的地方。</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Task</span>(<span class="params"></span>) </span>&#123;  <span class="keyword">this</span>.queue = []  <span class="keyword">this</span>.queueIndex = <span class="number">0</span>  <span class="keyword">this</span>.loopCount = <span class="number">0</span>  <span class="keyword">this</span>.loopIndex = <span class="number">0</span>  <span class="keyword">this</span>.loopStart = <span class="number">0</span>&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>开始实现controller和job。他们应该包含两种形态，一种是本来的业务逻辑，一种形态是不管业务逻辑做什么，把业务逻辑响应的代码统一塞进任务队列。</li>
<li>如果最终调用格式是<code>new Task().loop(100).job1().job2().end().done()</code>。那么方法链上的方法是包装器，这些方法应该放在Task.prototype上。</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> _task_proto = &#123;    <span class="attr">loop</span>: <span class="function"><span class="keyword">function</span>(<span class="params">num</span>) </span>&#123;      <span class="keyword">this</span>.loopStart = <span class="keyword">this</span>.queenIndex;      <span class="keyword">this</span>.loopCount = num;    &#125;,    <span class="attr">job</span>: <span class="function"><span class="keyword">function</span>(<span class="params">str</span>) </span>&#123;      <span class="built_in">console</span>.log(str);    &#125;,    <span class="attr">end</span>: <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;      <span class="keyword">this</span>.loopIndex++;      <span class="keyword">if</span>(<span class="keyword">this</span>.loopIndex &lt; <span class="keyword">this</span>.loopCount) &#123;        <span class="keyword">this</span>.queenIndex = <span class="keyword">this</span>.loopStart;      &#125;<span class="keyword">else</span> &#123;        <span class="keyword">this</span>.loopIndex = <span class="number">0</span>;      &#125;    &#125;,    <span class="attr">done</span>: <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;      <span class="built_in">console</span>.log(<span class="string">'done'</span>);    &#125;&#125;;Task.prototype.__proto__ = _task_proto;</span><br></pre></td></tr></table></figure>

<ul>
<li>在Task，prototype上定义一个新的方法，这个方法专门用来控制任务执行，因为任务队列是依次执行并且由索引定位，和迭代器有点像，定义这个方法叫next。</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">Task.prototype.next = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;    <span class="keyword">var</span> task = <span class="keyword">this</span>.queen[<span class="keyword">this</span>.queenIndex];  <span class="comment">//取出新的任务    task.fn.apply(this, task.args); //执行任务中指向的具体的实现方法，并传入之前保存的参数    if(task.name !== 'done') &#123;      this.queenIndex++;      this.next();  //如果没执行完，任务索引+1并再次调用next    &#125;else &#123;      this.queen = [];      this.queenIndex = 0;  //如果执行完了，清空任务队列，重置任务索引    &#125;&#125;</span></span><br></pre></td></tr></table></figure>

<ul>
<li>之后比哪里<code>_taskproto</code>在Task.prototype上生成包装器，并让每个包装器返回this以供链式调用（每种都需要这样做），并且添加next，以便让任务队列执行。</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span>(<span class="keyword">var</span> i <span class="keyword">in</span> _task_proto)&#123;    (<span class="function"><span class="keyword">function</span>(<span class="params">i</span>)</span>&#123;        <span class="keyword">var</span> raw=Task.prototype[i];        Task.prototype[i]=<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;            <span class="keyword">this</span>.queen.push(&#123;                <span class="attr">name</span>:i,                <span class="attr">fn</span>:raw,                <span class="attr">args</span>:<span class="built_in">arguments</span>            &#125;);<span class="comment">//保存实现的方法、名字和参数到任务队列            if(i==='done')&#123;                this.next();            &#125;            return this;        &#125;    &#125;)(i);&#125;</span></span><br></pre></td></tr></table></figure>

<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> t=<span class="keyword">new</span> Task();t.job(<span class="string">'P'</span>).loop(<span class="number">3</span>).job(<span class="string">'handsome'</span>).end().loop(<span class="number">3</span>).job(<span class="string">'cool'</span>).end().job(<span class="string">'!'</span>).done();<span class="comment">//P handsome handsome handsome cool cool cool ! done</span></span><br></pre></td></tr></table></figure>



<h1 id="13-数组"><a href="#13-数组" class="headerlink" title="13. 数组"></a>13. 数组</h1><h2 id="13-1-API有哪些"><a href="#13-1-API有哪些" class="headerlink" title="13.1 API有哪些"></a>13.1 API有哪些</h2><ul>
<li>直接修改原数组的：push、unshift、pop、splice、reverse、sort、fill(x, start, end)、copyWithin(tochangeindex, changestart, changeend)</li>
<li>返回新数组的：concat、slice</li>
<li>返回字符串：join</li>
<li>位置/是否在数组内：indexOf()、lastindexOf()、includes()、find()</li>
<li>遍历方法：<ul>
<li>forEach()，所有元素执行一次，返回undefined。</li>
<li>map()返回值为新数组；filter()返回通过元素的新数组。</li>
<li>every()所有都满足返回true。</li>
<li>some()只要有一个元素满足就返回true。</li>
<li>reduce(fn(pre, cur, index, arr), basevalue)累加器。</li>
<li>reduceRight()从右边从左边加。</li>
</ul>
</li>
<li>迭代器：arr.keys()返回索引迭代器；arr.values()返回迭代器、值；arr.entries()返回键值对。</li>
</ul>
<h2 id="13-2-数组的sort方法底层如何排序"><a href="#13-2-数组的sort方法底层如何排序" class="headerlink" title="13.2 数组的sort方法底层如何排序"></a>13.2 数组的sort方法底层如何排序</h2><ul>
<li>谷歌浏览器：大于22的数组快排；小于22的用插入排序。</li>
<li>火狐浏览器：归并排序。</li>
<li>webkit：用的c++的qsort()。</li>
</ul>
<h1 id="14-面试题"><a href="#14-面试题" class="headerlink" title="14. 面试题"></a>14. 面试题</h1><h2 id="14-1-实现a-b-c"><a href="#14-1-实现a-b-c" class="headerlink" title="14.1 实现a().b().c()"></a>14.1 实现a().b().c()</h2><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">a</span>(<span class="params"></span>)</span>&#123;  <span class="keyword">return</span> &#123;    <span class="attr">b</span>:<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;      <span class="keyword">return</span> &#123;        <span class="attr">c</span>:<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;          <span class="built_in">console</span>.log(<span class="string">'a'</span>);        &#125;      &#125;    &#125;  &#125;&#125;a().b().c();<span class="comment">//a</span></span><br></pre></td></tr></table></figure>

<h2 id="14-2-实现o-a-b-c"><a href="#14-2-实现o-a-b-c" class="headerlink" title="14.2 实现o.a().b().c()"></a>14.2 实现o.a().b().c()</h2><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> temp=&#123;  <span class="attr">a</span>:<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;<span class="built_in">console</span>.log(<span class="string">'a'</span>);<span class="keyword">return</span> <span class="keyword">this</span>;&#125;,  <span class="attr">b</span>:<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;<span class="built_in">console</span>.log(<span class="string">'b'</span>);<span class="keyword">return</span> <span class="keyword">this</span>;&#125;,  <span class="attr">c</span>:<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;<span class="built_in">console</span>.log(<span class="string">'c'</span>);<span class="keyword">return</span> <span class="keyword">this</span>;&#125;&#125;<span class="keyword">var</span> o=<span class="keyword">new</span> <span class="built_in">Object</span>();o.__proto__=temp;o.a().b().c();<span class="comment">//a b c</span></span><br></pre></td></tr></table></figure>

]]></content>
      <tags>
        <tag>面试</tag>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>Hello World</title>
    <url>/2020/04/26/hello-world/</url>
    <content><![CDATA[<p>Welcome to <a href="https://hexo.io/" target="_blank" rel="noopener">Hexo</a>! This is your very first post. Check <a href="https://hexo.io/docs/" target="_blank" rel="noopener">documentation</a> for more info. If you get any problems when using Hexo, you can find the answer in <a href="https://hexo.io/docs/troubleshooting.html" target="_blank" rel="noopener">troubleshooting</a> or you can ask me on <a href="https://github.com/hexojs/hexo/issues" target="_blank" rel="noopener">GitHub</a>.</p>
<h2 id="Quick-Start"><a href="#Quick-Start" class="headerlink" title="Quick Start"></a>Quick Start</h2><h3 id="Create-a-new-post"><a href="#Create-a-new-post" class="headerlink" title="Create a new post"></a>Create a new post</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo new <span class="string">"My New Post"</span></span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/writing.html" target="_blank" rel="noopener">Writing</a></p>
<h3 id="Run-server"><a href="#Run-server" class="headerlink" title="Run server"></a>Run server</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo server</span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/server.html" target="_blank" rel="noopener">Server</a></p>
<h3 id="Generate-static-files"><a href="#Generate-static-files" class="headerlink" title="Generate static files"></a>Generate static files</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo generate</span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/generating.html" target="_blank" rel="noopener">Generating</a></p>
<h3 id="Deploy-to-remote-sites"><a href="#Deploy-to-remote-sites" class="headerlink" title="Deploy to remote sites"></a>Deploy to remote sites</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo deploy</span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/one-command-deployment.html" target="_blank" rel="noopener">Deployment</a></p>
]]></content>
      <categories>
        <category>JavaScript</category>
      </categories>
      <tags>
        <tag>hello</tag>
      </tags>
  </entry>
  <entry>
    <title>jQuery中的DOM操作</title>
    <url>/2020/04/28/jQuery%E4%B8%AD%E7%9A%84DOM%E6%93%8D%E4%BD%9C/</url>
    <content><![CDATA[<h2 id="3-1-DOM操作的分类"><a href="#3-1-DOM操作的分类" class="headerlink" title="3.1 DOM操作的分类"></a>3.1 DOM操作的分类</h2><h3 id="1-DOM-Core"><a href="#1-DOM-Core" class="headerlink" title="1. DOM Core"></a>1. DOM Core</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">//获取表单对象</span></span><br><span class="line"><span class="built_in">document</span>.getElementsByTagName(<span class="string">"form"</span>);</span><br><span class="line"><span class="comment">//获取元素的src属性</span></span><br><span class="line">element.getAttribute(<span class="string">"src"</span>);</span><br></pre></td></tr></table></figure>

<h3 id="2-HTML-DOM"><a href="#2-HTML-DOM" class="headerlink" title="2. HTML DOM"></a>2. HTML DOM</h3><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="built_in">document</span>.forms;</span><br><span class="line">element.src;</span><br></pre></td></tr></table></figure>

<h3 id="3-CSS-DOM"><a href="#3-CSS-DOM" class="headerlink" title="3. CSS DOM"></a>3. CSS DOM</h3><figure class="highlight js"><table><tr><td class="code"><pre><span class="line">element.style.color = <span class="string">"red"</span>;</span><br></pre></td></tr></table></figure>

<h2 id="3-2-jQuery中的DOM操作"><a href="#3-2-jQuery中的DOM操作" class="headerlink" title="3.2 jQuery中的DOM操作"></a>3.2 jQuery中的DOM操作</h2><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;!DOCTYPE <span class="meta-keyword">html</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">html</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">head</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">meta</span> <span class="attr">charset</span>=<span class="string">"utf-8"</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">title</span>&gt;</span>第三章<span class="tag">&lt;/<span class="name">title</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">script</span> <span class="attr">type</span>=<span class="string">"text/javascript"</span> <span class="attr">src</span>=<span class="string">"http://ajax.microsoft.com/ajax/jquery</span></span></span><br><span class="line"><span class="tag"><span class="string">		/jquery-1.4.min.js"</span>&gt;</span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;/<span class="name">head</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">body</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">p</span> <span class="attr">title</span>=<span class="string">"选择你最喜欢的水果"</span>&gt;</span>你最喜欢的水果是？<span class="tag">&lt;/<span class="name">p</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">ul</span>&gt;</span></span><br><span class="line">			<span class="tag">&lt;<span class="name">li</span> <span class="attr">title</span>=<span class="string">"苹果"</span>&gt;</span>苹果<span class="tag">&lt;/<span class="name">li</span>&gt;</span></span><br><span class="line">			<span class="tag">&lt;<span class="name">li</span> <span class="attr">title</span>=<span class="string">"橘子"</span>&gt;</span>橘子<span class="tag">&lt;/<span class="name">li</span>&gt;</span></span><br><span class="line">			<span class="tag">&lt;<span class="name">li</span> <span class="attr">title</span>=<span class="string">"菠萝"</span>&gt;</span>菠萝<span class="tag">&lt;/<span class="name">li</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;/<span class="name">ul</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">html</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h3 id="3-2-1-查找节点"><a href="#3-2-1-查找节点" class="headerlink" title="3.2.1 查找节点"></a>3.2.1 查找节点</h3><h4 id="1-查找元素节点"><a href="#1-查找元素节点" class="headerlink" title="1. 查找元素节点"></a>1. 查找元素节点</h4><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> $li = $(<span class="string">"ul li:eq(1)"</span>);  <span class="comment">//获取&lt;ul&gt;里第2个&lt;li&gt;节点</span></span><br><span class="line"><span class="keyword">var</span> li_txt = $li.text();    <span class="comment">//获取第2个&lt;li&gt;元素节点的文本内容</span></span><br><span class="line">alert(li_txt);</span><br></pre></td></tr></table></figure>

<h4 id="2-查找属性节点"><a href="#2-查找属性节点" class="headerlink" title="2. 查找属性节点"></a>2. 查找属性节点</h4><p>attr()方法的参数可以是一个，也可以是两个。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> $para = (<span class="string">"p"</span>);                 <span class="comment">//获取&lt;p&gt;节点</span></span><br><span class="line"><span class="keyword">var</span> p_txt = $para.attr(<span class="string">"title"</span>);   <span class="comment">//获取&lt;p&gt;元素节点属性title</span></span><br><span class="line">alert(p_txt);</span><br></pre></td></tr></table></figure>

<h3 id="3-2-2-创建节点"><a href="#3-2-2-创建节点" class="headerlink" title="3.2.2 创建节点"></a>3.2.2 创建节点</h3><h4 id="1-创建元素节点"><a href="#1-创建元素节点" class="headerlink" title="1. 创建元素节点"></a>1. 创建元素节点</h4><p>（1）创建新元素；</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">$(html);</span><br></pre></td></tr></table></figure>

<p>（2）将元素插入文档中。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> $li_1 = $(<span class="string">"&lt;li title='apple'&gt;apple&lt;/li&gt;"</span>);  <span class="comment">//创建第1个&lt;li&gt;元素</span></span><br><span class="line"><span class="keyword">var</span> $li_2 = $(<span class="string">"&lt;li titlt='orange'&gt;orange&lt;/li&gt;"</span>);  <span class="comment">//创建第2个&lt;li&gt;元素</span></span><br><span class="line">$(<span class="string">"ul"</span>).append($li_1);            <span class="comment">//添加到&lt;ul&gt;节点中，使之能在网页中显示</span></span><br><span class="line">$(<span class="string">"ul"</span>).append($li_2);</span><br><span class="line">$(<span class="string">"ul"</span>).append($li_1).append($li_2);<span class="comment">//链式写法</span></span><br></pre></td></tr></table></figure>

<h4 id="2-创建文本节点"><a href="#2-创建文本节点" class="headerlink" title="2. 创建文本节点"></a>2. 创建文本节点</h4><p>创建文本节点就是在创建元素节点时直接把文本内容写出来，然后使用apped()等方法将它们添加到文档中就可以了。</p>
<h4 id="3-创建属性节点"><a href="#3-创建属性节点" class="headerlink" title="3. 创建属性节点"></a>3. 创建属性节点</h4><p>创建属性节点与创建文本节点类似，直接在创建元素节点时一起创建。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> $li_1 = $(<span class="string">"&lt;li title='apple'&gt;apple&lt;/li&gt;"</span>);</span><br></pre></td></tr></table></figure>

<h3 id="3-2-3-插入节点"><a href="#3-2-3-插入节点" class="headerlink" title="3.2.3 插入节点"></a>3.2.3 插入节点</h3><table>
<thead>
<tr>
<th>方法</th>
<th>描述</th>
<th>示例</th>
</tr>
</thead>
<tbody><tr>
<td>append()</td>
<td>向每个匹配的元素内部追加内容</td>
<td>HTML代码：<br /><code>&lt;p&gt;我想说：&lt;/p&gt;</code> <br />jQuery代码：<br /><code>$(&quot;p&quot;).append(&quot;b&gt;Hello&lt;/b&gt;&quot;);</code> <br />结果：<br /><code>&lt;p&gt;我想说：&lt;b&gt;Hello&lt;/b&gt;&lt;/p&gt;</code></td>
</tr>
<tr>
<td>appendTo()</td>
<td>将所有匹配的元素追加到指定的元素中。</td>
<td>HTML代码：<br /><code>&lt;p&gt;我想说：&lt;/p&gt;</code> <br />jQuery代码：<br /><code>$(&quot;&lt;b&gt;Hello&lt;/b&gt;&quot;).appendTo(&quot;p&quot;);</code> <br />结果：<br /><code>&lt;p&gt;我想说：&lt;b&gt;Hello&lt;/b&gt;&lt;/p&gt;</code></td>
</tr>
<tr>
<td>prepend()</td>
<td>向每个匹配的元素内部前置内容。</td>
<td>HTML代码：<br /><code>&lt;p&gt;我想说：&lt;/p&gt;</code> <br />jQuery代码：<br /><code>$(&quot;p&quot;).append(&quot;b&gt;Hello&lt;/b&gt;&quot;);</code> <br />结果：<br /><code>&lt;p&gt;&lt;b&gt;Hello&lt;/b&gt;我想说：&lt;/p&gt;</code></td>
</tr>
<tr>
<td>prependTo</td>
<td>将所有匹配的元素前置到指定的元素中。</td>
<td>HTML代码：<br /><code>&lt;p&gt;我想说：&lt;/p&gt;</code> <br />jQuery代码：<br /><code>$(&quot;&lt;b&gt;Hello&lt;/b&gt;&quot;).prependTo(&quot;p&quot;);</code> <br />结果：<br /><code>&lt;p&gt;&lt;b&gt;Hello&lt;/b&gt;我想说：&lt;/p&gt;</code></td>
</tr>
<tr>
<td>after</td>
<td>在每个匹配的元素之后插入内容。</td>
<td>HTML代码：<br /><code>&lt;p&gt;我想说：&lt;/p&gt;</code> <br />jQuery代码：<br /><code>$(&quot;p&quot;).after(&quot;b&gt;Hello&lt;/b&gt;&quot;);</code> <br />结果：<br /><code>&lt;p&gt;我想说：&lt;b&gt;Hello&lt;/b&gt;&lt;/p&gt;</code></td>
</tr>
<tr>
<td>insertAfter</td>
<td>将所有匹配的元素插入到指定元素的后面。</td>
<td>HTML代码：<br /><code>&lt;p&gt;我想说：&lt;/p&gt;</code> <br />jQuery代码：<br /><code>$(&quot;&lt;b&gt;Hello&lt;/b&gt;&quot;).insertAfter(&quot;p&quot;);</code> <br />结果：<br /><code>&lt;p&gt;我想说：&lt;b&gt;Hello&lt;/b&gt;&lt;/p&gt;</code></td>
</tr>
<tr>
<td>before</td>
<td>在每个匹配的元素之前插入内容。</td>
<td>HTML代码：<br /><code>&lt;p&gt;我想说：&lt;/p&gt;</code> <br />jQuery代码：<br /><code>$(&quot;p&quot;).before(&quot;b&gt;Hello&lt;/b&gt;&quot;);</code> <br />结果：<br /><code>&lt;p&gt;&lt;b&gt;Hello&lt;/b&gt;我想说：&lt;/p&gt;</code></td>
</tr>
<tr>
<td>insertBefore</td>
<td>将所有匹配的元素插入到指定的元素的前面。</td>
<td>HTML代码：<br /><code>&lt;p&gt;我想说：&lt;/p&gt;</code> <br />jQuery代码：<br /><code>$(&quot;&lt;b&gt;Hello&lt;/b&gt;&quot;).insertBefore(&quot;p&quot;);</code> <br />结果：<br /><code>&lt;p&gt;&lt;b&gt;Hello&lt;/b&gt;我想说：&lt;/p&gt;</code></td>
</tr>
</tbody></table>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> $li_1 = $(<span class="string">"&lt;li titlt='apple'&gt;apple&lt;/li&gt;"</span>);  <span class="comment">//创建第1个元素</span></span><br><span class="line"><span class="keyword">var</span> $li_2 = $(<span class="string">"&lt;li title='orange'&gt;orange&lt;/li&gt;"</span>);<span class="comment">//创建第2个元素</span></span><br><span class="line"><span class="keyword">var</span> $li_3 = $(<span class="string">"&lt;li title='banana'&gt;banana&lt;/li&gt;"</span>);<span class="comment">//创建第3个元素</span></span><br><span class="line"><span class="keyword">var</span> $parent = $(<span class="string">"ul"</span>);  <span class="comment">//获取&lt;ul&gt;节点，即&lt;li&gt;的父节点</span></span><br><span class="line"><span class="keyword">var</span> $two_li = $(<span class="string">"ul li:eq(1)"</span>);  <span class="comment">//获取&lt;ul&gt;节点中第2个&lt;li&gt;元素节点</span></span><br><span class="line">$parent.append($li_1);  <span class="comment">//append()方法将创建的元素添加到父元素的最后面</span></span><br><span class="line">$parent.prepend($li_2); <span class="comment">//prepend()方法将创建的元素添加到父元素的最前面</span></span><br><span class="line">$parent.insertAfter($two_li); <span class="comment">//insertAfter()方法将创建的元素添加到获取的元素之后</span></span><br></pre></td></tr></table></figure>

<p>对DOM元素进行移动：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> $one_li = $(<span class="string">"ul li:eq(1)"</span>);  <span class="comment">//获取&lt;ul&gt;节点中的第2个&lt;li&gt;元素节点</span></span><br><span class="line"><span class="keyword">var</span> $two_li = $(<span class="string">"ul li:eq(2)"</span>);  <span class="comment">//获取&lt;ul&gt;节点中的第3个&lt;li&gt;元素节点</span></span><br><span class="line">$two_li.insertBefore($one_li);   <span class="comment">//移动节点</span></span><br></pre></td></tr></table></figure>

<h3 id="3-2-4-删除节点"><a href="#3-2-4-删除节点" class="headerlink" title="3.2.4 删除节点"></a>3.2.4 删除节点</h3><p>jquery提供了两种删除节点的方法，即remove()和empty()。</p>
<h4 id="1-remove-方法"><a href="#1-remove-方法" class="headerlink" title="1. remove()方法"></a>1. remove()方法</h4><p>从DOM中删除所有匹配的元素，传入的参数用于根据jQuery表达式来筛选元素。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">$(<span class="string">"ul li:eq(1)"</span>).remove();  <span class="comment">//获取第2个元素&lt;li&gt;节点后，将它从网页中删除</span></span><br></pre></td></tr></table></figure>

<p>当某个节点用remove()方法删除后，该节点所包含的所有后代节点将同时被删除，返回值是一格指向已被删除的节点的引用，可以在以后再次使用这些元素。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> $li = $(<span class="string">"ul li:eq(1)"</span>).remove();  <span class="comment">//获取第2个&lt;li&gt;元素节点后，将它从网页中删除</span></span><br><span class="line">$li.appendTo(<span class="string">"ul"</span>);  <span class="comment">//把刚才删除的节点重新添加到&lt;ul&gt;元素里</span></span><br></pre></td></tr></table></figure>

<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">$(<span class="string">"ul li:eq(1)"</span>).appendTo(<span class="string">"ul"</span>);</span><br><span class="line"><span class="comment">//appendTo()方法可以用来移动元素</span></span><br><span class="line"><span class="comment">//移动元素时首先从文档上删除此元素，然后将该元素插入到文档中的指定节点</span></span><br></pre></td></tr></table></figure>

<p>通过remove()选择性删除元素：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">$(ul li:eq(<span class="number">1</span>)).remove(<span class="string">"li[title!=apple]"</span>);</span><br><span class="line"><span class="comment">//将&lt;li&gt;元素中属性title不等于"apple"的&lt;li&gt;元素删除</span></span><br></pre></td></tr></table></figure>

<h4 id="2-empty-方法"><a href="#2-empty-方法" class="headerlink" title="2. empty()方法"></a>2. empty()方法</h4><p>empty()方法能清空元素中的所有后代节点。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">$(<span class="string">"ul li:eq(1)"</span>).empty();  </span><br><span class="line"><span class="comment">//获取第2个元素节点后，清空此元素[里]的内容</span></span><br><span class="line"><span class="comment">//清空元素的内容，留下&lt;li&gt;标签默认的符号·</span></span><br></pre></td></tr></table></figure>

<h3 id="3-2-5-复制节点"><a href="#3-2-5-复制节点" class="headerlink" title="3.2.5 复制节点"></a>3.2.5 复制节点</h3><figure class="highlight js"><table><tr><td class="code"><pre><span class="line">$(<span class="string">"ul li"</span>).click(<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">    $(<span class="keyword">this</span>).clone().appendTo(<span class="string">"ul"</span>);</span><br><span class="line">&#125;)</span><br><span class="line"><span class="comment">//复制当前单击的节点，并将它追加到&lt;ul&gt;元素中</span></span><br></pre></td></tr></table></figure>

<p>复制节点后，被复制的新元素不具有任何行为，若需要新元素也具有复制功能，可在clone()方法中传递参数true，即复制元素的同时复制元素中所绑定的事件。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">$(<span class="keyword">this</span>).clone(<span class="literal">true</span>).appendTo(<span class="string">"body"</span>);</span><br></pre></td></tr></table></figure>

<h3 id="3-2-6-替换节点"><a href="#3-2-6-替换节点" class="headerlink" title="3.2.6 替换节点"></a>3.2.6 替换节点</h3><p><code>replaceWith()</code>：将所有匹配的元素都替换成指定的HTML或者DOM元素</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">$(<span class="string">"p"</span>).replaceWith(<span class="string">"&lt;strong&gt;---&lt;/strong&gt;"</span>);</span><br></pre></td></tr></table></figure>

<p><code>replaceAll()</code>：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">$(<span class="string">"&lt;strong&gt;---&lt;/strong&gt;"</span>).replaceAll(<span class="string">"p"</span>);</span><br></pre></td></tr></table></figure>

<p>Tip：<strong>若在替换之前，已经为元素绑定事件，替换后原先绑定的事件将会与被替换的元素一起消失，需要在新元素上重新绑定事件。</strong></p>
<h3 id="3-2-7-包裹节点"><a href="#3-2-7-包裹节点" class="headerlink" title="3.2.7 包裹节点"></a>3.2.7 包裹节点</h3><p>即将某个节点有其他标记包裹起来。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">$(<span class="string">"strong"</span>).wrap(<span class="string">"&lt;b&gt;&lt;/b&gt;"</span>);  <span class="comment">//用&lt;b&gt;标签把&lt;strong&gt;元素包裹起来</span></span><br></pre></td></tr></table></figure>

<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">b</span>&gt;</span><span class="tag">&lt;<span class="name">strong</span>&gt;</span><span class="tag">&lt;/<span class="name">strong</span>&gt;</span><span class="tag">&lt;/<span class="name">b</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h4 id="1-wrapAll-方法"><a href="#1-wrapAll-方法" class="headerlink" title="1. wrapAll()方法"></a>1. wrapAll()方法</h4><p>wrapAll()方法会将所有匹配的元素用一个元素来包裹，而wrap()方法是将所有的元素进行单独的包裹。</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">strong</span> <span class="attr">title</span>=<span class="string">"选择你最喜欢的水果"</span>&gt;</span>你最喜欢的水果是？<span class="tag">&lt;/<span class="name">strong</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">strong</span> <span class="attr">title</span>=<span class="string">"选择你最喜欢的水果"</span>&gt;</span>你最喜欢的水果是？<span class="tag">&lt;/<span class="name">strong</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">ul</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">li</span> <span class="attr">title</span>=<span class="string">"苹果"</span>&gt;</span>苹果<span class="tag">&lt;/<span class="name">li</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">li</span> <span class="attr">title</span>=<span class="string">"橘子"</span>&gt;</span>橘子<span class="tag">&lt;/<span class="name">li</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">li</span> <span class="attr">title</span>=<span class="string">"菠萝"</span>&gt;</span>菠萝<span class="tag">&lt;/<span class="name">li</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">ul</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>wrap()：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">$(<span class="string">"strong"</span>).wrap(<span class="string">"&lt;b&gt;&lt;/b&gt;"</span>);</span><br></pre></td></tr></table></figure>

<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">b</span>&gt;</span><span class="tag">&lt;<span class="name">strong</span> <span class="attr">title</span>=<span class="string">"选择你最喜欢的水果"</span>&gt;</span>你最喜欢的水果是？<span class="tag">&lt;/<span class="name">strong</span>&gt;</span><span class="tag">&lt;/<span class="name">b</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">b</span>&gt;</span><span class="tag">&lt;<span class="name">strong</span> <span class="attr">title</span>=<span class="string">"选择你最喜欢的水果"</span>&gt;</span>你最喜欢的水果是？<span class="tag">&lt;/<span class="name">strong</span>&gt;</span><span class="tag">&lt;/<span class="name">b</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>wrapAll()：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">$(<span class="string">"strong"</span>).wrapAll(<span class="string">"&lt;b&gt;&lt;/b&gt;"</span>);</span><br></pre></td></tr></table></figure>

<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">b</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">strong</span> <span class="attr">title</span>=<span class="string">"选择你最喜欢的水果"</span>&gt;</span>你最喜欢的水果是？<span class="tag">&lt;/<span class="name">strong</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">strong</span> <span class="attr">title</span>=<span class="string">"选择你最喜欢的水果"</span>&gt;</span>你最喜欢的水果是？<span class="tag">&lt;/<span class="name">strong</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">b</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h4 id="2-wrapInner-方法"><a href="#2-wrapInner-方法" class="headerlink" title="2. wrapInner()方法"></a>2. wrapInner()方法</h4><p>wrapInner()方法将每一个匹配的元素的子内容（包括文本节点）用其他结构化的标记包裹起来。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">$(<span class="string">"strong"</span>).wrapInner(<span class="string">"&lt;b&gt;&lt;/b&gt;"</span>);</span><br></pre></td></tr></table></figure>

<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">strong</span> <span class="attr">title</span>=<span class="string">"选择你最喜欢的水果"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">b</span>&gt;</span></span><br><span class="line">        你最喜欢的水果是？</span><br><span class="line">    <span class="tag">&lt;/<span class="name">b</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">strong</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h3 id="3-2-8-属性操作"><a href="#3-2-8-属性操作" class="headerlink" title="3.2.8 属性操作"></a>3.2.8 属性操作</h3><p>jquery中用attr()方法来获取和设hi元素属性，removeAttr()方法来删除元素属性。</p>
<h4 id="1-获取属性和设置属性"><a href="#1-获取属性和设置属性" class="headerlink" title="1. 获取属性和设置属性"></a>1. 获取属性和设置属性</h4><p>获取元素的属性，只需传递一格参数，即属性名称。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> $para = $(<span class="string">"p"</span>);              <span class="comment">//获取&lt;p&gt;节点</span></span><br><span class="line"><span class="keyword">var</span> p_txt = $para.attr(<span class="string">"title"</span>); <span class="comment">//获取&lt;p&gt;元素节点属性title</span></span><br></pre></td></tr></table></figure>

<p>设置元素的属性，需传递两个参数，即属性名称和对应的值。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">$(<span class="string">"p"</span>).attr(<span class="string">"title"</span>,<span class="string">"your title"</span>);  <span class="comment">//设置单个的属性值</span></span><br></pre></td></tr></table></figure>

<p>一次性设置多个属性：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">$(<span class="string">"p"</span>).attr(&#123;</span><br><span class="line">    <span class="string">"title"</span>:<span class="string">"your title"</span>,</span><br><span class="line">    <span class="string">"name"</span>:<span class="string">"test"</span></span><br><span class="line">&#125;);</span><br><span class="line"><span class="comment">//将一个“名/值”形式的对象设置为匹配元素的属性</span></span><br></pre></td></tr></table></figure>

<h4 id="2-删除属性"><a href="#2-删除属性" class="headerlink" title="2. 删除属性"></a>2. 删除属性</h4><figure class="highlight js"><table><tr><td class="code"><pre><span class="line">$(<span class="string">"p"</span>).removeAttr(<span class="string">"title"</span>);  <span class="comment">//删除&lt;p&gt;元素的属性title</span></span><br></pre></td></tr></table></figure>

<h3 id="3-2-9-样式操作"><a href="#3-2-9-样式操作" class="headerlink" title="3.2.9 样式操作"></a>3.2.9 样式操作</h3><h4 id="1-获取样式和设置样式"><a href="#1-获取样式和设置样式" class="headerlink" title="1. 获取样式和设置样式"></a>1. 获取样式和设置样式</h4><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">p</span> <span class="attr">class</span>=<span class="string">"myClass"</span> <span class="attr">title</span>=<span class="string">"选择你最喜欢的水果"</span>&gt;</span>你最喜欢的水果是？<span class="tag">&lt;/<span class="name">p</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>attr()方法：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> p_class = $(<span class="string">"p"</span>).attr(<span class="string">"class"</span>);  <span class="comment">//获取&lt;p&gt;元素的class</span></span><br><span class="line">$(p).attr(<span class="string">"class"</span>,<span class="string">"high"</span>);           <span class="comment">//设置&lt;p&gt;元素的class为“high”</span></span><br></pre></td></tr></table></figure>

<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">p</span> <span class="attr">class</span>=<span class="string">"high"</span> <span class="attr">title</span>=<span class="string">""</span>&gt;</span>--<span class="tag">&lt;/<span class="name">p</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h4 id="2-追加样式"><a href="#2-追加样式" class="headerlink" title="2. 追加样式"></a>2. 追加样式</h4><figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="comment">/*获取样式和设置样式*/</span></span><br><span class="line"><span class="selector-class">.high</span>&#123;</span><br><span class="line">    <span class="attribute">font-weight</span>: bold; <span class="comment">/*粗体*/</span></span><br><span class="line">    <span class="attribute">color</span>: red;        <span class="comment">/*红色字体*/</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">/*追加样式*/</span></span><br><span class="line"><span class="selector-class">.another</span>&#123;</span><br><span class="line">    <span class="attribute">font-style</span>: italic;  <span class="comment">/*斜体*/</span></span><br><span class="line">    <span class="attribute">color</span>: blue;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">$(<span class="string">"input:eq(2)"</span>).click(<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">    $(<span class="string">"p"</span>).addClass(<span class="string">"another"</span>);  <span class="comment">//给&lt;p&gt;元素追加“another”类</span></span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<h4 id="3-移除样式"><a href="#3-移除样式" class="headerlink" title="3. 移除样式"></a>3. 移除样式</h4><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">p</span> <span class="attr">class</span>=<span class="string">"high another"</span>&gt;</span>test<span class="tag">&lt;/<span class="name">p</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p><code>removeClass</code>：从匹配的元素中删除全部或者指定的class。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">$(<span class="string">"p"</span>).removeClass(<span class="string">"high"</span>);  <span class="comment">//移除&lt;p&gt;元素中值为“high”的class</span></span><br></pre></td></tr></table></figure>

<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">p</span> <span class="attr">class</span>=<span class="string">"another"</span>&gt;</span>test<span class="tag">&lt;/<span class="name">p</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>若要把两个class都删除，则需使用两次<code>removeClass()</code>方法：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">$(<span class="string">"p"</span>).removeClass(<span class="string">"high"</span>).removeClass(<span class="string">"another"</span>);</span><br></pre></td></tr></table></figure>

<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">$(<span class="string">"p"</span>).removeClass(<span class="string">"high another"</span>);</span><br><span class="line">$(<span class="string">"p"</span>).removeClass();  <span class="comment">//不带参数时，会将所有class的值全部删除</span></span><br></pre></td></tr></table></figure>

<h4 id="4-切换样式"><a href="#4-切换样式" class="headerlink" title="4. 切换样式"></a>4. 切换样式</h4><p><code>toggle()</code>：控制行为上的重复切换。交替执行代码3和代码4两个函数，若元素原来是显示的，则隐藏它，反之则显示它。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">$toggleBtn.toggle(<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">    <span class="comment">//显示元素   代码3</span></span><br><span class="line">&#125;，<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">    <span class="comment">//隐藏元素   代码4</span></span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>

<p><code>toggleClass</code>：控制样式上的重复切换。类名存在则删除它，类明明不存在则添加它。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">$(<span class="string">"p"</span>).toggleClass(<span class="string">"another"</span>);   <span class="comment">//重复切换类名“another”</span></span><br></pre></td></tr></table></figure>

<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">p</span> <span class="attr">class</span>=<span class="string">"myClass"</span>&gt;</span><span class="tag">&lt;/<span class="name">p</span>&gt;</span></span><br><span class="line">---&gt;//单击一次</span><br><span class="line"><span class="tag">&lt;<span class="name">p</span> <span class="attr">class</span>=<span class="string">"myClass another"</span>&gt;</span><span class="tag">&lt;/<span class="name">p</span>&gt;</span></span><br><span class="line">---&gt;//再次单击后，返回原来样式</span><br><span class="line"><span class="tag">&lt;<span class="name">p</span> <span class="attr">class</span>=<span class="string">"myClass"</span>&gt;</span><span class="tag">&lt;/<span class="name">p</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h4 id="5-判断是否含有某个样式"><a href="#5-判断是否含有某个样式" class="headerlink" title="5. 判断是否含有某个样式"></a>5. 判断是否含有某个样式</h4><p><code>hasClass()</code>：判断元素中是否含有某个class，有则返回true，否则返回false。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">$(<span class="string">"p"</span>).hasClass(<span class="string">"another"</span>);   <span class="comment">//判断是否含有“another”的class</span></span><br></pre></td></tr></table></figure>

<h3 id="3-2-10-设置和获取HTML、文本和值"><a href="#3-2-10-设置和获取HTML、文本和值" class="headerlink" title="3.2.10 设置和获取HTML、文本和值"></a>3.2.10 设置和获取HTML、文本和值</h3><h4 id="1-html-方法"><a href="#1-html-方法" class="headerlink" title="1. html()方法"></a>1. html()方法</h4><p>类似于JavaScript中的innerHTML属性，可以用来读取或者设置某个元素中的HTML内容。</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">p</span>&gt;</span><span class="tag">&lt;<span class="name">strong</span>&gt;</span>123456<span class="tag">&lt;/<span class="name">strong</span>&gt;</span><span class="tag">&lt;/<span class="name">p</span>&gt;</span></span><br></pre></td></tr></table></figure>

<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> p_html = $(<span class="string">"p"</span>).html();  <span class="comment">//获取&lt;p&gt;元素的HTML代码</span></span><br><span class="line">alert(p_html);               <span class="comment">//---&gt; &lt;p&gt;&lt;strong&gt;123456&lt;/strong&gt;&lt;/p&gt;</span></span><br></pre></td></tr></table></figure>

<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">$(<span class="string">"p"</span>).html(<span class="string">"&lt;strong&gt;123456&lt;strong&gt;"</span>); <span class="comment">//设置&lt;p&gt;元素的HTML代码</span></span><br></pre></td></tr></table></figure>

<h4 id="2-text-方法"><a href="#2-text-方法" class="headerlink" title="2. text()方法"></a>2. text()方法</h4><p>类似与JavaScript的innerText属性，可以用来读取或者设置某个元素中的文本内容。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> p_text = $(<span class="string">"p"</span>).text();   <span class="comment">//获取&lt;p&gt;元素的文本内容</span></span><br><span class="line">alert(p_text);                <span class="comment">//---&gt;123456</span></span><br></pre></td></tr></table></figure>

<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">$(<span class="string">"p"</span>).text(<span class="number">999</span>);   <span class="comment">//设置&lt;p&gt;元素的文本内容</span></span><br></pre></td></tr></table></figure>

<p>Tip：<strong>text()方法支持所有的浏览器，对HTML文档和XML文档都有效</strong></p>
<h4 id="3-val-方法"><a href="#3-val-方法" class="headerlink" title="3. val()方法"></a>3. val()方法</h4><p>类似于JavaScript中的value属性，可以用来设置和获取元素的值。</p>
<p>若元素是文本框，则返回元素的值，若为多选，则返回一个包含所有选择的值的数组。</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">"text"</span> <span class="attr">id</span>=<span class="string">"address"</span> <span class="attr">value</span>=<span class="string">"请输入邮箱地址"</span>/&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">"text"</span> <span class="attr">id</span>=<span class="string">"password"</span> <span class="attr">value</span>=<span class="string">"请输入邮箱密码"</span>/&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">"button"</span> <span class="attr">value</span>=<span class="string">"登录"</span>/&gt;</span></span><br></pre></td></tr></table></figure>

<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">//获取焦点时，若...则将地址框中的值清空</span></span><br><span class="line">$(<span class="string">"#address"</span>).focus(<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">    <span class="keyword">var</span> txt_value = $(<span class="keyword">this</span>).val();   <span class="comment">//获取地址文本框的值</span></span><br><span class="line">    <span class="keyword">if</span>(txt_value == <span class="string">"请输入邮箱地址"</span>&#123;</span><br><span class="line">       $(<span class="keyword">this</span>).val(<span class="string">""</span>);</span><br><span class="line">       &#125;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">//失去焦点时，若地址框的值为空，则将地址框的值设置为...</span></span><br><span class="line">$(<span class="string">"#address"</span>).blur(<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">    <span class="keyword">var</span> txt_value = $(<span class="keyword">this</span>).val();   <span class="comment">//获取地址文本框的值</span></span><br><span class="line">    <span class="keyword">if</span> (txt_value==<span class="string">""</span>)&#123;</span><br><span class="line">        $(<span class="keyword">this</span>).val(<span class="string">"请输入邮箱地址"</span>)；</span><br><span class="line">    &#125;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">$(<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">    <span class="comment">//地址框</span></span><br><span class="line">    <span class="comment">//获取焦点时，若...则将地址框中的值清空</span></span><br><span class="line">    $(<span class="string">"#address"</span>).focus(<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">        <span class="keyword">var</span> txt_value = $(<span class="keyword">this</span>).val();   <span class="comment">//获取地址文本框的值</span></span><br><span class="line">        <span class="keyword">if</span>(txt_value == <span class="string">"请输入邮箱地址"</span>)&#123;</span><br><span class="line">           $(<span class="keyword">this</span>).val(<span class="string">""</span>);</span><br><span class="line">    &#125;);</span><br><span class="line">    <span class="comment">//失去焦点时，若地址框的值为空，则将地址框的值设置为...</span></span><br><span class="line">    $(<span class="string">"#address"</span>).blur(<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">        <span class="keyword">var</span> txt_value = $(<span class="keyword">this</span>).val();   <span class="comment">//获取地址文本框的值</span></span><br><span class="line">        <span class="keyword">if</span> (txt_value==<span class="string">""</span>)&#123;</span><br><span class="line">            $(<span class="keyword">this</span>).val(<span class="string">"请输入邮箱地址"</span>)；</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">//密码框</span></span><br><span class="line">    <span class="comment">//获取焦点时，若...则将地址框中的值清空</span></span><br><span class="line">    $(<span class="string">"#password"</span>).focus(<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">        <span class="keyword">var</span> txt_value = $(<span class="keyword">this</span>).val();   <span class="comment">//获取密码文本框的值</span></span><br><span class="line">        <span class="keyword">if</span>(txt_value == <span class="string">"请输入邮箱密码"</span>)&#123;</span><br><span class="line">           $(<span class="keyword">this</span>).val(<span class="string">""</span>);</span><br><span class="line">    &#125;);</span><br><span class="line">    <span class="comment">//失去焦点时，若地址框的值为空，则将地址框的值设置为...</span></span><br><span class="line">    $(<span class="string">"#password"</span>).blur(<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">        <span class="keyword">var</span> txt_value = $(<span class="keyword">this</span>).val();   <span class="comment">//获取密码文本框的值</span></span><br><span class="line">        <span class="keyword">if</span> (txt_value==<span class="string">""</span>)&#123;</span><br><span class="line">            $(<span class="keyword">this</span>).val(<span class="string">"请输入邮箱密码"</span>)；</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;);</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>

<p><code>defaultValue</code>：该表单元素的初始值。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">$(<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">    <span class="comment">//地址框</span></span><br><span class="line">    <span class="comment">//获取焦点时，若...则将地址框中的值清空</span></span><br><span class="line">    $(<span class="string">"#address"</span>).focus(<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">        <span class="keyword">var</span> txt_value = $(<span class="keyword">this</span>).val();   <span class="comment">//获取地址文本框的值</span></span><br><span class="line">        <span class="keyword">if</span>(txt_value == <span class="keyword">this</span>.defaultValue)&#123;</span><br><span class="line">           $(<span class="keyword">this</span>).val(<span class="string">""</span>);</span><br><span class="line">    &#125;);</span><br><span class="line">    <span class="comment">//失去焦点时，若地址框的值为空，则将地址框的值设置为...</span></span><br><span class="line">    $(<span class="string">"#address"</span>).blur(<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">        <span class="keyword">var</span> txt_value = $(<span class="keyword">this</span>).val();   <span class="comment">//获取地址文本框的值</span></span><br><span class="line">        <span class="keyword">if</span> (txt_value==<span class="string">""</span>)&#123;</span><br><span class="line">            $(<span class="keyword">this</span>).val(<span class="string">"请输入邮箱地址"</span>)；</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">//密码框</span></span><br><span class="line">    <span class="comment">//获取焦点时，若...则将地址框中的值清空</span></span><br><span class="line">    $(<span class="string">"#password"</span>).focus(<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">        <span class="keyword">var</span> txt_value = $(<span class="keyword">this</span>).val();   <span class="comment">//获取密码文本框的值</span></span><br><span class="line">        <span class="keyword">if</span>(txt_value == <span class="keyword">this</span>.defaultValue)&#123;</span><br><span class="line">           $(<span class="keyword">this</span>).val(<span class="string">""</span>);</span><br><span class="line">    &#125;);</span><br><span class="line">    <span class="comment">//失去焦点时，若地址框的值为空，则将地址框的值设置为...</span></span><br><span class="line">    $(<span class="string">"#password"</span>).blur(<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">        <span class="keyword">var</span> txt_value = $(<span class="keyword">this</span>).val();   <span class="comment">//获取密码文本框的值</span></span><br><span class="line">        <span class="keyword">if</span> (txt_value==<span class="string">""</span>)&#123;</span><br><span class="line">            $(<span class="keyword">this</span>).val(<span class="keyword">this</span>.defaultValue)；</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;);</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>

<p>若使用<code>defaultValue</code>，<code>this</code>指向当前的文本框，则<code>this.defaultValue</code>就是当前文本框的默认值，可替换上面的”请输入…“。</p>
<p><code>val()</code>方法不仅能设置元素的值，同时也能获取元素的值，还能使select（下拉列表框、checkbox（多选框）和radio（单选框）相应的选项被选中。</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">select</span> <span class="attr">id</span>=<span class="string">"single"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">option</span>&gt;</span>选择1号<span class="tag">&lt;/<span class="name">option</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">option</span>&gt;</span>选择2号<span class="tag">&lt;/<span class="name">option</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">option</span>&gt;</span>选择3号<span class="tag">&lt;/<span class="name">option</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">select</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">select</span> <span class="attr">id</span>=<span class="string">"multiple"</span> <span class="attr">multiple</span>=<span class="string">"multiple"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">option</span>&gt;</span>选择1号<span class="tag">&lt;/<span class="name">option</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">option</span>&gt;</span>选择2号<span class="tag">&lt;/<span class="name">option</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">option</span>&gt;</span>选择3号<span class="tag">&lt;/<span class="name">option</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">option</span>&gt;</span>选择4号<span class="tag">&lt;/<span class="name">option</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">option</span> <span class="attr">selected</span>=<span class="string">"selected"</span>&gt;</span>5<span class="tag">&lt;/<span class="name">option</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">select</span>&gt;</span></span><br></pre></td></tr></table></figure>

<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">$(<span class="string">"#single"</span>).val(<span class="string">"选择2号"</span>);           <span class="comment">//选中下拉框的第二项</span></span><br><span class="line">$(<span class="string">"multiple"</span>).val([<span class="string">"选择2号"</span>,<span class="string">"选择3号"</span>]);   <span class="comment">//选中第二、第三项</span></span><br></pre></td></tr></table></figure>

<p><code>val()</code>方法是从最后一格选项往前读取，若选项的value或者text中任意一项符合就会被选中。</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">option</span> <span class="attr">value</span>=<span class="string">"选择2号"</span>&gt;</span>选择1号<span class="tag">&lt;/<span class="name">option</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">option</span> <span class="attr">value</span>=<span class="string">"选择1号"</span>&gt;</span>选择2号<span class="tag">&lt;/<span class="name">option</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>此时，无论使用<code>val(&quot;选择1号&quot;)</code>或是<code>val(&quot;选择2号&quot;)</code>，</p>
<p>都会选择后一个<code>&lt;option value=&quot;选择1号&quot;&gt;选择2号&lt;/option&gt;</code></p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">"checkbox"</span> <span class="attr">value</span>=<span class="string">"check1"</span>&gt;</span>多选1</span><br><span class="line"><span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">"checkbox"</span> <span class="attr">value</span>=<span class="string">"check2"</span>&gt;</span>多选2</span><br><span class="line"><span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">"checkbox"</span> <span class="attr">value</span>=<span class="string">"check3"</span>&gt;</span>多选3</span><br><span class="line"><span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">"checkbox"</span> <span class="attr">value</span>=<span class="string">"check4"</span>&gt;</span>多选4</span><br><span class="line"><span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">"radio"</span> <span class="attr">value</span>=<span class="string">"radio1"</span>&gt;</span>单选1</span><br><span class="line"><span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">"radio"</span> <span class="attr">value</span>=<span class="string">"radio2"</span>&gt;</span>单选2</span><br><span class="line"><span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">"radio"</span> <span class="attr">value</span>=<span class="string">"radio3"</span>&gt;</span>单选3</span><br></pre></td></tr></table></figure>

<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">$(<span class="string">":checkbox"</span>).val([<span class="string">"check2"</span>,<span class="string">"check3"</span>]);  <span class="comment">//选中多选2和多选3</span></span><br><span class="line">$(<span class="string">":radio"</span>).val([<span class="string">"radio"</span>]);               <span class="comment">//选中单选2</span></span><br></pre></td></tr></table></figure>

<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">$(<span class="string">"#single option:eq(1)"</span>).attr(<span class="string">"selected"</span>,<span class="string">"true"</span>);</span><br><span class="line">$(<span class="string">"[value=radio2]:radio"</span>).attr(<span class="string">"checked"</span>,<span class="string">"true"</span>);</span><br></pre></td></tr></table></figure>

<h3 id="3-2-11-遍历节点"><a href="#3-2-11-遍历节点" class="headerlink" title="3.2.11 遍历节点"></a>3.2.11 遍历节点</h3><h4 id="1-children-方法"><a href="#1-children-方法" class="headerlink" title="1. children()方法"></a>1. children()方法</h4><p>用于取得匹配元素的子元素集合。</p>
<p><img src="C:%5CUsers%5CAdministrator%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20200427201546510.png" alt="image-20200427201546510"></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> $body = $(<span class="string">"body"</span>).children();</span><br><span class="line"><span class="keyword">var</span> $p = $(<span class="string">"p"</span>).children();</span><br><span class="line"><span class="keyword">var</span> $ul = $(<span class="string">"ul"</span>).children();</span><br><span class="line">alert($body.length);  <span class="comment">//2个子元素</span></span><br><span class="line">alert($p.length);     <span class="comment">//0个子元素</span></span><br><span class="line">alert($ul.length);    <span class="comment">//3个子元素</span></span><br><span class="line"><span class="keyword">for</span> (<span class="keyword">var</span> i = <span class="number">0</span>, len = $ul.length; i&lt;len; i++)&#123;</span><br><span class="line">    alert($ul[i].innerHTML);  <span class="comment">//循环输入&lt;li&gt;元素的HTML内容</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>Tip：<strong>childre()方法只考虑子元素而不考虑任何后代元素</strong></p>
<h4 id="2-next-方法"><a href="#2-next-方法" class="headerlink" title="2. next()方法"></a>2. next()方法</h4><p>用于取得匹配元素<strong>后面</strong>紧邻的同辈元素。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> $p1 = $(<span class="string">"p"</span>).next();   <span class="comment">//取得紧邻&lt;p&gt;元素后的同辈元素</span></span><br></pre></td></tr></table></figure>

<h4 id="3-prev-方法"><a href="#3-prev-方法" class="headerlink" title="3. prev()方法"></a>3. prev()方法</h4><p>用于取得匹配元素<strong>前面</strong>紧邻的同辈元素。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> $ul = $(<span class="string">"ul"</span>).prev();   <span class="comment">//取得紧邻&lt;ul&gt;元素前的</span></span><br></pre></td></tr></table></figure>

<h4 id="4-siblings-方法"><a href="#4-siblings-方法" class="headerlink" title="4. siblings()方法"></a>4. siblings()方法</h4><p>用于取得匹配元素前后所有的同辈元素。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">$(<span class="string">"---"</span>).click(<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">    $(<span class="keyword">this</span>).addClass(<span class="string">"---"</span>)   </span><br><span class="line">        <span class="comment">//为当前元素增加---类</span></span><br><span class="line">        .children(<span class="string">"a"</span>).show().end()</span><br><span class="line">            <span class="comment">//将子节点的&lt;a&gt;元素显示出来并重新定位到上次操作的元素</span></span><br><span class="line">        .siblings().removeClass(<span class="string">"---"</span>)</span><br><span class="line">            <span class="comment">//获取元素的兄弟元素，并去掉它们的---类</span></span><br><span class="line">        .children(<span class="string">"a"</span>).hide();</span><br><span class="line">            <span class="comment">//将兄弟元素的&lt;a&gt;元素隐藏</span></span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>

<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> $p2 = $(<span class="string">"p"</span>).siblings();  <span class="comment">//取得紧邻&lt;p&gt;元素的同辈元素</span></span><br></pre></td></tr></table></figure>

<h4 id="5-closest"><a href="#5-closest" class="headerlink" title="5. closest()"></a>5. closest()</h4><p>取得最近的匹配元素。</p>
<p>首先检查当前元素是否匹配，若匹配则直接返回元素本身，不匹配则向上查找父元素，逐级向上知道找到匹配选择器的元素。如果什么都没找到则返回一格空的jQuery对象。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">$(<span class="built_in">document</span>).bind(<span class="string">"click"</span>,<span class="function"><span class="keyword">function</span>(<span class="params">e</span>)</span>&#123;</span><br><span class="line">    $(e.target).closest(<span class="string">"li"</span>).css(<span class="string">"color"</span>,<span class="string">"red"</span>);</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>

<h3 id="3-2-12-CSS-DOM操作"><a href="#3-2-12-CSS-DOM操作" class="headerlink" title="3.2.12 CSS-DOM操作"></a>3.2.12 CSS-DOM操作</h3><p>读取和设置style对象的各种属性。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">$(<span class="string">"p"</span>).css(<span class="string">"color"</span>);   <span class="comment">//获取&lt;p&gt;元素的样式颜色</span></span><br></pre></td></tr></table></figure>

<p>无论color属性是外部CSS导入，还是直接拼接在HTML元素里（内联），css()方法都可以获取到属性style里的其他属性的值。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">$(<span class="string">"p"</span>).css(<span class="string">"color"</span>,<span class="string">"red"</span>);  <span class="comment">//设置&lt;p&gt;元素的样式颜色为红色</span></span><br><span class="line">$(<span class="string">"p"</span>).css(&#123;</span><br><span class="line">    <span class="string">"fontSize"</span>: <span class="string">"30px"</span>,</span><br><span class="line">    <span class="string">"backgroundColor"</span>: <span class="string">"#888888"</span></span><br><span class="line">&#125;)  <span class="comment">//同时设置字体大小和背景色</span></span><br></pre></td></tr></table></figure>

<p>Tips:</p>
<ol>
<li>如果值是数字，将会被自动转化为像素值；</li>
<li>在css()方法中，如果属性中带有“-”符号，如果设置这些属性的值的时候不带引号，需使用驼峰式写法。建议都加上引号。</li>
</ol>
<p><strong>透明度设置，可直接使用opacity属性。</strong></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">$(<span class="string">"p"</span>).css(<span class="string">"opacity"</span>,<span class="string">"0.5"</span>);</span><br></pre></td></tr></table></figure>

<p><strong>获取某个元素的height属性：</strong></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">$(element).css(<span class="string">"height"</span>);  <span class="comment">//width</span></span><br><span class="line">$(element).height();       <span class="comment">//width</span></span><br></pre></td></tr></table></figure>

<p>Tips:</p>
<ol>
<li>在jQuery1.2版本以后的height()方法可以用来获取window和document的高度；</li>
<li>css()方法获取的高度值与样式的设置有关，可能会得到“auto”，或者是“10px”之类的字符串；</li>
<li>height()方法获取的高度值则是元素在页面中的实际高度，与样式的设置无关，并且不带单位。</li>
</ol>
<h4 id="1-offset-方法"><a href="#1-offset-方法" class="headerlink" title="1. offset()方法"></a>1. offset()方法</h4><p>获取元素在当前视窗的相对偏移，返回的对象包含两个属性，即top和left，只对可见元素有效。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> offset = $(<span class="string">"p"</span>).offset();  <span class="comment">//获取&lt;p&gt;元素的offset()</span></span><br><span class="line"><span class="keyword">var</span> left = offset.left;        <span class="comment">//获取左偏移</span></span><br><span class="line"><span class="keyword">var</span> top = offset.top;          <span class="comment">//获取上偏移</span></span><br></pre></td></tr></table></figure>

<h4 id="2-position-方法"><a href="#2-position-方法" class="headerlink" title="2. position()方法"></a>2. position()方法</h4><p>获取元素相对于最近的一个position样式属性设置为<strong>relative</strong>或者<strong>absolute</strong>的祖父节点的相对偏移。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> position = $(<span class="string">"p"</span>).position();</span><br><span class="line"><span class="keyword">var</span> left = position.left;</span><br><span class="line"><span class="keyword">var</span> top = position.top;</span><br></pre></td></tr></table></figure>

<h4 id="3-scrollTop-方法和scrollLeft-方法"><a href="#3-scrollTop-方法和scrollLeft-方法" class="headerlink" title="3. scrollTop()方法和scrollLeft()方法"></a>3. scrollTop()方法和scrollLeft()方法</h4><p>获取元素的滚动条距顶端的距离和距左侧的距离。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> $p = $(<span class="string">"p"</span>);</span><br><span class="line"><span class="keyword">var</span> scrollTop = $p.scrollTop();</span><br></pre></td></tr></table></figure>

<p>若给这两个方法指定一个参数，可以控制元素的滚动条滚动到指定位置。</p>
<h2 id="3-3-案例研究-超链接和图片提示效果"><a href="#3-3-案例研究-超链接和图片提示效果" class="headerlink" title="3.3 案例研究 | 超链接和图片提示效果"></a>3.3 案例研究 | 超链接和图片提示效果</h2><h3 id="1-超链接提示效果"><a href="#1-超链接提示效果" class="headerlink" title="1. 超链接提示效果"></a>1. 超链接提示效果</h3><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">body</span>&gt;</span></span><br><span class="line">   <span class="tag">&lt;<span class="name">p</span>&gt;</span><span class="tag">&lt;<span class="name">a</span> <span class="attr">href</span>=<span class="string">"#"</span> <span class="attr">class</span>=<span class="string">"tooltip"</span> <span class="attr">title</span>=<span class="string">"这是我的超链接1"</span>&gt;</span>提示1<span class="tag">&lt;/<span class="name">a</span>&gt;</span><span class="tag">&lt;/<span class="name">p</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">p</span>&gt;</span><span class="tag">&lt;<span class="name">a</span> <span class="attr">href</span>=<span class="string">"#"</span> <span class="attr">class</span>=<span class="string">"tooltip"</span> <span class="attr">title</span>=<span class="string">"这是我的超链接2"</span>&gt;</span>提示2<span class="tag">&lt;/<span class="name">a</span>&gt;</span><span class="tag">&lt;/<span class="name">p</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">p</span>&gt;</span><span class="tag">&lt;<span class="name">a</span> <span class="attr">href</span>=<span class="string">"#"</span> <span class="attr">title</span>=<span class="string">"这是自带提示1"</span>&gt;</span>自带提示1<span class="tag">&lt;/<span class="name">a</span>&gt;</span><span class="tag">&lt;/<span class="name">p</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">p</span>&gt;</span><span class="tag">&lt;<span class="name">a</span> <span class="attr">href</span>=<span class="string">"#"</span> <span class="attr">title</span>=<span class="string">"这是自带提示2"</span>&gt;</span>自带提示2<span class="tag">&lt;/<span class="name">a</span>&gt;</span><span class="tag">&lt;/<span class="name">p</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br></pre></td></tr></table></figure>

<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">$(<span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="keyword">var</span> x = <span class="number">5</span>;</span><br><span class="line">    <span class="keyword">var</span> y = <span class="number">5</span>;</span><br><span class="line">    $(<span class="string">"a.tooltip"</span>).mouseover(<span class="function"><span class="keyword">function</span>(<span class="params">e</span>) </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.myTitle = <span class="keyword">this</span>.title;</span><br><span class="line">        <span class="keyword">this</span>.title = <span class="string">""</span>;</span><br><span class="line">        <span class="keyword">var</span> tooltip = <span class="string">"&lt;div id='tooltip'&gt;"</span> + <span class="keyword">this</span>.myTitle + <span class="string">"&lt;/div&gt;"</span>; <span class="comment">//创建&lt;&lt;div&gt;元素</span></span><br><span class="line">        $(<span class="string">"body"</span>).append(tooltip);  <span class="comment">//追加至文档中</span></span><br><span class="line">        $(<span class="string">"#tooltip"</span>).css(&#123;</span><br><span class="line">            <span class="string">"position"</span>: <span class="string">"absolute"</span>,</span><br><span class="line">            <span class="string">"top"</span>: (e.pageY + y) + <span class="string">"px"</span>,</span><br><span class="line">            <span class="string">"left"</span>: (e.pageX + x) + <span class="string">"px"</span></span><br><span class="line">        &#125;).show(<span class="string">"fast"</span>);  <span class="comment">//设置x和y坐标</span></span><br><span class="line">    &#125;).mouseout(<span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.title = <span class="keyword">this</span>.myTitle;</span><br><span class="line">        $(<span class="string">"#tooltip"</span>).remove();   <span class="comment">//移除</span></span><br><span class="line">    &#125;).mousemove(<span class="function"><span class="keyword">function</span>(<span class="params">e</span>)</span>&#123;</span><br><span class="line">        $(<span class="string">"#tooltip"</span>)</span><br><span class="line">            .css(&#123;</span><br><span class="line">            <span class="string">"top"</span>:(e.pagey + y) +<span class="string">"px"</span>,</span><br><span class="line">            <span class="string">"left"</span>:(e.pageX + x) + <span class="string">"px"</span></span><br><span class="line">        &#125;);</span><br><span class="line">    &#125;);</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>

<h3 id="2-图片提示效果"><a href="#2-图片提示效果" class="headerlink" title="2. 图片提示效果"></a>2. 图片提示效果</h3><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">ul</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">li</span>&gt;</span><span class="tag">&lt;<span class="name">a</span> <span class="attr">href</span>=<span class="string">""</span> <span class="attr">class</span>=<span class="string">"tooltip"</span> <span class="attr">title</span>=<span class="string">"黑色"</span>&gt;</span><span class="tag">&lt;<span class="name">img</span> <span class="attr">src</span>=<span class="string">""</span> <span class="attr">alt</span>=<span class="string">"黑色"</span>&gt;</span><span class="tag">&lt;/<span class="name">a</span>&gt;</span><span class="tag">&lt;/<span class="name">li</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">li</span>&gt;</span><span class="tag">&lt;<span class="name">a</span> <span class="attr">href</span>=<span class="string">""</span> <span class="attr">class</span>=<span class="string">"tooltip"</span> <span class="attr">title</span>=<span class="string">"粉色"</span>&gt;</span><span class="tag">&lt;<span class="name">img</span> <span class="attr">src</span>=<span class="string">""</span> <span class="attr">alt</span>=<span class="string">"黑色"</span>&gt;</span><span class="tag">&lt;/<span class="name">a</span>&gt;</span><span class="tag">&lt;/<span class="name">li</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">ul</span>&gt;</span></span><br></pre></td></tr></table></figure>

<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">$(<span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="keyword">var</span> x = <span class="number">10</span>;</span><br><span class="line">    <span class="keyword">var</span> y = <span class="number">20</span>;</span><br><span class="line">    $(<span class="string">"a.tooltip"</span>).mouseover(<span class="function"><span class="keyword">function</span>(<span class="params">e</span>) </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.myTitle = <span class="keyword">this</span>.title;</span><br><span class="line">        <span class="keyword">this</span>.title = <span class="string">""</span>;</span><br><span class="line">        <span class="keyword">var</span> imgTitle = <span class="keyword">this</span>.myTitle ? <span class="string">"&lt;br&gt;"</span> + <span class="keyword">this</span>.myTitle : <span class="string">""</span>;</span><br><span class="line">        <span class="keyword">var</span> tooltip = <span class="string">"&lt;div id='tooltip'&gt;&lt;img src='"</span> + <span class="keyword">this</span>.href + <span class="string">"' alt='预览图'/&gt;"</span> +imgTitle + <span class="string">"&lt;/div&gt;"</span>;   <span class="comment">//创建&lt;div&gt;元素，插入预览图</span></span><br><span class="line">        $(<span class="string">"body"</span>).append(tooltip);  <span class="comment">//追加到文档中</span></span><br><span class="line">        $(<span class="string">"#tooltip"</span>)</span><br><span class="line">            .css(&#123;</span><br><span class="line">            <span class="string">"top"</span>: (e.pageY + y) + <span class="string">"px"</span>,</span><br><span class="line">            <span class="string">"left"</span>: (e.pageX + x) + <span class="string">"px"</span>,  <span class="comment">//设置坐标</span></span><br><span class="line">        &#125;).show(<span class="string">"fast"</span>);</span><br><span class="line">    &#125;).mouseout(<span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.title = <span class="keyword">this</span>.myTitle;</span><br><span class="line">        $(<span class="string">"#tooltip"</span>).remove();   <span class="comment">//移除&lt;div&gt;</span></span><br><span class="line">    &#125;).mousemove(<span class="function"><span class="keyword">function</span>(<span class="params">e</span>) </span>&#123;</span><br><span class="line">        $(<span class="string">"#tooltip"</span>)</span><br><span class="line">            .css(&#123;</span><br><span class="line">            <span class="string">"top"</span>: (e.pageY + y) + <span class="string">"px"</span>,</span><br><span class="line">            <span class="string">"left"</span>: (e.pageX + x) + <span class="string">"px"</span>,</span><br><span class="line">        &#125;);</span><br><span class="line">    &#125;);</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>锋利的jQuery</category>
      </categories>
      <tags>
        <tag>笔记</tag>
        <tag>jQuery</tag>
      </tags>
  </entry>
  <entry>
    <title>面试 | 性能优化</title>
    <url>/2021/06/23/%E6%80%A7%E8%83%BD%E4%BC%98%E5%8C%96/</url>
    <content><![CDATA[<h1 id="1-加载相关"><a href="#1-加载相关" class="headerlink" title="1. 加载相关"></a>1. 加载相关</h1><h2 id="1-1-为什么要强调CSS要放在header里，js放在尾部？"><a href="#1-1-为什么要强调CSS要放在header里，js放在尾部？" class="headerlink" title="1.1 为什么要强调CSS要放在header里，js放在尾部？"></a>1.1 为什么要强调CSS要放在header里，js放在尾部？</h2><blockquote>
<p>DOMContentLoaded和load：</p>
<ul>
<li>DOMContentLoaded事件触发时，仅当DOM加载完成，不包括样式表、图片…</li>
<li>load事件触发时，页面上所有的DOM，样式表，脚本，图片都已加载完成。</li>
</ul>
</blockquote>
<ul>
<li>构建Render树需要DOM和CSSOM，所以HTML和CSS都会阻碍渲染，所以需要CSS尽早加载，以缩短首次渲染的时间。</li>
<li>此外，由于CSS不会阻塞文档的解析，但是会阻塞文档渲染，把CSS放在头部可以先生成CSSOM树，后续渲染DOM的时候，可以一次性构建Render树，只需要渲染一次。如果把CSS放在后面，会先解析一次DOM，加载CSS之后，会重新渲染之前的DOM，需要两次渲染。</li>
<li>阻塞浏览器的解析，也就是说发现一个外链脚本时，需等待脚本下载完成并执行后才会继续解析HTML。</li>
<li>普通的脚本会阻塞浏览器解析，加上defer或async属性，脚本就变成异步，可等到解析完毕再执行。<ul>
<li>async异步执行，异步下载完毕后就会执行，不确保执行顺序，一定在onload前，但不确定在DOMContentLoaded事件的前后。</li>
<li>defer延迟执行，相当于放在body最后，理论上在DOMContentLoaded事件前。</li>
<li>执行js代码过长会卡住渲染，对于需要很多时间计算的代码可以考虑用Web Worker，它可以让我们另开一个线程执行脚本，不影响渲染。</li>
</ul>
</li>
</ul>
<h2 id="1-2-Onload-amp-DOMContentLoaded-amp-domready"><a href="#1-2-Onload-amp-DOMContentLoaded-amp-domready" class="headerlink" title="1.2 Onload&amp;DOMContentLoaded&amp;domready"></a>1.2 Onload&amp;DOMContentLoaded&amp;domready</h2><ul>
<li>DOMContentLoaded事件触发时，仅当<strong>DOM加载完成</strong>，不包括样式表，图片。</li>
<li>load事件触发时，页面上所有的DOM，样式表，脚本，图片都已加载完。</li>
<li>domready事件在<strong>DOM加载后、资源加载之前</strong>被触发，在本地浏览器的DOMContentLoaded事件的形式被调用。</li>
</ul>
<h1 id="2-白屏、首屏"><a href="#2-白屏、首屏" class="headerlink" title="2. 白屏、首屏"></a>2. 白屏、首屏</h1><h2 id="2-1-白屏"><a href="#2-1-白屏" class="headerlink" title="2.1 白屏"></a>2.1 白屏</h2><ul>
<li><p>白屏事件指的是浏览器开始显示内容的事件，一般认为浏览器开始渲染body或者解析完head标签的时候就是页面白屏结束的时间。</p>
</li>
<li><p>计算方法：</p>
<ul>
<li>IE8：title后输出一个时间pagestarttime，head结束前输出一个时间firstpaint。</li>
<li>白屏时间 = firstpaint - pagestartime / performance.timing.navigationStart</li>
</ul>
</li>
<li><p>优化：</p>
<ul>
<li>加快js的执行速度，比如无限滚动的页面，可以用js先渲染一个屏幕范围内的东西。</li>
<li>减少文件体积。</li>
<li>首屏同步渲染html，后续的滚屏再异步加载和渲染。</li>
</ul>
</li>
</ul>
<h1 id="2-2-首屏"><a href="#2-2-首屏" class="headerlink" title="2.2 首屏"></a>2.2 首屏</h1><ul>
<li><p>首屏时间是指用户打开网站开始，到浏览器首屏内容渲染完成的时间。</p>
</li>
<li><p>计算方法：</p>
<ul>
<li>模块标签标记。适用于内容不需要拉取数据才能生存以及页面不考虑图片等资源的加载情况。结束位置加时间戳输出时间。</li>
<li>统计首屏内图片加载最慢时间。通常图片加载最慢，所以会把首屏内加载最慢的图片的时间当作首屏时间。</li>
<li>自定义计算。（忽略图片等资源加载情况，只考虑页面主要DOM；只考虑首屏的主要模块，而不是严格意义首屏以上的所有内容。）</li>
</ul>
</li>
<li><p>优化：</p>
<ul>
<li>首屏数据拉取逻辑放在顶部，数据最快返回。</li>
<li>首屏渲染css及js逻辑优先内联html，返回时能立即执行。</li>
<li>次屏逻辑延后执行。</li>
</ul>
</li>
</ul>
<h2 id="2-3-DOM构建时间"><a href="#2-3-DOM构建时间" class="headerlink" title="2.3 DOM构建时间"></a>2.3 DOM构建时间</h2><p>浏览器开始对基础页文件内容进行解析，构建出一个DOM树的时间。domready事件在DOM加载后、资源加载之前被触发，在本地浏览器的DOMContentLoaded事件的形式被调用。</p>
<h2 id="2-4-整页时间"><a href="#2-4-整页时间" class="headerlink" title="2.4 整页时间"></a>2.4 整页时间</h2><ul>
<li>整个页面加载完成时间</li>
<li>loadEventEnd - navigationStart / onload记录时间戳</li>
</ul>
<h1 id="3-渲染相关"><a href="#3-渲染相关" class="headerlink" title="3. 渲染相关"></a>3. 渲染相关</h1><h2 id="3-1-如何减少重绘和回流"><a href="#3-1-如何减少重绘和回流" class="headerlink" title="3.1 如何减少重绘和回流"></a>3.1 如何减少重绘和回流</h2><ul>
<li>使用transform替代top</li>
<li>使用visibility替换display: none，前者只会引起重绘，后者会引发回流。</li>
<li>不使用table布局，很小的一个小改动有可能造成整个table的重新布局。</li>
<li>CSS选择符从右往左匹配查找，避免节点层级过多，过滤掉一些无关的样式规则和元素。</li>
<li>将频繁重绘或者回流的节点设置为图层，图层能够阻止该节点的渲染行为影响别的节点。（will-change属性、video和iframe标签等）</li>
<li>节点属性不要放在一个循环里当循环变量。</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">//每次都要去取正确的值才行</span></span><br><span class="line"><span class="keyword">for</span> (<span class="keyword">let</span> i = <span class="number">0</span>; i &lt; <span class="number">100</span>; i++) &#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="built_in">document</span>.querySelector(<span class="string">'.test'</span>).style.offsetTop);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="3-2-为什么操作DOM的性能很差？"><a href="#3-2-为什么操作DOM的性能很差？" class="headerlink" title="3.2 为什么操作DOM的性能很差？"></a>3.2 为什么操作DOM的性能很差？</h2><ul>
<li>因为DOM是属于渲染引擎中的东西，而JS又是JS引擎中的东西。当我们通过JS操作DOM的时候，其实这个<strong>操作涉及到了两个线程之间的通信</strong>，那么势必会带来一些性能上的损耗。</li>
<li>操作DOM次数一多，也就等同于一直在进行线程之间的通信，并且操作DOM<strong>可能还会带来重绘回流</strong>的情况，所以也就导致了性能上的问题。</li>
</ul>
<h2 id="3-3-插入-几万个DOM，如何实现页面不卡顿？"><a href="#3-3-插入-几万个DOM，如何实现页面不卡顿？" class="headerlink" title="3.3 插入 几万个DOM，如何实现页面不卡顿？"></a>3.3 插入 几万个DOM，如何实现页面不卡顿？</h2><blockquote>
<p>关键在于如何分批次部分渲染DOM。</p>
</blockquote>
<ul>
<li>通过requestAnimationFrame的方式去循环的插入DOM。</li>
<li>通过虚拟滚动。</li>
</ul>
<blockquote>
<p>虚拟滚动原理：只渲染可视区域内的内容，非可见区域的那就完全不渲染了，当用户在滚动的时候就实时去替换渲染的内容。即使列表很长，但是渲染的DOM元素永远只有那么几个，当我们滚动也米娜的时候就会去实时更新DOM。</p>
</blockquote>
<h2 id="3-4-不考虑缓存和优化网络协议的前提下，可以通过哪些方式来最快地渲染页面？"><a href="#3-4-不考虑缓存和优化网络协议的前提下，可以通过哪些方式来最快地渲染页面？" class="headerlink" title="3.4 不考虑缓存和优化网络协议的前提下，可以通过哪些方式来最快地渲染页面？"></a>3.4 不考虑缓存和优化网络协议的前提下，可以通过哪些方式来最快地渲染页面？</h2><blockquote>
<p>减少生成渲染树的时间。渲染树：DOM+CSSDOM。</p>
</blockquote>
<ul>
<li>考虑文件大小。</li>
<li>script标签使用上考虑async和defer。</li>
<li>需要下载的内容是否需要在首屏使用。</li>
<li>CSS、HTML的代码书写。</li>
</ul>
<h1 id="4-图片优化"><a href="#4-图片优化" class="headerlink" title="4. 图片优化"></a>4. 图片优化</h1><h2 id="4-1-图片大小如何优化？"><a href="#4-1-图片大小如何优化？" class="headerlink" title="4.1 图片大小如何优化？"></a>4.1 图片大小如何优化？</h2><ul>
<li>减少像素点。</li>
<li>减少每个像素点能够显示的颜色。</li>
</ul>
<h2 id="4-2-图片加载如何优化？"><a href="#4-2-图片加载如何优化？" class="headerlink" title="4.2 图片加载如何优化？"></a>4.2 图片加载如何优化？</h2><ul>
<li>用CSS去代替。</li>
<li>用CDN加载，计算出适配屏幕的宽度，然后去请求相应裁剪好的图片。</li>
<li>小图使用base64格式。base64编码就是可以将一张图片数据编码成一串字符串，使用该字符串代替图像地址url。</li>
<li>将多个图标文件整合到一张图片中（精灵图/雪碧图）。</li>
<li>选择正确的图片格式：<ul>
<li>尽量使用WebP格式，因为WebP格式具有更好的图像数据压缩算法，能带来更小的图片体积，而且拥有肉眼识别无差异的图片质量。</li>
<li>小图使用PNG，其实对于大部分图标这类图片，完全可以使用SVG代替。</li>
<li>照片使用JPEG。</li>
</ul>
</li>
</ul>
<h1 id="5-防抖、节流"><a href="#5-防抖、节流" class="headerlink" title="5. 防抖、节流"></a>5. 防抖、节流</h1><h2 id="5-1-防抖"><a href="#5-1-防抖" class="headerlink" title="5.1 防抖"></a>5.1 防抖</h2><p>如果在频繁的事件回调中做复杂计算，很有可能导致页面卡顿，不如<strong>将多次计算合并为一次计算，只在一个精确点做操作</strong>。频繁触发，有足够空闲事件才执行。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> debounce = <span class="function">(<span class="params">func, wait = <span class="number">50</span></span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">let</span> timer = <span class="number">0</span>;</span><br><span class="line">  <span class="keyword">return</span> <span class="function"><span class="keyword">function</span> (<span class="params">...args</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (timer) &#123;</span><br><span class="line">      clearTimeout(timer);</span><br><span class="line">      timer = setTimeout (<span class="function"><span class="params">()</span> =&gt;</span> &#123;</span><br><span class="line">        func.apply(<span class="keyword">this</span>, args);</span><br><span class="line">      &#125;, wait)</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="5-2-节流"><a href="#5-2-节流" class="headerlink" title="5.2 节流"></a>5.2 节流</h2><p>防抖动是将多次执行变为最后一次执行，节流是将多次执行变成每隔一段事件执行。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">throttle</span> (<span class="params">func, wait</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">let</span> last;</span><br><span class="line">  <span class="keyword">return</span> <span class="function"><span class="keyword">function</span> (<span class="params">...args</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">let</span> now =+ <span class="keyword">new</span> <span class="built_in">Date</span>();</span><br><span class="line">    <span class="keyword">if</span> (!last || now &gt; last + wait) &#123;</span><br><span class="line">      last = now;</span><br><span class="line">      func.apply(<span class="keyword">this</span>, args);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h1 id="6-预渲染"><a href="#6-预渲染" class="headerlink" title="6. 预渲染"></a>6. 预渲染</h1><p>通过预渲染将下载的文件预先在后台渲染，可以使用以下代码开启预渲染。</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">link</span> <span class="attr">rel</span> = <span class="string">"prerender"</span> <span class="attr">href</span> = <span class="string">"http://example.com"</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>预渲染虽然可以提高页面的加载速度，但是要确保该页面大概率会被用户在之后打开，否则就是白白浪费资源去渲染。</p>
<h1 id="7-懒执行"><a href="#7-懒执行" class="headerlink" title="7. 懒执行"></a>7. 懒执行</h1><ul>
<li>懒执行就是<strong>将某些逻辑延迟到使用时再计算</strong>。</li>
<li>该技术可以用于首屏优化，对于某些耗时逻辑并不需要在首屏就使用的，就可以使用懒执行。</li>
<li>懒执行需要唤醒，一般可以通过<strong>定时器或者事件的调用</strong>来唤醒。</li>
</ul>
<h1 id="8-懒加载"><a href="#8-懒加载" class="headerlink" title="8. 懒加载"></a>8. 懒加载</h1><ul>
<li>懒加载就是<strong>将不关键的资源延后加载。</strong></li>
<li>懒加载的原理：只加载自定义区域内需要加载的东西，通常是可视区域，但也可以是即将进入可视区域的区域。</li>
<li>对于图片来说，先设置图片标签的src属性为一张占位图，将真实的图片资源放入自定义属性中，当进入自定义区域时，就将自定义属性替换为src属性，这一图片就会去下载资源，实现了图片懒加载。</li>
<li>懒加载不仅可以用于图片，也可以使用在别的资源上。比如进入可视区域才开始播放视频等。</li>
</ul>
<h1 id="9-CDN"><a href="#9-CDN" class="headerlink" title="9. CDN"></a>9. CDN</h1><ul>
<li>CDN的原理：尽可能的在各个地方分布机房缓存数据，这一即使我们的跟服务器远在国外，在国内的用户也可以通过国内的机房迅速加载资源。</li>
<li>我们可以将静态资源尽量使用CDN加载，由于浏览器对于单个域名有并发请求上限，可以考虑使用多个CDN域名。并且对于CDN加载静态资源需要注意CDN域名要与主站不同，否则每次请求都会带上主站的cookie，平白消耗流量。</li>
</ul>
]]></content>
      <categories>
        <category>面试</category>
      </categories>
      <tags>
        <tag>面试</tag>
      </tags>
  </entry>
  <entry>
    <title>面试 | 网络</title>
    <url>/2021/06/02/%E7%BD%91%E7%BB%9C-1/</url>
    <content><![CDATA[<h1 id="1-OSI七层模型"><a href="#1-OSI七层模型" class="headerlink" title="1. OSI七层模型"></a>1. OSI七层模型</h1><ul>
<li>应用层：为应用程序提供服务，并管理应用程序之间的通信（SMTP、HTTP、FTP）。</li>
<li>表示层：处理数据的标识问题，比如编码、格式转化、加密解密等。</li>
<li>会话层：负责建立管理和断开通信连接，实现数据同步。</li>
<li>传输层：端到端传输数据，同时处理传输错误、控制流量等。（TCP、UDP）</li>
<li>网络层：地址管理、路由选择（IP协议）。</li>
<li>数据链路层：数据分割成帧、mac寻址、差错校检、信息纠正等。（以太网）</li>
<li>物理层：利用传输介质为数据链路层提供物理连接。</li>
</ul>
<blockquote>
<p>发送端从应用层👉物理层，打包发送。</p>
<p>接收层从物理层👉应用层，解析获取。</p>
</blockquote>
<p>路由器工作在哪一层？</p>
<ul>
<li><p>网络层。</p>
</li>
<li><p>路由器要连接因特网中各局域网、广域网的设备，它会根据信道的情况自动选择和设定路由，以最佳路径、先后顺序发送信号。</p>
</li>
<li><p>路由和交换机最主要的区别就是<strong>交换机在OSI参考模型第二层（数据链路层）</strong>，而<strong>路由发生在第三层（网络层）</strong>。</p>
</li>
<li><p>这一区别决定了路由和交换机在移动信息的过程中需要使用不同的控制信息，所以两者实现各自功能的方式是不同的。</p>
</li>
</ul>
<h1 id="2-DNS解析过程"><a href="#2-DNS解析过程" class="headerlink" title="2. DNS解析过程"></a>2. DNS解析过程</h1><ul>
<li>根DNS服务器：返回<strong>顶级域名</strong>DNS服务器的IP地址。</li>
<li>顶级域DNS服务器：返回<strong>权威域名</strong>DNS服务器的IP地址。</li>
<li>权威DNS服务器：返回<strong>相应主机</strong>的IP地址。</li>
</ul>
<p>以输入<code>www.google.com</code>为例：</p>
<ol>
<li>浏览器检查自身缓存，有无解析此域名对应的IP。</li>
<li>操作系统缓存hosts文件中查询。</li>
<li>没有的话，请求本地域名服务器（LDNS）解析域名，一般在城市某处，距离不会太远。</li>
<li>如果还没有的话，就去根DNS域服务器查询，此时会给出<code>.com</code>的顶级域名服务器。</li>
<li>然后去<code>.com</code>服务器查询，此时会给出这个域名<code>google.com</code>的地址，这是网站注册的域名服务器。</li>
<li>去NameServer查询，根据映射关系表找到目标IP，返回给LDNS（LDNS缓存域名及IP），</li>
<li>LDNS解析结果返回用户（缓存到系统缓存中），域名解析结束。</li>
</ol>
<h1 id="3-TCP和UDP"><a href="#3-TCP和UDP" class="headerlink" title="3. TCP和UDP"></a>3. TCP和UDP</h1><h2 id="3-1-UDP和TCP的区别"><a href="#3-1-UDP和TCP的区别" class="headerlink" title="3.1 UDP和TCP的区别"></a>3.1 UDP和TCP的区别</h2><p><img src="https://imgconvert.csdnimg.cn/aHR0cHM6Ly91c2VyLWdvbGQtY2RuLnhpdHUuaW8vMjAxOC80LzE5LzE2MmRiNWU5N2U5YTllMDE?x-oss-process=image/format,png" alt="TCP、UDP协议的区别"></p>
<ul>
<li><strong>UDP协议</strong>是面向无连接的，<strong>不需要在正式传递数据之前先连接起双方</strong>。UDP协议只是数据报文的搬运工，不保证有序且不丢失的传递到对端，并且UDP协议也没有任何控制流量的算法。总的来说，UDP相较于TCP更加的轻便。一般可以用于直播、即时通讯、即时游戏等。</li>
<li><strong>TCP</strong>无论是建立连接还是断开连接都需要先进行握手。在传输数据的过程中，通过各种算法保证数据的可靠性，带来的问题就是相比于UDP来说不那么的高效。</li>
</ul>
<h2 id="3-2-TCP建立连接–三次握手"><a href="#3-2-TCP建立连接–三次握手" class="headerlink" title="3.2 TCP建立连接–三次握手"></a>3.2 TCP建立连接–三次握手</h2><p><img src="https://imgconvert.csdnimg.cn/aHR0cDovL2ltZy5ibG9nLmNzZG4ubmV0LzIwMTcwNjA1MTEwNDA1NjY2?x-oss-process=image/format,png" alt="三次握手"></p>
<blockquote>
<p>起初，两端都是``CLOSED<code>状态。在通信开始前，双方都会创建TCB。服务器创建完TCB后便进入</code>LISTEN`状态，此时开始等待客户端发送数据。</p>
</blockquote>
<ul>
<li>第一次握手：<code>客服端</code>向服务端发送连接请求报文段。该报文段中包含自身的数据通讯初始序号。请求发送后，客户端便进入<code>SYN-SENT</code>状态。</li>
<li>第二次握手：<code>服务端</code>收到连接请求报文段后，如果同意连接，则会发送一个应答，该应答中也会包含自身的数据通讯初始序号，发送完成后便进入<code>SYN-RECEIVED</code>状态。</li>
<li>第三次握手：当客户端收到连接同意的应答后，还要向服务端发送一个确认报文。客户端发完这个报文段后便进入<code>ESTABLISHED</code>状态，服务端收到这个应答后也进入<code>ESTABLISHED</code>状态，此时连接建立成功。</li>
</ul>
<blockquote>
<ul>
<li>客户端–发送带有 SYN 标志的数据包–一次握手–服务端</li>
<li>服务端–发送带有 SYN/ACK 标志的数据包–二次握手–客户端</li>
<li>客户端–发送带有 ACK 标志的数据包–三次握手–服务端</li>
<li>SYN(synchronous建立联机) ACK(acknowledgement 确认) PSH(push传送) FIN(finish结束) RST(reset重置) URG(urgent紧急)Sequence number(顺序号码) Acknowledge number(确认号码)</li>
</ul>
</blockquote>
<h2 id="3-3-为什么TCP建立连接需要三次握手，明明两次就可以建立起连接？"><a href="#3-3-为什么TCP建立连接需要三次握手，明明两次就可以建立起连接？" class="headerlink" title="3.3 为什么TCP建立连接需要三次握手，明明两次就可以建立起连接？"></a>3.3 为什么TCP建立连接需要三次握手，明明两次就可以建立起连接？</h2><ul>
<li>三次握手的目的是建立可靠的通信通道，说到通信，简单来说就是数据的发送和接收，而三次握手最主要的目的就是双方确认自己与对方的发送与接收是正常的。</li>
<li>防止出现失效的连接请求报文段被服务端接收的情况，从而产生错误。</li>
<li>客户端发送了一个连接请求A，但是因为网络原因造成了超时，这是TCP会启动超时重传的机制再次发送一个连接请求B。此时请求顺利到达服务端，服务端应答完就建立了请求，然后接收数据后释放了连接。</li>
<li>假设这时候连接请求A在两端关闭后终于抵达了服务端，那么此时服务端会认为客户端又需要建立TCP连接，从而应答了该请求并进入<code>ESTABLISHED</code>状态，但此时客户端是<code>CLOSED</code>状态，那么就会导致服务端一直等待，造成资源的浪费。</li>
</ul>
<blockquote>
<p>第一次握手：Client什么都不能确认，Server确认了对方发送正常。</p>
<p>第二次握手：Client确认了：自己发送、接收正常，对方发送、接收正常；Server确认了：自己接收正常，对方发送正常。</p>
<p>第三次握手：Client确认了：自己发送、接收正常，对方发送、接收正常；Server确认了：自己发送、接收正常，对方发送接收正常。</p>
</blockquote>
<h2 id="3-4-TCP断开连接–四次挥手"><a href="#3-4-TCP断开连接–四次挥手" class="headerlink" title="3.4 TCP断开连接–四次挥手"></a>3.4 TCP断开连接–四次挥手</h2><p><img src="https://imgconvert.csdnimg.cn/aHR0cDovL2ltZy5ibG9nLmNzZG4ubmV0LzIwMTcwNjA2MDg0ODUxMjcy?x-oss-process=image/format,png" alt="四次挥手"></p>
<blockquote>
<p>TCP是全双工的，在断开连接时两端都要发送<code>FIN</code>和<code>ACK</code></p>
</blockquote>
<ul>
<li>第一次挥手：若客户端A认为数据发送完成，则它需要向服务端B发送连接释放请求。</li>
<li>第二次挥手：B收到连接释放请求后，会告诉应用层药是否TCP链接。然后会发生ACK包，并进入<code>CLOSE_WAIT</code>状态，此时表明A到B的连接已经释放，不再接收A发的数据了。但是因为TCP连接是双向的，所以B仍旧可以发送数据给A。</li>
<li>第三次挥手：B如果此时还有没发完的数据会继续发送，完毕后会向A发送连接释放请求，然后B便进入<code>LAST-ACK</code>状态。</li>
<li>第四次握挥手：A收到释放请求后，向B发送确认应答，此时A进入<code>TIME-WAIT</code>状态。该状态会持续<code>2MSL</code>（Maximun Segment Lifetime, 即报文最大生存时间，指报文段在网络中生存的事件，超时会被抛弃）。若该时间段内没有B的重发请求的话，就进入<code>CLOSED</code>状态。</li>
</ul>
<blockquote>
<p>通过延迟确认的技术（通常有时间限制，否则对方会误以为需要重传），可以将第二次和第三次握手合并，延迟ACK包的发送。</p>
</blockquote>
<p><a href="https://blog.csdn.net/qzcsu/article/details/72861891" target="_blank" rel="noopener">三次握手-四次挥手</a></p>
<h2 id="3-5-为什么客户端A要进入TIME-WAIT状态，等待2MSL时间后才进入CLOSED状态？"><a href="#3-5-为什么客户端A要进入TIME-WAIT状态，等待2MSL时间后才进入CLOSED状态？" class="headerlink" title="3.5 为什么客户端A要进入TIME-WAIT状态，等待2MSL时间后才进入CLOSED状态？"></a>3.5 为什么客户端A要进入TIME-WAIT状态，等待2MSL时间后才进入CLOSED状态？</h2><p>为了保证B能收到A的确认应答。若A发完确认应答后直接进入CLOSED状态，如果确认应答因为网络问题一直没有到达，那么会造成B不能正常关闭。</p>
<blockquote>
<p>第一，保证客户端发送的最后一个ACK报文能够到达服务器，因为这个ACK报文可能丢失，站在服务器的角度看来，我已经发送了FIN+ACK报文请求断开了，客户端还没有给我回应，应该是我发送的请求断开报文它没有收到，于是服务器又会重新发送一次，而客户端就能在这个2MSL时间段内收到这个重传的报文，接着给出回应报文，并且会重启2MSL计时器。</p>
<p>第二，防止类似与“三次握手”中提到了的“已经失效的连接请求报文段”出现在本连接中。客户端发送完最后一个确认报文后，在这个2MSL时间中，就可以使本连接持续的时间内所产生的所有报文段都从网络中消失。这样新的连接中不会出现旧连接的请求报文。</p>
</blockquote>
<h2 id="3-6-为什么建立连接是三次握手，关闭连接是四次挥手？"><a href="#3-6-为什么建立连接是三次握手，关闭连接是四次挥手？" class="headerlink" title="3.6 为什么建立连接是三次握手，关闭连接是四次挥手？"></a>3.6 为什么建立连接是三次握手，关闭连接是四次挥手？</h2><ul>
<li>建立连接的时候， 服务器在<code>LISTEN</code>状态下，收到建立连接请求的SYN报文后，把<code>ACK和SYN放在一个报文</code>里发送给客户端。</li>
<li>关闭连接时，服务器收到对方的FIN报文时，仅仅表示对方不再发送数据了但是还能接收数据，而自己也未必全部数据都发送给对方了，所以己方可以立即关闭，也可以发送一些数据给对方后，再发送FIN报文给对方来表示同意现在关闭连接，因此，己方<strong>ACK和FIN一般都会分开发送</strong>，从而导致多了一次。</li>
</ul>
<h2 id="3-7-TCP如何解决数据丢包或报文顺序不对的问题？"><a href="#3-7-TCP如何解决数据丢包或报文顺序不对的问题？" class="headerlink" title="3.7 TCP如何解决数据丢包或报文顺序不对的问题？"></a>3.7 TCP如何解决数据丢包或报文顺序不对的问题？</h2><blockquote>
<p>TCP有ARQ超时重传机制。（Automatic Repeat-reQuest）</p>
</blockquote>
<ul>
<li>一种是停止等待ARQ。<ul>
<li>A向B发送一个报文，同时启动定时器，如果<strong>超时就重新发送</strong>。B如果收到相同序号的报文就会丢弃重新应答。A接收相同序号应答也丢弃。</li>
<li>这种为单个单个传，效率比较低，但也不会有那种多个丢包的情况。</li>
</ul>
</li>
<li>另外一种是连续ARQ，高效一点。<ul>
<li>它用的窗口，A持续发这个窗口内的数据，B累计确认，收到多个后统一应答A，ACK标志告诉A，这个序号之前的数据已经收到。但如果A收到３个重复的ACK，那就说明有失序或丢包的情况，就会启用快速重传/快速恢复。</li>
<li>快速重传TCP taho，阙值设为当前窗口一半，窗口大小设为1开始慢开始，重新传送。</li>
<li>快速恢复TCP Remo，机制是窗口减半，却只为当前窗口，启用拥堵避免。不过它是重发接收端要的包，接收到一个ACK就退出。如果丢了很多个包就再3ACK触发一遍。</li>
<li><code>快恢复进行了优化——TCP New Reno。它的区别在于它**记下了这个发送段的最大序号，并且每次都比对**。比如说1-10，丢包丢了4，7。最大序号就是10。接收方发的ACK包是4、发送方发4，接收方收到4，发7。那么发送方就会对比7和10，知道是丢了多个包，发7。接收方收到，发11，发送方收到11后，对比10，比10大就退出快恢复阶段。</code></li>
</ul>
</li>
</ul>
<h2 id="3-8-TCP如何实现流量控制"><a href="#3-8-TCP如何实现流量控制" class="headerlink" title="3.8 TCP如何实现流量控制"></a>3.8 TCP如何实现流量控制</h2><ul>
<li><p>通过滑动窗口和拥堵窗口实现。</p>
</li>
<li><p>滑动窗口主要是用于接收方，保证接收方能够接受数据。接收方通过报文告知发送方当前接受窗口剩余大小，发送窗口根据该值变化大小滑动窗口（待发送区）发送报文。</p>
</li>
<li><p>拥堵窗口，主要用于网络，防止过多的数据拥堵网络，避免负载过大的情况。</p>
</li>
</ul>
<h1 id="4-HTTP协议"><a href="#4-HTTP协议" class="headerlink" title="4. HTTP协议"></a>4. HTTP协议</h1><h2 id="4-1-POST和GET有什么区别"><a href="#4-1-POST和GET有什么区别" class="headerlink" title="4.1 POST和GET有什么区别"></a>4.1 POST和GET有什么区别</h2><ul>
<li><p>用法：GET一般用于无副作用、幂等的场景。POST多用于有副作用、不幂等的情况。</p>
<blockquote>
<p>幂等：发送M和N次请求，服务器资源状态一致。比如说，注册10个账号和11个账号是不幂等的，对文章进行了10次和11次修改是幂等的，因为前者多了一个账号（资源），后者是更新同一个资源。</p>
<p>副作用：副作用是指对服务器上资源做改变。比如搜索是无副作用的，但更新是有副作用的。</p>
</blockquote>
</li>
<li><p>从本质上来说，POST和GET都取决于HTTP，<strong>使用哪个方法与应用层传输没有必然的联系</strong>。HTTP 没有要求，如果是POST，数据就要放在BODY中。也没有要求GET，数据（参数）就一定要放在URL中而不能放在BODY中。</p>
</li>
<li><p>GET能请求缓存，但是POST不可以。</p>
</li>
<li><p>POST支持更多编码类型。</p>
</li>
<li><p>GET回退无害，POST会再次提交。</p>
</li>
<li><p>GET能被保存为书签，而POST不可以。</p>
</li>
<li><p>由于浏览器URL有限制，所以GET的长度受限，但POST不受限（因为都在BODY中）。</p>
</li>
</ul>
<h1 id="4-2-HTTP常用首部"><a href="#4-2-HTTP常用首部" class="headerlink" title="4.2 HTTP常用首部"></a>4.2 HTTP常用首部</h1><ul>
<li><p>通用首部：</p>
<ul>
<li>cache-control：控制缓存行为</li>
<li>connection：连接的性质，比如keep-alive</li>
<li>user-Agent：用户信息</li>
<li>Date：报文创建时间</li>
</ul>
</li>
<li><p>请求：</p>
<ul>
<li>Referrer Policy：表示来源的（浏览器所访问的前一个页面），可以用于辅助检测crsf估计，一般浏览器的默认值是<code>no-referrer-when-downgrade</code>，意思是https降级http的时候不传源地址。</li>
<li>Accept：能正确接收的媒体类型。</li>
<li>Accept-XX（Charset/Encoding/Language）：能正确接收的XX。</li>
<li>Expect：期望服务端的指定行文。</li>
<li>If-Watch：两端资源标记比较。</li>
<li>If-Modified-Since：比较时间，未修改返回<code>304 Not Modified</code>。</li>
<li>If-None-Match：比较标记，未修改返回<code>304 Not Modified</code>。</li>
</ul>
</li>
<li><p>响应：</p>
<ul>
<li>Location：重定向到某个Location。</li>
<li>Server：服务器名字。</li>
<li>Age：响应存在时间。</li>
<li>Accept-Ranges：可以接收的范围类型。</li>
</ul>
</li>
</ul>
<h2 id="4-3-http请求中connection-keep-alive的意义在哪里"><a href="#4-3-http请求中connection-keep-alive的意义在哪里" class="headerlink" title="4.3 http请求中connection=keep-alive的意义在哪里"></a>4.3 http请求中<code>connection=keep-alive</code>的意义在哪里</h2><ul>
<li>HTTP是基于TCP的，每一个HTTP请求都需要进行三次握手。</li>
<li>如果一个页面对某一个域名有多个请求，就会进行频繁的建立连接和断开连接。所以HTTP1.0中出现了<code>Connection：keep-alive</code>，用于建立长连接。</li>
<li>Keep-Alive模式更加高效，因为避免了<strong>连接建立和释放的开销</strong>。但是，长时间的TCP连接容易导致系统资源无效占用，配置不当的keep-alive有时比重复利用连接带来的损失还更大。所以，<strong>正确设置keep-alive timeout时间</strong>非常重要。</li>
</ul>
<h2 id="4-4-http请求中cache-control有哪些参数可以设置"><a href="#4-4-http请求中cache-control有哪些参数可以设置" class="headerlink" title="4.4 http请求中cache-control有哪些参数可以设置"></a>4.4 http请求中cache-control有哪些参数可以设置</h2><ul>
<li>Public：表示任何缓存都可以缓存响应。</li>
<li>Private：表示响应<strong>仅供单个用户使用</strong>，不得由共享高速缓存存储。私有缓存可以存储响应。</li>
<li>No-cache：强制缓存在发布缓存副本之前将请求提交<strong>到源服务器以进行验证</strong>。</li>
<li>No-store：缓存不应该存储有关<strong>客户端请求</strong>或<strong>服务器响应</strong>的任何内容。</li>
</ul>
<h2 id="4-5-HTTP状态码"><a href="#4-5-HTTP状态码" class="headerlink" title="4.5 HTTP状态码"></a>4.5 HTTP状态码</h2><ul>
<li>1XX：通知<ul>
<li>100：Continue，客户端重新发请求。</li>
<li>101：Switching Protocols，改用协议（http换到https或者http1.1换到2.0之类的）。</li>
</ul>
</li>
<li>2XX：成功<ul>
<li>200：OK 操作成功</li>
<li>201：Created，按照客户端请求创建了一个<strong>新资源</strong>。</li>
<li>202：Accepted，请求无法或不被实时处理。</li>
<li>204：No Content，请求成功，但是报文不含实体的主体部分。</li>
<li>205：Reset Content，请求成功，但是报文不含实体主体部分，要求客户端重置内容。</li>
<li>206：Partial Content，进行范围请求。</li>
</ul>
</li>
<li>3XX：重定向<ul>
<li>301：Moved Permanently，永久性重定向，资源已经被分配到了新的URL</li>
<li>302：Found，临时重定向，资源临时分配了URL，实际上部分客户端把它当成303处理</li>
<li>303：See Other，表示资源存在另一个URL，应用GET获取资源</li>
<li>304：Not Modified，允许访问资源，但实体主体为空（客户端已经有此数据，不需要再次发送）</li>
<li>307：Temporary Redirect，临时重定向，资源临时分配了URL，但是希望客户端能够保持方法不变，请求新地址（解决302被当成303处理的问题）</li>
</ul>
</li>
<li>4XX：客户端错误<ul>
<li>400：Bad Request，请求报文语法错误。</li>
<li>401：Unauthorized，发送的请求需要通过验证，客户端视图对一个受保护的资源操作但没有认证证书。</li>
<li>403：Forbidden，请求资源存在但被拒绝，常用于一个资源只允许在特定时间段内访问（如果不想透露可以谎报404）。</li>
<li>404：Not Found，找不到请求的资源。</li>
<li>405：Method Not Allowed，不支持的请求方法，比如只支持GET，但是收到了POST请求。</li>
</ul>
</li>
<li>5XX：服务端错误<ul>
<li>500：Internal Server Error，执行请求时发送错误，处理异常。</li>
<li>501：Not Implemented，不支持此请求方法（和405区别在于，405是访问的资源不支持，而501表示服务器不能操作此方法。</li>
<li>502：Bad Gateway，代理与上行服务器之间出现问题。</li>
<li>503：Service Unavailable，服务器暂时处于超负荷或者维护中。</li>
</ul>
</li>
</ul>
<h3 id="4-5-1-状态码返回204可能的原因"><a href="#4-5-1-状态码返回204可能的原因" class="headerlink" title="4.5.1 状态码返回204可能的原因"></a>4.5.1 状态码返回204可能的原因</h3><ul>
<li>服务器拒绝请求返回。</li>
<li>GET资源存在但表示是空的，服务器通过这个响应代码告诉客户端：客户端的输入已被接受，但客户端不应该改变任何UI元素。</li>
</ul>
<h3 id="4-5-2-状态码204和205的区别"><a href="#4-5-2-状态码204和205的区别" class="headerlink" title="4.5.2 状态码204和205的区别"></a>4.5.2 状态码204和205的区别</h3><ul>
<li>204和205的区别在于205要求了重置。</li>
<li>用一个表单为例，如果提交后返回204，那么表单里的各个字段值不变，可以就绪修改它们。但如果得到的响应代码为205，那么表单里的各个字段将被重置为它们的初始值。</li>
</ul>
<h3 id="4-5-3-状态码304的可能情况"><a href="#4-5-3-状态码304的可能情况" class="headerlink" title="4.5.3 状态码304的可能情况"></a>4.5.3 状态码304的可能情况</h3><blockquote>
<p>这个与浏览器的协商缓存有关，用时间来说明。</p>
</blockquote>
<ul>
<li>当用户第一次请求资源A时，服务器会添加一个名为<code>Last-Modified</code>的响应头，这个头说明了A的<strong>最后修改时间</strong>，浏览器会把A的内容以及最后的响应时间缓存下来。</li>
<li>当用户第二次请求A时，在请求中包含一个名为<code>If-Modified-Since</code>请求头，它的值就是第一次请求时服务器通过<code>Last-Modified</code>响应头发送给浏览器的值，即资源A最后的修改时间。</li>
<li><code>If-Modified-Since</code>请求头就是在告诉浏览器，我这里浏览器缓存的A的最后修改时间是这个，如果A最后的修改时间还是这个，就不用响应这个请求，会把缓存里的内容直接显示出来。</li>
<li>服务器会获取<code>If-Modified-Since</code>值，与A当前的最后修改时间做对比。如果<strong>相同，服务器返回304状态码</strong>，表示A与浏览器上次缓存的相同，无需再次发送，浏览器可以显示自己的缓存页面。如果<strong>对比不同</strong>，那么说明A已经改变，<strong>服务器会返回200状态码</strong>。</li>
</ul>
<h2 id="4-6-http协议中的长短连接和长短轮询"><a href="#4-6-http协议中的长短连接和长短轮询" class="headerlink" title="4.6 http协议中的长短连接和长短轮询"></a>4.6 http协议中的长短连接和长短轮询</h2><ul>
<li>短连接：所谓短连接，即<strong>连接只保持在数据传输过程</strong>，请求发起、请求建立、数据返回、连接关闭。它适用于一些<strong>实时数据请求</strong>，配合轮询来进行新旧数据的更替。</li>
<li>长连接：长连接便是在连接发起后，在<strong>请求关闭连接前客户端与服务器都保持连接</strong>，实质是保持这个通信管道，之后便可以对其进行复用。</li>
<li>短轮询：短轮询指的是在<strong>循环周期内，不断发起请求，每一次请求都立即返回结果</strong>，根据新旧数据对比决定是否使用这个结果。</li>
<li>长轮询：长轮询是在请求的过程中，若是<strong>服务端数据并没有更新</strong>，那么则将这个连接挂起，<strong>直到服务器推送新的数据</strong>，再返回，然后再进入循环周期。</li>
</ul>
<h1 id="5-Https与Http"><a href="#5-Https与Http" class="headerlink" title="5. Https与Http"></a>5. Https与Http</h1><h2 id="5-1-Https为什么不安全"><a href="#5-1-Https为什么不安全" class="headerlink" title="5.1 Https为什么不安全"></a>5.1 Https为什么不安全</h2><ul>
<li>数据以<strong>明文</strong>传递，有被<strong>窃听</strong>的风险。</li>
<li>接收到的报文<strong>无法证明是发送时的报文</strong>，不能保证完整性，因此报文有被<strong>篡改</strong>的风险。</li>
<li><strong>不验证通信两端的身份</strong>，请求或响应有被<strong>伪造</strong>的风险。</li>
</ul>
<h2 id="5-2-Http和Https有什么区别"><a href="#5-2-Http和Https有什么区别" class="headerlink" title="5.2 Http和Https有什么区别"></a>5.2 Http和Https有什么区别</h2><ul>
<li>HTTP是超文本传输协议，信息是<strong>明文传输</strong>，HTTPS则是具有安全性的<strong>SSL加密传输协议</strong>。</li>
<li>HTTP和HTTPS使用的是完全不同的连接方式，用的端口也不一样，前者是80，后者是443。</li>
<li><strong>HTTPS协议需要CA申请证书</strong>，一般免费证书较少，因为需要一定费用。</li>
<li>HTTP的连接很简单，是无状态的。HTTPS协议是由<strong>TSL协议</strong>进行了加密，比HTTP协议安全。</li>
</ul>
<h2 id="5-3-TLS"><a href="#5-3-TLS" class="headerlink" title="5.3 TLS"></a>5.3 TLS</h2><blockquote>
<p>HTTPS仍用HTTP传输信息，但信息通过TLS进行了加密。</p>
<p>TSL作用于表示层。</p>
</blockquote>
<h3 id="5-3-1-两种加密技术"><a href="#5-3-1-两种加密技术" class="headerlink" title="5.3.1 两种加密技术"></a>5.3.1 两种加密技术</h3><ul>
<li>对称加密：两边有相同的密钥，都知道如何加密解密。</li>
<li>非对称加密：数据公钥加密，私钥解密，私钥只有发出公钥的一方知道。</li>
<li>对称加密问题在于如何让双方都知道密码，而不被其他人知道。非对称加密可以完美解决对称加密存在的问题。</li>
<li>流程：服务器将公钥发散出去，之后客户端创建一个密钥，用此公钥加密后发送给服务器，服务器用私钥解密，就能得知此密钥。双方都知道密码，之后就可以采用对称加密的方式进行数据传输了。</li>
</ul>
<h3 id="5-3-2-TSL的握手过程"><a href="#5-3-2-TSL的握手过程" class="headerlink" title="5.3.2 TSL的握手过程"></a>5.3.2 TSL的握手过程</h3><ul>
<li><p>客户端发送一个随机值<strong>ClientHello</strong>，并附带上支持的协议和支持的加密方式。</p>
</li>
<li><p>服务端收到一个随机值<strong>ClientHello</strong>，产生一个随机值<strong>ServerHello</strong>，根据客户端需求的协议返回，从客户端支持的对称加密算法中选一个作为最终通讯的算法，并发送自己的CA证书。</p>
</li>
<li><p>客户端收到服务器CA证书、协商的通讯加密算法、随机值<strong>ServerHello</strong>，验证通过后再生成第三个随机值<strong>预主密钥Pre-Master</strong>，用CA证书公钥加密后发送给服务器（如果需要验证证书，还要附带上证书）。</p>
<blockquote>
<p>用三个随机值+协商的加密算法，合成最终通讯的密钥。</p>
<p>change-cipher-spec Finished 客户端握手结束通知。</p>
</blockquote>
</li>
<li><p>服务端收到加密过的随机值之后，私钥解密获得第三个随机值<strong>预主钥Pre-Master</strong>。</p>
<blockquote>
<p>使用第一、二步中协商的对称加密算法+三个随机值合成最终的协商密钥。</p>
</blockquote>
</li>
<li><p>此时两端都有了最终的协商密钥了，接下来的传输就用这个密钥加密解密。</p>
</li>
<li><p>在<strong>TLS握手阶段，两端使用非对称加密的方式来通信</strong>，但是因为<strong>非对称加密损耗的性能比对称加密大</strong>，所以在<strong>正式传输数据时，两端传输其实是使用对称加密的方式通信。</strong></p>
</li>
</ul>
<h2 id="5-4-HTTPS的缺点"><a href="#5-4-HTTPS的缺点" class="headerlink" title="5.4 HTTPS的缺点"></a>5.4 HTTPS的缺点</h2><ul>
<li>通信两端都需要进行加密和解密，会消耗大量的CPU、内存等资源，增加服务器的负载。</li>
<li>加密运算和多次握手降低了访问速度。</li>
<li>开发阶段，加大了页面调试难度。由于信息被加密了，用代理工具的话，需要先解密然后才能看到真实信息。</li>
<li>用HTTPS访问的页面，页面内的外部资源都得用HTTPS请求，包括脚本中的AJAX请求。</li>
</ul>
<h2 id="5-5-HTTPS的单向认证和双向认证"><a href="#5-5-HTTPS的单向认证和双向认证" class="headerlink" title="5.5 HTTPS的单向认证和双向认证"></a>5.5 HTTPS的单向认证和双向认证</h2><ul>
<li>单向认证：<ul>
<li>客户端保存着服务器的证书并信任该证书。</li>
<li>https一般是单向认证，这样可以让绝大部分人都可以访问你的站点。</li>
</ul>
</li>
<li>双向认证：<ul>
<li>先决条件是有2个或者2个以上的证书，一个是服务器证书，其他是客户端证书。</li>
<li>服务器保存着客户端的证书并信任该证书，客户端保存着服务器的证书并信任该证书。这样，在证书验证成功的情况下即可完成请求响应。</li>
<li>双向认证一般用于企业应用对接（比如堡垒机）。</li>
</ul>
</li>
</ul>
<h1 id="6-HTTP2-0"><a href="#6-HTTP2-0" class="headerlink" title="6. HTTP2.0"></a>6. HTTP2.0</h1><h2 id="6-1-http1-0和gttp2-0的区别是什么"><a href="#6-1-http1-0和gttp2-0的区别是什么" class="headerlink" title="6.1 http1.0和gttp2.0的区别是什么"></a>6.1 http1.0和gttp2.0的区别是什么</h2><ul>
<li><p>http2.0是对http1.0的改进，相较于http1.0<strong>更快更高效</strong>。</p>
</li>
<li><p>http2.0实现了<strong>多路复用</strong>，用<strong>一个TCP进行连接共享，一个请求对应一个id</strong>，这样就可以发送多个请求，接收方通过id来响应不同的请求，<strong>解决了http1.0队首阻塞和连接过多的问题</strong>。因为http2.0在同一域名<strong>不论访问多少文件都只有一个连接</strong>，所以对服务器而言，提升的并发量是很大的。</p>
</li>
<li><p>http2.0引入了<strong>二进制数据帧和流</strong>的概念，数据拆分成数据帧传输，并进行顺序表示，接收方收到数据后按序组合即可获取正确数据。这样就可以<strong>并行传输</strong>了，解决了<strong>http1.0只能串行传输</strong>的问题。</p>
</li>
<li><p>http2.0<strong>压缩头部</strong>，使用序号对头部编码，在两端备份索引表，通过<strong>对编码进行比较来判断是否需要传输，减少了需要传输的大小</strong>。解决了http1.0中<strong>头部反复传输资源浪费</strong>的问题。</p>
</li>
<li><p>http2.0中，服务器可以在客户端某个请求后，<strong>主动推送一些客户端一定需要的资源</strong>，这样也能<strong>减少请求的数目</strong>。</p>
</li>
<li><p>http2.0在出现丢包的情况时，需要重新传输，后面的数据也就被阻塞了，但是http1.0因为有多个连接，所以不会影响其他连接的传输，这样的话http2.0的性能反倒不如http1.0。</p>
<blockquote>
<p>这个主要是TCP的问题，解决方案：基于QUIC协议的3.0，多路复用（ID识别），纠错机制。</p>
</blockquote>
</li>
</ul>
<h2 id="6-2-新特性"><a href="#6-2-新特性" class="headerlink" title="6.2 新特性"></a>6.2 新特性</h2><ul>
<li>多路复用：通过<strong>一个TCP连接传输所有数据</strong>。一个请求对应一个id，这样一个链接上可以有多个请求，每个连接的请求可以随机地混杂在一起，接收方可以根据请求的id将请求再归属到各自不同的服务器端请求里面。</li>
<li>二进制分帧层：HTTP2.0<strong>性能增强的关键</strong>，它<strong>改变了通信两端交互数据的方式</strong>，原先是以文本传输，现在要先对数据进行二进制编码，再把数据分成一个一个的帧，接着把帧送到数据流中，最后对方接受帧并拼接成一条消息，再处理请求。</li>
<li>首部压缩：HTTP1.X的header带有大量信息，而且每次都要重复发送。http2.0<strong>使用encoder来减少需要传输的header大小</strong>，通讯双方各自cache一份header fields表，既<strong>避免了重复header的传输，又减小了需要传输的大小</strong>。</li>
<li>服务器推送：HTTP2.0<strong>支持服务器主动推送</strong>，简单地说就是<strong>一次请求返回多个响应</strong>，这也是一减少HTTP请求的方法。服务器除了处理最初的请求外，还会额外push客户端一定会请求的资源，无需客户端发出明确的请求。</li>
</ul>
<h2 id="6-3-存在问题"><a href="#6-3-存在问题" class="headerlink" title="6.3 存在问题"></a>6.3 存在问题</h2><ul>
<li>http2.0使用了多路复用，一般来说同一域名下只需要使用一个TCP连接。</li>
<li>但是当连接中出现丢包时，整个TCP都要开始等待重传，后面的数据也都被阻塞了。而http1.0可以开启多个连接，只会影响一个，不会影响其他的。</li>
<li>所以在丢包情况下，http2.0的情况反而不如http1.0。</li>
</ul>
<h1 id="7-HTTP3-0"><a href="#7-HTTP3-0" class="headerlink" title="7. HTTP3.0"></a>7. HTTP3.0</h1><blockquote>
<p>为了解决2.0丢包性能的问题，Google基于UDP提出了QUIC协议。</p>
<p>HTTP3.0中的底层支撑协议就是QUIC。所以HTTP3.0也叫HTTP-over-QUIC。</p>
</blockquote>
<h2 id="7-1-QUIC协议"><a href="#7-1-QUIC协议" class="headerlink" title="7.1 QUIC协议"></a>7.1 QUIC协议</h2><blockquote>
<p>UDP协议高效，但不可靠。QUIC基于UDP，在原来的基础上结合了TCP和HTTP的精华使它可靠。</p>
</blockquote>
<h2 id="7-2-特点"><a href="#7-2-特点" class="headerlink" title="7.2 特点"></a>7.2 特点</h2><ul>
<li>多路复用：HTTP2虽然是多路复用，但是TCP协议是没有这个功能的。QUIC原始就包含这个功能，并且传输的单个数据流可以保证有序交付且不会影响其他数据流。其在移动端也会比TCP好，因为TCP基于IP+端口识别连接，不适合多变的网络环境，但是<strong>QUIC是通过ID识别连接，不论网络如何变化，只要ID不变，就能迅速连上</strong>。</li>
<li>纠错机制：如果要发送三个包，协议会算出三个包的异或值并单独发出一个校验包，也就是总共发出了四个包。当出现其中的非校验包丢包的情况时，可以通过另外三个包计算出丢失的数据包的内容。这种技术只能使用在丢失一个包的情况下，如果出现丢失多个包就不能使用纠错机制了，只能使用重传的方式。</li>
<li>０-RTT：通过类似TCP快速打开的技术，缓存当前会话的上下文，在下次恢复会话的时候，只需要将之前的缓存传递给服务端验证通过就可以进行传输了。</li>
</ul>
<h1 id="8-输入URL到页面渲染的整个流程"><a href="#8-输入URL到页面渲染的整个流程" class="headerlink" title="8. 输入URL到页面渲染的整个流程"></a>8. 输入URL到页面渲染的整个流程</h1><ol>
<li><p>DNS解析：如果这一步做了智能DNS解析的话，会提供访问速度最快的IP地址回来，如果没有的话，会进行DNS迭代查询。</p>
</li>
<li><p>TCP握手：</p>
<ul>
<li><p>如果是https的话，还会TCP握手结束后就会及进行TLS握手，然后就开始正式地传输数据。</p>
</li>
<li><p><strong>应用层会下发数据到传输层</strong>，这里TCP协议会指明两端的端口号，然后<strong>下发给网路层</strong>。网络层中的IP协议会确定IP地址，并且指示了数据传输中如何跳转路由器。然后包会<strong>再被封装到数据链路层的数据帧结构中</strong>，最后就是<strong>物理层面</strong>的传输了。</p>
</li>
<li><p>数据在进入服务端之前，可能还会先经过负责负载均衡的服务器，它的作用就是<strong>将请求合理地分发到多台服务器上</strong>，这时假设服务端会响应一个HTML文件。</p>
</li>
<li><p>接收到响应的文件之后，首先浏览器会判断状态码是什么，如果是200那就继续解析，如果400或500的话就会报错，如果300的话会进行重定向。</p>
</li>
<li><p>浏览器开始解析文件，如果是gzip格式的话会先解压一下，然后通过文件的编码格式知道该如何去解码文件。</p>
</li>
<li><p>文件解码成功后会正式开始渲染流程，先会根据HTML构建DOM树，有CSS的话会去构建CSSOM树。如果遇到script标签的话，会判断<strong>是否存在async或者defer</strong>，前者会并行进行下载并执行JS，后者会先下载文件，然后等待HTML解析完成后顺序执行。如果以上都没有，就会阻塞住渲染流程知道JS执行完毕。</p>
</li>
<li><p>遇到文件下载的话会去下载文件，这里如果使用HTTP/2协议的话会极大地提高多图的下载效率。</p>
</li>
<li><p>CSSOM树和DOM树构建完成后会开始生成Render树，这一步就是确定页面元素的布局、样式等等诸多方面的东西。</p>
</li>
<li><p>在生成Render树的过程中，浏览器就开始调用GPU绘制，合成图层，将内容显示在屏幕上了。</p>
<blockquote>
<p>另外就是：DOMContentLoader事件触发代表初始的HTML被完全加载和解析，不需要等待CSS、JS、图片加载。Load时间触发代表页面中的DOM、CSS、JS、图片已经全部加载完毕。</p>
</blockquote>
</li>
</ul>
</li>
<li><p>最后就是TCP断开连接，四次挥手</p>
<blockquote>
<p>客户端：我没有数据要发送了，准备挂了</p>
<p>服务器：收到，但我还有一些数据没发送完，稍等一下 ……</p>
<p>服务器：好了，发送完了，可以断开连接了</p>
<p>客户端：OK，你断开连接吧（内心独白：我将会在2倍的最大报文段生存时间后关闭连接，如果再收到服务器的消息，那么服务器就是没听到我最后这句话，我就再发送一遍）</p>
</blockquote>
</li>
</ol>
<p>仅用于自己复习…来自：<a href="https://blog.nowcoder.net/n/d5e2c1b0abf1416e8379c0c24eb3458a" target="_blank" rel="noopener">前端复习企划7-网络</a></p>
]]></content>
      <categories>
        <category>面试</category>
      </categories>
      <tags>
        <tag>面试</tag>
        <tag>网络</tag>
      </tags>
  </entry>
</search>
